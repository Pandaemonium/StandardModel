# Aristotle task: Electroweak orbit obstruction: kernel u(1)_em and rank three

- project_id: 48a2d51f-c9b3-470b-8ac8-bbe07705ce3d
- status: integrated
- submitted: 2026-06-26
- job_type: proof
- source_prompt: AgentTasks/aristotle-submit/null-edge-ew-stabilizer-wave1-20260626-project/PROMPT.md
- submission_project: AgentTasks/aristotle-submit/null-edge-ew-stabilizer-wave1-20260626-project
- context_pack: AgentTasks/context-packs/null-edge-unified-mass-wave1-20260626-20260626-072657.md
- expected_output: PhysicsSM/Draft/NullEdgeElectroweakStabilizer.lean

## Purpose

This job is part of the first Aristotle wave for the null-edge unified mass program. It follows the returned strategy report and focuses on one high-leverage finite proof or audit target.

## Integration notes

When the job returns, review semantic alignment before promoting any proof result from PhysicsSM/Draft/. For proof jobs, check conventions, signs, hidden assumptions, and whether the statement proves the intended mathematical claim rather than a weakened substitute.

## Integration (2026-06-26)

Integrated into `PhysicsSM/Draft/NullEdgeElectroweakStabilizer.lean` (namespace
`PhysicsSM.Draft`, imports `Mathlib`), wired into `PhysicsSMDraft.lean`; report at
`AgentTasks/null-edge-ew-stabilizer-report.md` (T9 stage 1). Headline results:
`ew_stabilizer_kernel` (`ker B_EW = span_R {Q}`, the `u(1)_em` line generated by the
charge `Q = T_3 + Y/2`), `ew_stabilizer_kernel_finrank` (= 1), `ew_stabilizer_rank`
(= 3, via rank-nullity on `R^4 -> C^2`), `ew_quadratic_form_kernel` (the quadratic
obstruction `q(X) = ||B_EW X||^2` vanishes exactly on `span_R {Q}`), and
`B_EW_eq_matrix` (closed form = `X -> rho(X) H_0`). Hermitian generators
`T_a = sigma_a/2`, `Y/2 = (1/2) I`, `Y(H)=1`, vacuum `H_0=(0, v/sqrt 2)`, `v != 0`;
the `i` factor of the anti-Hermitian convention is an overall scalar irrelevant to
kernel/rank. Stated as a structural stabilizer/orbit-obstruction theorem, not a
prediction; no "gauge symmetry breaking" language. Note: the file declares generic
names (`T1`/`T2`/`T3`/`rho`/`H0`/`kappa`/`B_EW`/`qform`) directly in `PhysicsSM.Draft`;
verified collision-free against the full Draft tree by a complete `lake build PhysicsSMDraft`
(8670 jobs, exit 0). `lake env lean` exit 0; `#print axioms` only
`propext, Classical.choice, Quot.sound`; pre-commit clean. Stage-2 coefficient
normalization (`m_W`, `m_Z`, `m_gamma`) left for follow-up.
