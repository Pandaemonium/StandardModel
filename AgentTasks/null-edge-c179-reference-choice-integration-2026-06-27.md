# Summary of changes for run c228e0c9-1c89-41a3-9f29-268719dd1cc1
C179 reference-choice audit delivered as `C179_reference_choice_audit.md`. This is a strategy/audit job; no Lean obligation was discharged and Gate C1 remains open.

Blunt bottom line:

**Ranked first physical reference:** (1) Wilson/Neuberger overlap — the only candidate where single-sector content is a *theorem* (Ginsparg–Wilson identity → index/gap), exactly what the C178 warning demands; (2) abstract block, adopted only as the scaffold/interface underneath Rank 1, never as a closure; (3) domain-wall (best second reference / cross-check, but carries a residual-mass caveat); (4) Adams/staggered-overlap (texture-economical but needs an extra taste-index theorem); (5) CKM/tuned naive flavored-overlap — DISQUALIFIED as a first target, since it is precisely the literal naive-product operator that re-imports C175's 8+8.

**"Doubler-resolved" criteria (D1–D6):** correct continuum index/mode-count with no hidden factor-16; an O(1/a) spectral gap on doubler branches compatible with the C170R constants (kappa, omega, rho); a Ginsparg–Wilson (or equivalent) exact-chiral identity (tuning alone does NOT qualify); no ghost zeros of the inverse at doubler corners; locality on admissible backgrounds; and gauge consistency under `SMActsInternally` (inherits C177).

**CKM texture without the 8+8 trap:** factor as `D_kinetic(Neuberger, resolved) ⊗ M_flavor(CKM)` where the CKM mass texture is a *momentum-independent flavor factor* (no Brillouin-zone dependence). Then D1–D6 are properties of the kinetic kernel alone and CKM cannot spoil a proved index — this is the operational meaning of "CKM as texture, not as the operator."

**C159 certificate per reference:** Neuberger → GW identity + overlap index theorem (correct normalization) + locality + sub-gap compatibility; Adams → stacked taste-index + overlap GW; domain-wall → 5D→4D effective-overlap equivalence + residual-mass bound + approximate-GW with explicit error; naive CKM → an independent resolution proof that is honestly expected to be unprovable as stated (a no-go restatement); abstract block → only the `IsDoublerResolved` interface plumbing, nothing physical.

**Red flags / no-go cases:** R1 naive product as operator (C175 redux); R2 "tuned" passing for "resolved" (no GW backing); R3 ghost propagator zeros at doubler corners; R4 gauge/flavor mixing via a non-internal lifting term (breaks C177); R5 scaffold mistaken for closure (any unfilled `IsDoublerResolved` silently closing C1); R6 domain-wall residual mass swept under the rug; R7 lost locality on non-admissible backgrounds.

The document records the full scoring table and reasoning. Gate C1 is NOT closed.
