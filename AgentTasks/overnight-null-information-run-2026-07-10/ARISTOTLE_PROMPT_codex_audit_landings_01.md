# Codex audit 01: operational locality, finite SSB, and generated scale

Act as an independent semantic auditor. Do not edit files and do not run a full
project build. Read the four supplied fresh Lean modules line by line and return
`LANDING_AUDIT_REPORT.md` in ASCII Markdown.

Audit:

- `FiniteNoSignaling.lean`;
- `TwoRegionTensorMicrocausality.lean`;
- `FiniteSSBDegeneracyNoGo.lean`;
- `OneLoopDimensionalTransmutation.lean`;
- `OvernightTheoryAxiomGuard.lean`.

For every public declaration:

1. paraphrase exactly what its type proves;
2. test vacuity, hollow telescoping, false shape, and docstring-overreach;
3. identify hidden assumptions, zero-dimensional edge cases, and normalization
   or tensor-factorization inputs;
4. verify that the explicit witness/control genuinely excludes the advertised
   degenerate mode;
5. state the strongest manuscript sentence it supports and the strongest
   sentence it does not support;
6. propose the highest-value composition theorem connecting the module to the
   null-information carrier, with an exact statement shape and kill condition.

Pay special attention to whether unrestricted matrix inputs make the Kraus
theorem algebraically stronger but physically less specific; whether two-factor
commutation is being mistaken for emergent locality; whether finite degeneracy
is necessary versus sufficient for spontaneous breaking; and whether the
generated scale theorem is circular because `Lambda` already appears in the
running law.
