# Codex audit job 12: complex no-go, spatial Route B, and thermodynamic rigidity

Act as an adversarial mathematical physicist, Lean semantic reviewer, and
simulation-methods auditor. Read every supplied live source snapshot, active
target, matrix, Audit 11, simulator file, and latest results artifact. Do not
edit source files.

Current live state:

- both the positive-real and all-complex scalar-square no-gos for the explicit
  simultaneous axis coin are landed and guarded; the complex theorem uses
  `finrank ker(U^2-rI) <= 3` plus a nonzero complex eigenkernel control;
- internal Route B is landed, but exact unitarity (`successiveStep`) and the
  `-iH` tangent (`linearSplit`) are distinct families agreeing only to first
  order;
- the action-derived recurrence, stability, finite Gibbs response, variance,
  and Schottky V2 are landed;
- active targets pursue a finite three-axis position-register walk, exact
  conjugacy of the recurrence to a rotation, and Gibbs variance rigidity.

Return `POST_COMPLEX_SPATIAL_AUDIT_12.md` with:

1. an independent proof-shape and semantic audit of
   `AxisCoinComplexCliffordNoGo`, including whether the nonzero-kernel control
   actually prevents a false invertibility reading and whether the manuscript
   may now mark the simultaneous architecture killed;
2. a declaration-level audit of `FiniteGibbsVariance` and S20, distinguishing a
   theorem-backed fluctuation response from a self-comparison or fitted V2;
3. an adversarial review of all three active targets, especially whether
   `SuccessiveAxisPositionWalk` is too general or uses a sign table unrelated to
   the landed `alpha1,alpha2,alpha3,beta`;
4. the exact smallest Lean theorem that connects the conditional shifts/sign
   table to the Clifford generators and makes the tangent of the full spatial
   step equal `-iH`;
5. the exact next quantitative compact-momentum `3+1` convergence theorem,
   including factor order, norm, rate, witness, and wrong-order/phase control;
6. the four over-claim checks and nondegeneracy gate on every new flagship;
7. exact corrections to all three control matrices and any stale statement in
   Audit 11;
8. a whole-theory verdict: which end-to-end arrows now genuinely compose and
   which remaining dictionaries prevent the manuscript from claiming a full
   theory rather than a candidate architecture;
9. the single highest-value proof and the single highest-value V2 to launch
   after the active targets return.

Treat focused-package import gaps as packaging facts. Do not infer a live build
failure when `PhysicsSM` is absent. Be especially alert to a hollow spatial
unitarity theorem whose arbitrary velocity table never recovers the project
Dirac symbol.

```yaml
aristotle:
  project_id: eb5b181f-f71c-4513-a9bf-429dbf8d62b5
  target_file: POST_COMPLEX_SPATIAL_AUDIT_12.md
  submission_project: AgentTasks/aristotle-submit/codex-post-complex-spatial-audit-20260710-12-project
  output_dir: AgentTasks/aristotle-output/eb5b181f-f71c-4513-a9bf-429dbf8d62b5
  status: idle; harvested and live-reconciled report preserved
```
