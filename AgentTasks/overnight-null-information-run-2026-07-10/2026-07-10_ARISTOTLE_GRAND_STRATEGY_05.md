# GRAND_STRATEGY_05 — closing one candidate theory before dawn

> **Controlling post-harvest update (06:16 PDT).** This advisory did not fully
> absorb its final live instruction before returning. The explicit
> sign-table-to-Clifford basis dictionary and spatial-symbol tangent have
> landed in `CliffordDiagonalPositionBridge`. The subsequent
> `PluckerDiracCarrierBridge` now feeds the same pair's `massSq` into the Gibbs
> gap, exact `3+1` Clifford dispersion, and split-step tangent. The guard passes
> **8,092 jobs**. Therefore all statements below calling that finite dictionary
> or literal mass-parameter seam open are stale; the compact-box rate,
> field-valued shared action, primitive selection, physical continuum, and V4
> prediction remain genuinely open.

Chief-architect synthesis for the 2026-07-10 finite null-information run, written
against the run constitution (`RUN_PLAN.md`), the two matrices
(`THEORY_COMPLETION_MATRIX.md`, `MANUSCRIPT_CLAIM_MATRIX.md`),
`SIMULATION_BENCHMARKS.md`, `LEDGER.md`, `2026-07-10_ARISTOTLE_POST_COMPLEX_SPATIAL_AUDIT_12.md`,
the prior strategy advisory (`ARISTOTLE_RESULT_claude_grand_strategy_2302.md`),
the three Pro essays, and the live theorem tree.

**Live state folded in (post-snapshot):** `PluckerJointTheoryWitness` has landed
**placeholder-free** — the one-pair joint mass/action/flow/ensemble witness that
the active fleet was targeting. The consolidated guard passes **8090 jobs**. **S23**
is a new **exact shared-pair V1 regression** for that joint witness. The
laboratory is now **18 families: 15 V0/V1 and three true V2 rows S18, S21, S22**.
The decisive consequence is stated up front and governs the whole document:

> **The Mass / Dynamics / Thermodynamics seam is now CLOSED as a single Lean term
> on one nondegenerate carrier.** It is no longer three compatible models sharing
> a suggestive scalar; it is one supplied spinor pair whose Plücker invariant is
> *simultaneously* its Hodge decoder cost, its finite-action Hessian, the conserved
> first integral of its action-derived recurrence, and the exact fluctuation gap
> of its canonical ensemble. What remains open is not this seam but the arrows
> *upstream* (primitive selection of the decorations/action) and *sideways*
> (the sign-table↔Clifford spatial-Dirac dictionary and the 3+1 continuum bound).

Grades used throughout follow the run conventions:
theory layers `D` derived / `H` conditional-theorem / `I` imported / `B` bridge
with finite avatar + kill / `O` open interface / `K` killed;
claims `M` kernel-checked / `T|H` conditional / `C` pre-registered conjecture /
`[import]` / `[interp]`; benchmarks `V0`–`V4`.

---

## 1. The displayed candidate-theory spine, primitive data → observables

One opinionated spine. Every arrow carries a grade and its load-bearing anchor.
"Same carrier" means the arrow is realized on the *same supplied pair* `(psi, phi)`
now unified by `PluckerJointTheoryWitness.one_pair_joint_chain`; "adjacent carrier"
means a checked theorem on a different but compatible witness.

```
                         [B] decoration/alphabet selection open
                          |
(0) PRIMITIVE NULL DATA   |  finite oriented null edges + Gaussian-rational
    (null rays, labels) --+  spinor decorations
        |
        |  [D on selected alphabet]  D4NullRaySpinorFactorization:
        |  each selected future axial D4 null ray -> explicit spinor,
        v  rank-one Hermitian image = half-Pauli vector
(1) GAUGE / EQUIVALENCE CLASS
        |  [D finite] GenericFiniteHodge (unique harmonic rep in ker Q cap ker Q*),
        |  DecoderChainHomotopy, KreinChainEquivalence
        |  [B] invariant sector *selection* (which positive sector) open
        v
(2) POSITIVE PHYSICAL STATE
        |  [D explicit quartet] PositiveHodgeDecoder, PositiveHodgePhysicalMass,
        |  PositiveSectorIntertwinerInvariance; [B] universal sector selection
        v
(3) MASS INVARIANT  ===== joint carrier vertex, ONE Lean term =====
        |  [D] massSq psi phi = quartet Hodge decoder cost                (ArbitrarySpinorHodgeBridge)
        |  [D] = second difference of finite action  (= Hessian)         (PluckerActionHessian)
        |  [D] = SL2-presentation-invariant positive Hessian             (PluckerHessianSL2Invariance)
        |         controls: 4/25, 9/25, collinear 0, boost-invariant, 2I -> 64/25
        v
(4) FINITE DYNAMICS (selected action)
        |  [D given selected action] DiscretePluckerVariationalFlow:
        |     step mu x = (x.2, (2-mu) x.2 - x.1),  mu = massSq
        |  [D] DiscretePluckerFlowStability: pos-def conserved firstIntegral,
        |     all-iterate bound on the full elliptic window 0<mu<4
        |  [D] DiscretePluckerFlowRotation: exact conjugacy to a unit-circle
        |     rotation (mu=4/25 -> c=23/25, s=4 sqrt 6/25, s^2=96/625)
        |  [O] primitive *selection* of this action from null data
        v
(5) CANONICAL FLUCTUATIONS  ===== same joint carrier vertex =====
        |  [D] FiniteGibbsResponse: normalized, positive, d log Z/dbeta = -<E>
        |  [D] FiniteGibbsVariance: d<E>/dbeta = -Var, Var >= 0,
        |     Var(4/25)@0 = 4/625; Var = 0  <=>  constant spectrum (rigidity)
        |  [O] coarse-graining, irreversibility, time arrow, thermodynamic limit
        v
(6) QUANTUM COMPOSITION (adjacent carrier)
        |  [D normalized params] CheckerboardPathSumTransferPower (exact value 85),
        |     UnitaryCheckerboardTransfer, UnitaryHistoryComposition,
        |     HistoryOperatorMonoidalDagger (Pauli noncommuting control)
        |  [D] NullFactorizationSpinFiber, SU2SpinHalfAction (U^2=-I, U^4=I)
        |  [B] derive normalized coefficients/phases from primitive data
        v
(7) KINEMATICS / CAUSAL SUPPORT (adjacent carrier)
        |  [D internal] SuccessiveAxisDiracWalk on C^4: exact unitary step,
        |     linearSplit tangent -iH, H(k,m)^2 = (|k|^2+m^2) I,
        |     real_symbol_matches_project;  H(1,2,2,3)^2 = 18 I
        |  [K killed over all Complex] AxisCoinComplexCliffordNoGo: the selected
        |     six-channel coin B(+)B(+)B admits NO injective 4-D invariant block
        |     whose square is any complex scalar (finrank ker <= 3 < 4);
        |     non-vacuity guard complex_square_kernel_nonzero at -7/25+24i/25
        |  [O] sign-table<->Clifford spatial dictionary (SuccessiveAxisPositionWalk
        |     proves isometry only; tetraVelocity disconnected from alpha_i)
        |  [H] 1+1 fixed-momentum continuum FixedMomentumManyStepContinuum (D t^2/n)
        |  [O] 3+1 compact-momentum continuum bound
        v
(8) CALIBRATED UNITS
        |  [I / ratio-only] dCausal m 0 1 = 1/m, Compton widthSq >= 1/(4 m^2);
        |     only dimensionless ratios are model-internal; absolute scale imported
        v
(9) KNOWN-PHYSICS BENCHMARK
        |  [V2 import] S18 free 1+1 Dirac propagator; S21 free 3+1 split-step;
        |     S22 three-level fluctuation-response; each with disclosed analytic target
        v
(10) FALSIFIABLE EXTRAPOLATION
        [O] no honest V4 yet (see Section 6); nearest structural falsifier is
        SubluminalBound (v_g<1 for m != 0, =1 iff massless) and CP-minimality N>=3
```

**Reading the spine honestly.** Vertices (3)–(5) are one term on one carrier
(the closed seam). Vertices (0)→(3) are a *checked chain on the selected alphabet*
but with a `[B]` decoration-selection cut at the very top. Vertices (6)–(7) are
checked on *adjacent* carriers and are joined to (3) only by the shared scalar
`massSq`, not yet by a shared term — that is the honest boundary between "candidate
architecture" and "single-object theory."

---

## 2. The exact compositions that make the spine one Lean term on one carrier

The prior advisory correctly diagnosed that "the gap is not a missing arrow — it
is a missing *joint witness*." That witness has now **landed**:

```lean
-- PluckerJointTheoryWitness.one_pair_joint_chain  (LANDED, placeholder-free)
-- On ONE supplied pair (psi phi : CSpinor), a SINGLE conjunction:
--   (a) quartet Hodge decoder value            = massSq psi phi     [Mass = Hodge]
--   (b) action(q+e2)+action(q-e2)-2 action q   = massSq psi phi     [Mass = Hessian]
--   (c) firstIntegral (massSq) (step (massSq) x) = firstIntegral (massSq) x  [flow conserves]
--   (d) HasDerivAt (log Z) (-<E>) beta                              [Gibbs response]
--   (e) HasDerivAt <E> (-Var) beta                                 [fluctuation]
-- plus rational_joint_chain_control on edge0, edge1(2/5): massSq = 4/25,
--   step(4/25)(0,1) = (1, 46/25), Var@0 = 4/625;
-- plus collinear_joint_zero_control: massSq = 0, flat action, Var = 0.
```

This is exactly Claude-Q1's C1 realized: primitive Plücker area = gauge-quotient
Hodge cost = positive-sector spectral mass = action curvature, *on the same term*,
with a nondegenerate control (`4/25`) and a degenerate falsifier (collinear `0`).
The remaining question the brief asks — the **two or three compositions** that
extend this single term further along the spine — has three candidates, ranked by
how much of the spine each one welds and by tonight-feasibility.

### Composition A (feasible tonight) — weld the ROTATION vertex onto the joint carrier
Add the elliptic-window conjugacy to the same `massSq`, upgrading conjunct (c)
from "first integral conserved" to "conjugate to a genuine unit-circle rotation."

```lean
theorem one_pair_joint_chain_rotation
    (psi phi : CSpinor) (h : 0 < massSq psi phi) (h4 : massSq psi phi < 4)
    (x : State) (beta : ℝ) :
    (one_pair_joint_chain psi phi ...).1 ...            -- reuse landed conjuncts
    ∧ ∃ c s : ℝ, 2 - massSq psi phi = 2*c ∧ c^2 + s^2 = 1 ∧ s ≠ 0
        ∧ (phaseCoordinates c s) ∘ (step (massSq psi phi))
            = (rotation c s) ∘ (phaseCoordinates c s)
```
- **Seam guarantee.** `massSq psi phi` is literally the `mu` fed to `step`, so the
  landed `DiscretePluckerFlowRotation` chain (`2-mu=2c`, `c^2+s^2=1`, `s != 0`,
  `phaseCoordinates_intertwines_step`) composes with no new hypothesis except the
  elliptic-window bound `0 < massSq < 4` (satisfied by `4/25`).
- **Kill.** A nondegenerate pair with `massSq >= 4` where the recurrence leaves the
  positive-definite window ⇒ rotation vertex does not attach for that carrier
  (already witnessed: `mu=5` loses positivity). This is a *scope* result, not a
  falsifier of the seam.
- **Payoff.** Vertices (3)+(4)+(5) become one term *with genuine eternal-boundedness
  motion*, not merely a conserved quadratic.

### Composition B (feasible tonight) — weld the PRIMITIVE-DIRECTION vertex on top
Push the top of the term one node upstream, so the supplied spinor is a *selected
D4 null ray* rather than an unexplained input.

```lean
theorem d4_ray_joint_chain
    (r : SelectedFutureAxialD4Ray) (chi q : Quartet) (x : State) (beta : ℝ) :
    let psi := d4RaySpinor r; let phi := d4RaySpinor (d4Successor r)
    one_pair_joint_chain psi phi chi q x beta            -- same conjunction, D4-sourced pair
```
- **Seam guarantee.** `D4NullRaySpinorFactorization` already produces the explicit
  spinor from the ray; feeding it as `(psi, phi)` is definitional.
- **Honest boundary.** This closes "primitive *direction* → mass/flow/ensemble" for
  the **selected alphabet only**; it does **not** derive the alphabet or the
  preferred time axis (`[B]` at spine vertex 0 remains). State it as such.
- **Kill.** If the D4-sourced pair is collinear for some ray pair (so `massSq = 0`),
  the vertex degenerates — a genuine control, not a bug (it is the massless ray).

### Composition C (NOT feasible tonight — the honest hard cut) — weld KINEMATICS
This is the one that would make the spine a *single* object from mass to a
spacetime Dirac step. It is blocked, and the block must be named, not papered over.

```lean
-- BLOCKED: requires RouteBSpatialTangent.CliffordDiag.hDiag (Section 4/Audit 12 §4),
-- which is OPEN.  Only then does the same scalar massSq appear as the mass coin of
-- a spatial walk whose tangent is -iH on the SAME carrier.
theorem joint_chain_with_spatial_tangent ... := -- cannot be written as one term yet
```
- **Why it fails today.** `SuccessiveAxisPositionWalk` proves norm preservation for
  an *arbitrary* velocity table; `tetraVelocity` is disconnected from
  `alpha1,alpha2,alpha3` (they are off-diagonal, a diagonal sign table cannot equal
  them, and axis-0's `TTFF` actually matches `beta`, misassigned). Until the per-axis
  basis change `U j` with `U j · diag(sgn) · U j^H = alpha_j` is proved, the mass
  scale of vertex (3) and the mass coin of vertex (7) are only *numerically* equal,
  not the same term.

**Directive.** Land **A and B** (two compositions) tonight and display the
mass/dynamics/thermo/rotation/primitive-direction spine as one Lean term. Present
**C** as the single named open weld — "one theorem (`hDiag`) from a full-spine term."

---

## 3. Final proof-wave ranking, 05:40–06:30 (small closers only; no broad new jobs after 06:30)

Every row: exact statement, imports, witness, control, kill condition, manuscript
upgrade. Ordered by leverage-per-minute given the seam is already closed. Broad
jobs (§4 spatial bridge, §5 3+1 bound) must have been launched *before* 05:40; the
06:30 rule forbids opening them in this window, so this wave is closers and welds.

| # | Statement | Imports | Witness | Control | Kill | Manuscript upgrade |
|---|---|---|---|---|---|---|
| **1** | `one_pair_joint_chain_rotation` (Comp. A) | `PluckerJointTheoryWitness`, `DiscretePluckerFlowRotation` | `edge0, edge1(2/5)`: `mu=4/25, c=23/25, s=4√6/25` | `mu=5` leaves window; `wrong_cosine_control` c=3/5 | rotation fails inside `0<mu<4`, or intertwiner breaks | M16/M19: "the joint carrier's flow is a genuine rotation, not just a conserved form" |
| **2** | `d4_ray_joint_chain` (Comp. B) | `PluckerJointTheoryWitness`, `D4NullRaySpinorFactorization` | selected axial ray + successor | collinear ray pair ⇒ `massSq=0` | D4 spinor image is not rank-one half-Pauli | M1/M3: primitive-direction end welded (selected alphabet) |
| **3** | `GibbsVarianceRigidity` closer (`Var=0 ⇔ constant spectrum`) | `FiniteGibbsVariance`, `probability_positive` | `4/25` positive side | `degenerate_spectrum_zero_control` | a nonconstant spectrum with `Var=0`, or a zero Boltzmann weight | M17: fluctuation → **rigidity dichotomy** (audit §3.3: land) |
| **4** | `S23` exact shared-pair regression reconfirm (V1) | joint witness | `4/25`, `46/25`, `4/625` on one config/seed | collinear `0` row | numeric ≠ exact rational fixture | M14: 18th family, seam is *executably* one carrier |
| **5** | `AxisCoinPositiveCliffordNoGo` re-caption as corollary of complex form | `AxisCoinComplexCliffordNoGo` | `anisotropic_four_sector_witness` | real 4-D sector exists | positive form not a strict corollary | M6: "positive scalar is the physically decisive slice of an all-ℂ kill" |

**Sequencing.** #1 and #2 first (they are the spine welds and the highest-leverage
manuscript deliverables); #3 next (converts an inequality into a dichotomy, the most
physically substantive thermo upgrade); #4 shadows #1–#2 as V1 regression; #5 is a
caption/corollary tidy. If any slot frees before 06:30, do **not** open §4/§5 —
instead run the load-bearing-hypothesis drop test (Section 9, gate G7) on the joint
witness. From 06:30 the fleet is audit-only.

---

## 4. Action → EOM → unitary spatial walk → continuum: does one action suffice?

### The two dynamical objects are currently distinct
1. **Variational recurrence** (`DiscretePluckerVariationalFlow` + Stability +
   Rotation). A *scalar*, real, `ℝ²` map: a single link amplitude `q` evolving in
   discrete time under the selected adjacent-link action
   `S = Σ_t [½(q_{t+1}-q_t)^2 − ½ mu q_t^2]`, EOM
   `q_{t+1} = (2-mu) q_t − q_{t-1}`, mass entering as **stiffness** `mu = massSq`.
   Conserved positive-definite `firstIntegral`; elliptic ⇒ rotation for `0<mu<4`.
2. **Dirac split-step** (`SuccessiveAxisDiracWalk`). A *spinor-valued*, complex,
   `ℂ⁴` unitary: internal generator `-iH`, `H^2=(|k|^2+m^2)I`, mass entering as a
   **coin angle** in `β`. First-order tangent identity + `H(1,2,2,3)^2=18I`.

They share the **same scalar** (`massSq` = stiffness *numerically equals* the coin
mass), but they are **not the same action**: one is a second-order scalar site
action (harmonic-oscillator / rotation), the other requires a first-order,
spinor-field, Dirac-type action. The audit's diagnosis is decisive: the exact
8-parameter `successiveStep` and the 1-parameter `linearSplit` tangent coincide
only to first order, and the position walk that would carry the spatial index is
currently a *generic* isometry disconnected from `alpha_i`.

### Honest verdict: they cannot yet share one action; a field-valued action is required
The scalar variational action reproduces mass-as-curvature and an elliptic
rotation, but a rotation in a 2-D phase plane is **not** the Dirac evolution
`exp(-iHt)` on `ℂ⁴`. To make "extremize one action ⇒ get both the M19 recurrence
and the Route-B `-iH` walk" a theorem, the run must introduce a **new
field-valued (spinor-lattice) action** and prove two reductions:

```lean
-- PROPOSED (new field-valued action program; broad job, launch before 05:40)
-- A lattice spinor action  S_field[psi] = Σ_x  psi(x)^H ( i * Dslash_eps - m ) psi(x)
-- on the finite null graph, with eps the lattice spacing.  Two obligations:

-- (R1)  Scalar reduction: on the trivial-spin, single-link sector, the second
--       variation of S_field reproduces the M19 recurrence with mu = massSq.
theorem field_action_scalar_reduction :
    scalarSector.secondVariation S_field = DiscretePluckerVariationalFlow.step (massSq ..)

-- (R2)  Spinor first-order limit: the Euler-Lagrange operator of S_field, in the
--       fixed-momentum eps->0 limit, is the Route-B generator -iH.
theorem field_action_dirac_limit :
    Tendsto (fun eps => eulerLagrange S_field eps) (nhds 0) (nhds (-I • H k m))
```
- **Witness.** `k=(1,2,2), m=3` (`nondegenerate_1223_control`, `H^2=18I`) for R2;
  `mu=4/25` for R1.
- **Controls.** (i) reversed mass phase ⇒ R2 limit is `-iH'` with `H' != H`;
  (ii) `[alpha_1,alpha_2] != 0` (recomputed `(α₁α₂)₀₀ = I != -I = (α₂α₁)₀₀`) forces
  the unsymmetrized product to be genuinely first-order.
- **Kill (the decisive one).** If the conserved `firstIntegral` of the scalar
  recurrence cannot be realized as a component of the Dirac walk's conserved
  `⟨ψ, ψ⟩` inner product — i.e. if R1 and R2 demand *incompatible* time
  parameterizations — then the two dynamics are **physically distinct** and the
  manuscript must present **two actions** (a scalar stiffness action and a spinor
  Dirac action), honestly labeled, rather than one unified variational principle.

### Continuum ladder (all downstream of the spatial dictionary)
```
selected action
  -> [D] EOM recurrence (M19)                      LANDED
  -> [D] full-window stability + rotation           LANDED (+ Comp. A tonight)
  -> [O] field-valued action R1/R2                   PROPOSED above
  -> [O] sign-table<->Clifford spatial walk (§ below) OPEN (highest value)
  -> [H] fixed-momentum 1+1 continuum (S18)          LANDED
  -> [O] 3+1 compact-momentum bound Dbox T^2/n        PROPOSED (S24)
  -> [O] Strang O(1/n^2) rung                          PROPOSED (S25)
  -> [O] position-space l^2 / PDE / local net          OPEN
```

**The single highest-value spatial theorem** (Audit 12 §4/§9), the arrow whose
absence keeps "3+1 Dirac emerges from a null walk" a claim-with-a-named-gap:

```lean
namespace RouteBSpatialTangent
def sgn (velocity : Fin 3 → Fin 4 → Bool) (j : Fin 3) (a : Fin 4) : ℂ :=
  if velocity j a then -1 else 1
def cliff : Fin 3 → Mat4 | 0 => alpha1 | 1 => alpha2 | 2 => alpha3
structure CliffordDiag (velocity) where
  U : Fin 3 → Mat4
  hUnit : ∀ j, (U j)ᴴ * U j = 1 ∧ U j * (U j)ᴴ = 1
  hDiag : ∀ j, U j * Matrix.diagonal (sgn velocity j) * (U j)ᴴ = cliff j   -- THE new content
-- axisSymbol_hasDerivAt : per-axis symbol has derivative -i * cliff j at k=0
-- stepSymbol_hasDerivAt : full spatial step tangent = -iH   (product rule; reuses
--   linear_split_entry_hasDerivAt entrywise)
end RouteBSpatialTangent
```
- **Kill.** No unitary `U j` diagonalizing the Bool table to `alpha_j` (the table's
  signature `(2,2)` is compatible, so failure would be a real surprise); or the
  assembled tangent `!= -iH`. Either kill downgrades the entire Kinematics row to
  "isometry only, Dirac dictionary refuted for this table."

---

## 5. Simulation program that can in principle match known physics

Current lab: **18 families — 15 V0/V1 and three V2 (S18 free 1+1 Dirac, S21 free
3+1 split-step, S22 three-level fluctuation-response)**; S23 added as the exact
shared-pair joint-witness V1 regression. No V2 may be called a prediction; S20 is
retired to V0 algebraic self-consistency (Schottky curve is a tautology, gap
cancels). The next five confrontations, in order:

| ID | Tier | Target physics | Anchor | Inputs & units | Observable | Error metric / pass | Parameter accounting | Falsifier (negative control) |
|---|---|---|---|---|---|---|---|---|
| **S24** | V2 | free **3+1** compact-momentum Dirac propagator, full symbol | `walkSymbol3p1` (§5 of Audit 12), `SuccessiveAxisDiracWalk` | `k=(1,2,2), m=3`, `T=1`, `c=ħ=1`, binary64; box `|k|≤K,|m|≤M` | Frobenius `‖walk^n − exp(-iHT)‖` | monotone to `n=1024`, final `<0.002`, uniform bound `Dbox·T²/n` holds | all supplied (imported analytic Dirac target); **no fit** | reversed mass phase ⇒ wrong `H'`; `[α₁,α₂]≠0` ⇒ first-order residual `n·err → c>0` |
| **S25** | V2 | Strang-symmetrized 3+1 step ⇒ `O(1/n²)` | palindromic `½m,x,y,z,½m` order | same as S24 | scaled error `n²·err` stabilizes | `n²·err → const`, final `<1e-4` | supplied; **no fit** | claim `O(1/n²)` for the *unsymmetrized* product ⇒ fails (it is `O(1/n)`) |
| **S26** | V2→V3 | 1+1 **position-space** Dirac wavepacket spreading | `SummableFourierContinuumLift` + walk-specific envelope (open) | Gaussian packet, `m,ε`, ℓ² grid | `‖ψ_walk(t) − ψ_Dirac(t)‖_{ℓ²}` | V2 if envelope supplied; **V3 if envelope calibrated** with held-out `t` | envelope constant: **fitted** if V3 (disclose training/held-out) | nonsummable envelope ⇒ no ℓ² convergence |
| **S27** | V2 | finite-temperature **specific heat** with a physical β unit | `FiniteGibbsVariance.meanEnergy_hasDerivAt` | genuine 3-level `(0,4/25,9/25)`, import `k_B`, β in physical units | `C(T)=β²Var`, peak location/height | central-difference vs `-d⟨E⟩/dβ` to `O(h²)`, Richardson `<1e-10` | `k_B` **imported**; spectrum supplied; **no fit** | degenerate spectrum ⇒ `C≡0`; reversed response sign fails |
| **S28** | V3 | fit **one dimensionless ratio** (e.g. a two-generation mass ratio or a mixing invariant) | `FiniteKMCP`/`KMPhaseCounting` (Jarlskog `6912/78125`), mass modules | one training datum, disclosed | held-out second observable | held-out within stated tolerance | **exactly one fitted parameter**, training datum named, held-out named | held-out misses ⇒ model has no predictive content beyond the fit |

**Program-level rules (enforced).** (i) Every V2 imports its analytic target and
counts **zero** consequences as predictions. (ii) S26/S28 must publish training vs
held-out splits or be demoted. (iii) Error metrics are Frobenius/ℓ² for propagators,
Richardson-extrapolated finite differences for thermodynamics, exact rational for
V0/V1. (iv) Each row keeps a *genuinely failing* control (phase reversal, commutator
order, degenerate spectrum, collinear pair, nonsummable envelope).

**Falsifiers of the whole simulation claim (M14).** Only fitted flexibility explains
any V2 agreement; a V2 misses its analytic target; a negative control passes; or the
"one carrier" S23 regression disagrees with the exact rational fixture.

---

## 6. One pre-registrable V4 candidate — or the honest proof that none exists yet

**Verdict: no honest V4 prediction exists tonight, and this must be stated
plainly.** The reason is structural, not rhetorical: every dynamical arrow in the
spine begins **one node downstream of primitive selection** — the spinor
decorations, the action, the coin coefficients/phases, and the preferred time axis
are all *supplied*, and only dimensionless *ratios* are model-internal (no absolute
scale is derived). A V4 requires a parameter-free (or trained-before-unblinding)
number with a held-out falsifier; a theory that inserts its coin angle cannot
predict the observable that the coin angle sets.

**Nearest-to-honest structural candidates (all currently postdictions or no-gos):**
- CP-minimality: `0 < physicalPhases N ↔ 3 ≤ N` (Jarlskog `6912/78125` at `N=3`,
  zero at `N=2`). Falsifiable in principle (a nonzero-phase 2-family sector kills
  it) but it is a *no-go / postdiction*, not a numeric forecast, and
  `FamilyRankNoGo.forcing_iff_rankfixing` warns any "forcer" may be the datum in
  disguise.
- `SubluminalBound`: `v_g<1` for `m≠0`, `=1` iff massless — but this is imported
  relativistic kinematics, not a novel output.

**Shortest theorem/data path to a genuine V4 (pre-registered program):**
1. **Derive one coin coefficient from primitive counting.** Prove that the
   normalized `(c,s)` (or the mass angle) is *forced* by a finite invariant of the
   selected D4 alphabet — not free. **Kill:** if any real `(c,s)` with `c²+s²=1` is
   admissible (coefficient is a free input), no prediction is possible from it.
2. **Derive one absolute scale, or prove only ratios are meaningful.** Either fix
   the lattice spacing `ε` from primitive data, or prove a scale-invariance theorem
   that forbids an absolute prediction (which itself is a publishable structural
   result). **Kill:** a hidden calibration smuggles in the scale.
3. **Pre-register one dimensionless ratio** computed from the fixed carrier with
   **zero** fitted parameters, then compare to an unread datum. Only step 3 output
   earns the label `V4`.

Until step 1 lands, the manuscript's prediction section must say: *"The framework
currently makes no parameter-free numerical prediction; its falsifiable content is
structural (CP-minimality, subluminality, spectral rigidity, the four-obstruction
count) and its shortest path to a genuine prediction is the coefficient-forcing
theorem above."*

---

## 7. Boldest defensible manuscript thesis, section order, and headline sentences

### Central thesis (bold, earned)
> **Mass, finite time-evolution, and canonical fluctuations are one object: the
> positive-sector spectral rank-defect of coherently composed null information.**
> On a single finite carrier, one nonnegative Plücker invariant of a decorated
> null-edge pair is simultaneously (i) the disagreement of its two null directions,
> (ii) the least-cost Hodge decoder value of its gauge class, (iii) the
> presentation-invariant positive Hessian of a finite action, (iv) the conserved
> stiffness of an exact, full-window-stable recurrence conjugate to a rotation, and
> (v) the fluctuation gap of its canonical ensemble — proved as a single Lean term.

This is *stronger* than the RUN_PLAN A1 working thesis because the seam is now one
term (`PluckerJointTheoryWitness`), not a composition of separately graded claims.

### Section order
1. **Thesis and the one-carrier theorem** — lead with `one_pair_joint_chain`.
2. **Primitive ontology** — null edges/decorations; `[B]` selection cut stated at
   the top, `D4NullRaySpinorFactorization` for the selected alphabet.
3. **Gauge class and positive state** — Hodge harmonic representative; sector
   selection open.
4. **Mass** — Plücker = Hodge = action Hessian; SL2 rigidity; controls `4/25, 9/25,
   0, 64/25`.
5. **Dynamics** — selected action → EOM → full-window stability → rotation.
6. **Thermodynamics** — Gibbs response, fluctuation positivity, rigidity dichotomy.
7. **Quantum composition and spin** — unitary histories, defining SU(2).
8. **Kinematics** — Route-B internal Dirac walk; the all-ℂ scalar-square coin kill;
   the *named open* spatial dictionary; 1+1 continuum.
9. **Standard-Model finite avatars** — gauge/Higgs/flavor, with explicit exceptions.
10. **Geometry, gravity, cosmology** — finite avatars and continuum boundaries.
11. **Simulation laboratory** — 18 families; V0–V2 honest, no prediction claimed.
12. **What would refute this** — structural falsifiers; the coefficient-forcing
    path to a genuine prediction.

### Headline sentences — **earned now**
- "One nonnegative invariant of a decorated null-edge pair is, on a single carrier
  and as a single kernel-checked term, its mass, the Hessian of its action, the
  conserved stiffness of its dynamics, and the gap of its fluctuations."
- "The selected six-channel D4 coin admits no four-dimensional invariant block
  whose square is any complex scalar; the naive 'this coin *is* the Dirac set'
  identification is refuted over all of ℂ, with an explicit nonzero eigenkernel."
- "Fluctuation vanishes if and only if the spectrum is constant — canonical
  fluctuation is exactly spectral nondegeneracy."

### Headline sentences — **earned only if the active jobs land**
- (needs §4 `hDiag`) "A finite null walk on the spatial register has tangent `-iH`;
  3+1 Dirac kinematics is the continuum limit of null-edge propagation."
- (needs §5 S24) "The 3+1 free-Dirac propagator is recovered from the split-step
  null walk with a uniform `O(1/n)` bound on a compact momentum box."
- (needs field-action R1/R2) "One field-valued action yields both the scalar
  stiffness recurrence and the spinor Dirac walk."

**Hedging discipline.** Replace `may/might/suggests` with the exact missing theorem,
input, or scale limit wherever an arrow is open (RUN_PLAN §0).

---

## 8. Derivation/import parameter ledger

Every quantity the theory touches, graded **derived (D) / imported (I) / selected
(S) / open (O)**, with the anchor or the exact missing step.

| Parameter | Role | Grade | Anchor / status | Exact next step to upgrade |
|---|---|---|---|---|
| Spinor decorations (Gaussian-rational) | primitive labels on null edges | **S** (selected); **D** on chosen alphabet via `D4NullRaySpinorFactorization` | supplied; D4 ray→spinor dictionary landed | derive the alphabet + preferred time axis from primitive counting |
| Preferred time axis / D4 alphabet | picks future cone, 3+1 split | **S** | supplied selected future axial rays | kill: prove no invariant selects it ⇒ axis is free input |
| Finite action (adjacent-link) | weights histories, sets EOM | **S** | `DiscretePluckerVariationalFlow` (selected) | field-valued action R1/R2 (§4); primitive selection |
| Action stiffness `mu` | mass in the recurrence | **D** (=`massSq`) | `PluckerActionHessian`, joint witness | already tied to Plücker invariant |
| Reference frame / SL2 boost | presentation of the pair | **D** invariance | `PluckerHessianSL2Invariance`, `PositiveSectorIntertwinerInvariance` | universal (not just rational) boost invariance |
| Lattice spacing `ε` | continuum step size | **O** | appears in S18 `D t²/n`, §5 `Dbox` | fix `ε` from primitive data, or prove only ratios meaningful |
| Coin phases / turn phase | unitarity of histories | **S** | `UnitaryCheckerboardTransfer` (`c²+s²=1`, imaginary turn, unit phases) | derive phases from primitive physical data |
| Coin coefficients `(c,s)=(3/5,4/5)` | mass/turn angle | **S** | supplied normalized | coefficient-forcing theorem (§6 step 1) |
| Gauge couplings `g_a` | gauge-mass Gram | **I/S** | `GaugeMassGram` (supplied reps/couplings) | derive local gauge action from redundancy |
| Temperature / `β` | canonical ensemble parameter | **S** dimensionless; **I** for physical unit | `FiniteGibbsResponse/Variance`; `k_B` imported in S27 | physical-temperature calibration; thermodynamic limit |
| Units / absolute scale | dimensionful bridge | **I** / ratio-only **D** | `dCausal=1/m`, Compton `≥1/(4m²)` | derive one absolute scale or prove scale-invariance |
| Standard-Model representations | group, charges, families | **I/B** | finite avatars (gauge/Higgs/flavor no-gos) | derive group from composition, not insert it |
| Higgs radial self-mass | scalar mass | **I** | `HiggsDofConservation/LongitudinalMode` (DOF only) | finite order-parameter dynamics + Hessian spectrum |
| CP phase count / Jarlskog | flavor structure | **D** (structural) | `KMPhaseCounting`, `FiniteKMCP` (`6912/78125`) | independent rank/family mechanism |
| Born probability rule | measurement outcomes | **I** (postulate P4) | `FiniteInstrumentAPI`; `no_free_probability` disclosure | explicitly not derived (honesty gate) |
| Empirical constants (masses, mixings) | calibration targets | **I** | benchmark inputs | any V3/V4 must disclose training vs held-out |

**Ledger rule (binding).** No consequence of an `I` row counts as a prediction. Every
`S` row is a *selection* the theory must eventually derive or defend as a symmetry
choice; every `O` row names its exact missing theorem.

---

## 9. Hard 07:00 audit checklist — pass/fail gates

The manuscript presents a **coherent candidate full theory** iff **all G-gates
pass**. Any FAIL forces the labeled downgrade, not silence.

| Gate | Check | PASS criterion | FAIL action |
|---|---|---|---|
| **G1 Seam is one term** | `PluckerJointTheoryWitness.one_pair_joint_chain` is `s o r r y`-free, its assumption print is contained in `{propext, Classical.choice, Quot.sound}` | placeholder-free, clean assumptions | drop the "single Lean term" thesis; revert to compatible-models language |
| **G2 Nondegeneracy** | each conjunct has a positive witness *and* a genuinely failing control | `4/25`/`46/25`/`4/625` positive **and** collinear `0` control fire | flag the vacuous conjunct; do not display it as content |
| **G3 Guard footprint** | consolidated guard passes; all `Sources/*.lean` `s o r r y`-free | **8090 jobs** pass; no `s o r r y`, no `n a t i v e _ d e c i d e` in trusted promotion, no new assumptions/`@[implemented_by]` | quarantine any regressing module |
| **G4 Kill route intact** | `AxisCoinComplexCliffordNoGo` + non-vacuity `complex_square_kernel_nonzero` | `finrank ker ≤ 3`, nonzero kernel at `-7/25+24i/25`, real 4-D sector exists | do not caption "simultaneous Dirac coin killed" |
| **G5 Spatial gap named** | `SuccessiveAxisPositionWalk` captioned "generic isometry; sign-table↔Clifford **open**" | caption matches Audit 12 §3.1; `hDiag` acknowledged open | remove any "spatial Dirac walk" caption (hollow) |
| **G6 No tautology V2** | S20 tiered V0 self-consistency; S18/S21/S22 confront analytic targets not themselves | S20 not counted as V2; three genuine V2 rows | strip inflated V2 status |
| **G7 Load-bearing hypotheses** | drop each explicit hypothesis of the joint witness + each composition; re-elaborate | every dropped hypothesis breaks elaboration | a droppable hypothesis ⇒ theorem weaker than prose; re-caption |
| **G8 Name-vs-type** | every physics identification (mass, gauge, gravity, Λ, generation) appears in the Lean *type*, not only docstring | type carries the object | downgrade to `[C]`, annotate "label, not derived" |
| **G9 Import tagging** | every heat-kernel / known-Hamiltonian / continuum / `k_B` / Born step tagged `[import]`; no prediction language adjacent | all tagged; no `[import]`-adjacent prediction | retag; delete prediction claim |
| **G10 Parameter ledger honesty** | Section 8 ledger matches the manuscript; no `S`/`I` consequence sold as prediction | ledger and prose agree | correct the prose |
| **G11 Prediction honesty** | Section 6 verdict ("no honest V4 yet") is stated, not hedged | explicit statement present | insert it; no `may/might/suggests` substitute |
| **G12 Composition-test executability** | at least one primitive→observable chain is a single term with shared variables (mass/dynamics/thermo, + Comp. A/B if landed) | joint witness (+welds) displayed as one term | mark the chain "has an unnamed seam"; do not display as end-to-end |

**Dawn decision rule.** If **G1–G4, G6, G7, G9, G12 pass** (seam closed, kills and
V2s honest, hypotheses load-bearing, chain executable), the manuscript **is** a
coherent candidate full theory *with two named open welds* (spatial Dirac
dictionary §4; 3+1 continuum §5) — and it must say exactly that. If **G1, G3, or
G12 fail**, it is a checked anthology, not a theory, and the thesis must retreat to
the compatible-models claim. G5, G8, G10, G11 failures are honesty failures that
must be fixed in prose before publication regardless of the theory verdict.

---

## Appendix — one-line status of every arrow the brief named

- arbitrary-pair Plücker mass = Hodge cost = invariant action Hessian — **D**, joint carrier.
- same stiffness drives full-window-stable recurrence, conjugate to a rotation — **D** (Comp. A welds it to the carrier).
- finite canonical response, fluctuation positivity, rigidity — **D** (rigidity dichotomy = tonight's closer #3).
- six-channel simultaneous Clifford interpretation killed over all complex scalar squares — **K**, non-vacuity guarded.
- four-component successive-axis internal + finite-position unitary walks — internal **D**; position walk **isometry only** (spatial Dirac dictionary **O**).
- V2 fixed-momentum 1+1 (S18) and 3+1 (S21) reproductions — **H/[import]**, disclosed.
- missing sign/eigenbasis→Clifford spatial dictionary — **O**, exact statement in §4 (`CliffordDiag.hDiag`).
- compact-momentum 3+1 `O(1/n)` theorem — **O**, exact statement S24/§5.
- one-pair joint mass/action/flow/ensemble witness — **LANDED placeholder-free** (`PluckerJointTheoryWitness`), regression **S23** (V1).
- laboratory — **18 families: 15 V0/V1 + V2 S18/S21/S22**; S20 retired to V0 self-consistency.
