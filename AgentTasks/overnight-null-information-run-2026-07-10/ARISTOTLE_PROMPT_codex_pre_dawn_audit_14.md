# Codex standing Audit 14: pre-dawn candidate-theory gate

Act as an adversarial mathematical-physics auditor preparing the hard 07:00
audit. Read the run constitution, both matrices, benchmark manifest, ledger,
Audit 13, Grand Strategy 05, main manuscript, simulator, latest result, and the
live Lean modules named below. Return `PRE_DAWN_AUDIT_14.md`.

Controlling live state:

- `CliffordDiagonalPositionBridge` is landed and guarded;
- `PluckerJointTheoryWitness` is landed and guarded;
- `PluckerDiracCarrierBridge` is landed and guarded at 8,092 jobs;
- S24 makes the lab 19 families: 16 V0/V1 and V2 S18/S21/S22;
- compact `3+1` rate `ce6e18d6` and finite unitary path action `f22d0921` are
  active and must remain open until their proofs land.

Required audit:

1. independently inspect the exact types and proofs of all three bridge
   modules, not their docstrings;
2. decide precisely what `PluckerDiracCarrierBridge` connects and whether it is
   merely substitution, a real anti-fragmentation seam, or both; identify every
   generic conjunct and every physical identification still absent;
3. recompute `massSq=4/25`, `H(1,2,2,4/25)^2=(5641/625)I`, nonzero `H`, and the
   collinear zero-momentum collapse; reject any arithmetic or convention drift;
4. audit S24 as V1 and the full count as 19 = 16 V0/V1 + 3 V2; ensure no V1/V2
   row is called a prediction;
5. audit the finite unitary path-action target statements for truth,
   non-vacuity, and whether zero residual action legitimately deserves
   "action/EOM" language or must be captioned as a least-residual
   characterization;
6. audit the active compact-rate theorem against Audit 13's first-order product
   estimate and reject any `O(1/n^2)` promotion of the unsymmetrized step;
7. perform an independent anchor sweep of M1-M21 and list every missing,
   mismatched, or prose-only anchor in severity order;
8. run the four over-claim checks and nondegeneracy gate on every overnight
   flagship;
9. return exact manuscript corrections plus a binary dawn verdict for each:
   coherent finite candidate architecture, one shared-action theory, physical
   continuum theory, and predictive theory;
10. name the single most important theorem after the active rate/action jobs,
    with exact statement shape, witness, control, and kill condition.

Do not edit source. Treat staging/package gaps separately from live failures.
Boldness is welcome only where the Lean type and evidence tier earn it.

```yaml
aristotle:
  project_id: 80b2a459-078b-48f6-8682-71a2828ea658
  target_file: PRE_DAWN_AUDIT_14.md
  submission_project: AgentTasks/aristotle-submit/codex-pre-dawn-audit-20260710-14-project
  output_dir: AgentTasks/aristotle-output/80b2a459-078b-48f6-8682-71a2828ea658
  status: harvested at 07:01 PDT; preserved with live reconciliation
```

2026-07-10 06:39 PDT live instruction: `FiniteUnitaryPathAction` landed and the
guard advanced to 8,093 jobs; caption it only as a generic least-residual
action shell. Claude's S25 finite-instrument V1 also landed, advancing the lab
to 20 families: 17 V0/V1 and V2 S18/S21/S22. Aristotle accepted the instruction.
The live claim matrix now runs through M22; include M22 in the anchor sweep.

2026-07-10 07:05 PDT operator reconciliation: the returned package snapshot
lagged the live tree. `FiniteUnitaryPathAction` and `FiniteInstrumentAPI` are
both fully proved and imported by the consolidated guard, which passes at 8,094
jobs. The laboratory remains 20 families with three V2 rows. Audit 14's
semantic verdicts remain controlling; its stale staging/build findings do not.
