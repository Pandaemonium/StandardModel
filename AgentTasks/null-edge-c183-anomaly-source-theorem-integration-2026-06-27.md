# Summary of changes for run 28e40611-a7b5-4dba-9046-afcb8009874a
Designed and machine-verified the `AnomalyIndexSource` interface for Gate C1 reference import in `RequestProject/AnomalyIndexSource.lean` (builds clean, no proof placeholders, only standard kernel dependencies `propext`/`Classical.choice`/`Quot.sound`). All six requested outputs are delivered as Lean declarations with explanatory docstrings.

1. Import API + theorem. `GateC1.AnomalyIndexImport` is the precise interface for importing anomaly/index data from a reference (overlap / domain-wall / flavored-overlap) sector to the null-edge target along a `GaugeSafeGappedHomotopy`. The two genuine inputs are explicit hypothesis fields — `refIndexThm` (the reference index theorem value) and the homotopy's `preservesIndex`/`preservesParity` invariance — never hidden assumptions and never hidden. The conclusion `AnomalyIndexImport.target_indexWeight` (and `target_wittenParity`, `target_anomalyConsistent`) transports the index weight to the null-edge sector. A docstring field `note` records the explicit disclaimer that the result comes from the reference index theorem plus homotopy transport, NOT from the overlap/Ginsparg–Wilson algebra (per C171R/C178).

2. Topological/index vs representation-theoretic split. `TopologicalIndexData` (integer index weight + global SU(2) parity) is the homotopy/gap-invariant data that gets transported; the representation-theoretic Standard Model content is kept type-separate (`RepresentationAnomalyData`/`SMAnomalyInputs`) and is explicitly NOT transported — it must be checked independently, matching C178's "independent certificate" point.

3. Exact SM inputs. `WeylMultiplet` and `SMAnomalyInputs` capture: representation content (`colorMult`, `weakMult`, `colorTriplet`, `weakDoublet`), hypercharge normalization (`hyper : ℚ` in the Q = T₃ + Y convention), chirality assignment (all fields written as left-handed Weyl multiplets, i.e. u^c/d^c/e^c conjugates), and gauge internality (`gaugeInternal : Prop`, carried so it cannot be silently dropped). The concrete `smGeneration` lists the five multiplets with standard hypercharges. `sm_generation_anomalyFree` machine-checks that one generation cancels all four perturbative anomalies (grav²–U(1), U(1)³, SU(2)²–U(1), SU(3)²–U(1)) and the global SU(2)/Witten anomaly (even doublet count).

4. Seven-slot signature encodes the index weight. `SectorSignature` has exactly seven fields; the net chiral index/anomaly weight sits in slot 7 (`indexWeight`), the global SU(2) (Witten) anomaly in slot 3 (`wittenParity`), and the integer-normalized net hypercharge 6·Y in slot 4 (`hyper6`), so all anomaly/index data is recoverable from the signature. `SectorSignature.anomalyConsistent` ties slots 3 and 7 together as the admissibility predicate.

5. Minimal certificate, no hidden assumptions. `AnomalyIndexCertificate` bundles the SM inputs, the checked `AnomalyFree` proof, the import, and the reference index-data matching, with `valid` concluding the null-edge sector is anomaly-consistent. `smCertificate` is a concrete inhabited instance proving the structure is realizable with no proof placeholders and no extra axioms.

6. Literature sources. `referenceSources` and the §6 docstring list the theorem sources to check later for the direct reference index theorem (Atiyah–Singer; Callias; Kaplan/Shamir/Furman–Shamir domain-wall fermions; Narayanan–Neuberger/Neuberger overlap; Ginsparg–Wilson/Hasenfratz–Laliena–Niedermayer/Lüscher lattice index; Lüscher lattice gauge-anomaly cancellation; Witten 1982 global SU(2) anomaly; Bilal / Weinberg QFT II for the SM conditions). None is assumed in any proof.

Throughout, no anomaly cancellation is claimed from the overlap/GW algebra alone: that algebra is deliberately absent from the import's input fields.
