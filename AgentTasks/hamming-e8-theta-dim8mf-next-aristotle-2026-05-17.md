# Hamming E8 theta dimension-8 modular-form follow-up - 2026-05-17

Status: submitted to Aristotle.

Purpose: close the two remaining proof holes in
`PhysicsSM/Draft/E8ThetaDim8MF.lean`, after Codex reduced the file to:

```text
e8_dualLattice_eq_self
thetaSeries8_transform_S
```

The target module builds locally with exactly those two draft `sorry`s:

```text
lake build PhysicsSM.Draft.E8ThetaDim8MF
```

## Target file

```text
PhysicsSM/Draft/E8ThetaDim8MF.lean
```

Namespace:

```lean
namespace PhysicsSM.Coding.E8ThetaDim8
```

## Job A: E8 self-duality

Target theorem:

```lean
theorem e8_dualLattice_eq_self :
    SchwartzMap.dualLattice (d := 8) E8Lattice = E8Lattice := by
  sorry
```

Current supporting facts in the same file:

```lean
theorem e8_integral :
    forall u v : R8, u in E8Lattice -> v in E8Lattice ->
      exists m : Int, inner u v = m

theorem e8_covolume_eq_one :
    ZLattice.covolume E8Lattice volume = 1
```

Recommended routes:

1. Adapt SPL's dimension-24 proof
   `SpherePacking.Dim24.Uniqueness.Rigidity.Classify.Niemeier.DualSelf` to
   rank 8.
2. Or use the public `E8Matrix` row basis and `E8Matrix_unimodular` to compute
   the dual basis explicitly and show the dual lattice equals the same row
   span.

Constraints:

- Do not change the theorem statement.
- No new axioms, `opaque`, or `unsafe`.
- Do not spend time on the other remaining sorry.
- Small helper lemmas in this draft file are fine.

## Job B: S-transform by Poisson summation

Target theorem:

```lean
theorem thetaSeries8_transform_S
    {tau : Complex} (htau : 0 < tau.im) :
    thetaSeries8 (-1 / tau) =
      (-(tau * Complex.I)) ^ (4 : Complex) * thetaSeries8 tau := by
  sorry
```

Current supporting facts in the same file:

```lean
theorem summable_thetaTerm8
theorem e8_covolume_eq_one
theorem e8_dualLattice_eq_self
theorem thetaSeries8_add_one
theorem thetaSeries8_MDifferentiable
theorem thetaSeriesUHP8_isBoundedAtImInfty
```

Recommended route:

Adapt SPL's rank-24 theorem
`SpherePacking.Dim24.Uniqueness.Rigidity.Classify.EvenUnimodular.ThetaTransform.thetaSeries_transform_S`
to rank 8.  Use:

```lean
SchwartzMap.poissonSummation_lattice (d := 8)
fourier_gaussian_innerProductSpace
e8_covolume_eq_one
e8_dualLattice_eq_self
```

The dimension factor should be weight `4`, since `Module.finrank Real R8 / 2 = 4`.

Constraints:

- Do not change the theorem statement.
- No new axioms, `opaque`, or `unsafe`.
- It is acceptable for this job to use `e8_dualLattice_eq_self` as an existing
  theorem; Job A separately targets that proof.
- Small helper lemmas in this draft file are fine.

## Aristotle infrastructure note

The previous Aristotle return said its theorem-proving subagent could not work
on files importing SPL modules because `.olean` files were missing in the
subagent environment.  For this retry, the submission package is generated by:

```text
Scripts/prepare_aristotle_submission.ps1
```

The script patches `SpherePacking` to the remote Windows-safe fork instead of
copying stale local `.lake` state.  If the subagent still cannot import SPL,
try first building the module inside Aristotle's main project environment:

```text
lake build PhysicsSM.Draft.E8ThetaDim8MF
```

Then work in the fully built project.  Do not replace these SPL-dependent goals
with unrelated theorem statements.

## Submission package

```text
AgentTasks/aristotle-submit/hamming-e8-theta-dim8mf-next-20260517-project
```

Generated with:

```text
powershell -ExecutionPolicy Bypass -File Scripts/prepare_aristotle_submission.ps1 -JobName hamming-e8-theta-dim8mf-next-20260517 -TaskNote AgentTasks/hamming-e8-theta-dim8mf-next-aristotle-2026-05-17.md
```

Package checks:

```text
No .lake directory present after final regeneration.
SpherePacking patched to https://github.com/Pandaemonium/Sphere-Packing-Lean, rev windows-safe-auxiliary-renames.
Copied target has exactly the two intended proof holes.
```

Local smoke-test note:

```text
A fresh build inside the copied package began cloning/building dependencies, but
building from scratch was too slow locally and timed out.  The package was
regenerated afterward so no temporary .lake state is included in the Aristotle
archive.  The live checkout target had already passed:
lake build PhysicsSM.Draft.E8ThetaDim8MF
```

## Submitted jobs

| Job | Aristotle ID | Status at submission |
|-----|--------------|----------------------|
| E8 self-duality | `e0bdc02b-654d-4e39-9091-51253941e0ad` | queued |
| Theta S-transform | `3c9ed71d-c668-4cc2-9321-36c93b273d91` | queued |

Follow-up commands:

```text
aristotle result e0bdc02b-654d-4e39-9091-51253941e0ad --destination AgentTasks/aristotle-output/hamming-e8-theta-dim8mf-self-dual-20260517
aristotle result 3c9ed71d-c668-4cc2-9321-36c93b273d91 --destination AgentTasks/aristotle-output/hamming-e8-theta-dim8mf-s-transform-20260517
```
