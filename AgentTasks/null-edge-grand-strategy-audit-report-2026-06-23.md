# Null-edge grand strategy and physics-alignment audit

Date: 2026-06-23

Source: Aristotle project `4153d0e4-480f-4002-9ebb-64461384197a`, requested as a
no-build strategy/audit job over the strengthened program, publication plan,
interaction ontology, overnight plan, and integration notes.

## Executive read

Aristotle understood the assignment. The most useful high-level audit point is
that the program must preserve the invariant/frame-relative split:

```text
det(P)       = m^2      -- invariant mass-square
det(rho_u)  = (m/E_u)^2 -- observer-normalized mass ratio
```

Most physics-alignment risk comes from letting those two statements blur. The
program should therefore treat observer-conditioned normalization as a theorem
surface and a permanent documentation boundary, not merely prose.

The second most useful point is methodological: new quadratic identities should
be promoted only when they name a finite state space, grading, adjoint, first
order operator, covariance law, and square-level observable. This confirms the
Dirac square-root criterion already adopted in the strengthened program.

## Highest-value theorem targets

1. P1 frame audit: `det(A P A^dagger)=det(P)` for `A in SL(2,C)`, plus the
   observer-normalized determinant ratio.
2. Celestial moment wrapper: mass as the monopole/dipole deficit of the
   celestial direction distribution.
3. Dirac-slash convention audit: shared signature, Pauli ordering, gamma
   representation, and the existing bundle slash square theorem.
4. P3 crossed-module interchange: prove the double-category law for path-pair
   defects, or record a clean obstruction.
5. Super-Dirac square on an actual finite order complex, beyond abstract block
   algebra.
6. Mass-ratio/concurrence identity with explicit `m/E_u` and LOCC-only
   monotonicity boundary.
7. P9 source visibility API with a non-tautology contract: invisibility must be
   derived, while visible Pluecker excitations must source a bulk pairing under
   stated hypotheses.
8. P9 quantitative residual threshold versus everpresent-Lambda-style
   `1/sqrt(V)` scaling.
9. P7 recoverability spine: data processing and Petz/Fawzi-Renner style gaps as
   diagnostics, not automatic invisibility.
10. H3(O)/triality generation structure only as an internal representation lead,
    not a mass-spectrum theorem.

## Permanent documentation guardrails

- Normalized momentum definitions should state that `rho_u` is observer- or
  frame-conditioned. `det(P)` is invariant; `det(rho_u)` is a mass ratio.
- Pluecker mass theorem comments should keep the future-pointing positive
  Hermitian hypothesis visible.
- Dirac modules should share one convention block for signature, Pauli matrices,
  gamma matrices, and chirality sign.
- Concurrence modules should say `m/E_u`, not bare `m`, and restrict
  monotonicity to local or LOCC channels on the visible/internal cut.
- P9 source-visibility definitions should separate recoverability,
  indistinguishability, and source invisibility.
- Bivector interfaces should distinguish `Lambda^2 S` scalar Pluecker brackets,
  `Sym^2 S` self-dual curvature spinors, `S tensor Sbar` momentum vectors, and
  genuine four-dimensional bivectors.

## Immediate action taken

- Integrated the P1/P6 observer-conditioned warning into the publication plan and
  ontology before this report was written.
- Added the P7/P9 recoverability-not-invisibility warning to the overnight lane
  plan.
- Submitted follow-up Aristotle jobs for observer-conditioned normalization,
  chirality coherence, and antisymmetric hidden-relabelling mean-zero source
  mechanisms.
