# Advisor response to HELP_NEEDED_2026-07-11 (received ~06:45 PDT 2026-07-11)

Provenance: external expert material relayed by the user mid-run, responding
to the seven asks (H1-H7) in `HELP_NEEDED_2026-07-11.md`. The user
simultaneously WAIVED the 06:30/07:00 audit cutoff: "let's push hard on any
new angles to resolve the blockers instead of shifting to audit."

The advisor's own caveat, binding on us: "Citation details are from memory
and should be verified against the actual papers before anything
load-bearing rests on them."

Advisor's priority order: H5 frame-index check first (decidable on existing
fixtures), H2 kink-charge fixture second, H1 quotient third.

Verbatim response follows.

---

Yes — several of these connect to structures I can identify precisely. I'll go deep on H5, H2, and H1 (in your §6 format, loosely), then give compact but concrete contributions to H3, H4, H7, and H6. Citation details are from memory and should be verified against the actual papers before anything load-bearing rests on them.

## H5 — The invariant you're missing is almost certainly the *time-frame pair*, not any single winding

**Diagnosis.** Your four-site counterexample — same total turning `2π`, different compression signatures, with the corrected criterion depending on wall position relative to reflection-fixed sites — is the signature phenomenon of chiral *discrete-time* walks, and it has a known complete resolution. For a unitary walk `W` with chiral symmetry `Γ W Γ = W⁻¹`, a single winding number is **not** a complete invariant. The walk admits two inequivalent "symmetric time frames" (splittings `W = F₂F₁` vs `W' = F₁F₂` at the two chiral-symmetric half-steps), each with its own winding `ν₁, ν₂`; the complete bulk data is the pair, conventionally repackaged as `(ν₀, ν_π) = ((ν₁+ν₂)/2, (ν₁−ν₂)/2)`, which separately count protected modes at walk-eigenvalue `+1` (quasienergy 0) and `−1` (quasienergy π). Your "exact displayed `+1/−1` mode witnesses" are exactly quasienergy-0 and quasienergy-π modes, and two fields with equal total winding can differ in the *split* between frames. The wall-position dependence is the mechanism, not a pathology: a wall sitting on a frame's inversion-fixed site engages that frame's index; shifted by half a cell it engages the other frame. Winding-only protection *had* to fail — you've rediscovered, in your live compression, why Asbóth introduced the second frame.

**Proposed theorem.** Let `W` be the live finite walk with its edge-orientation-reversal grading `Γ` (first verify `ΓWΓ = W⁻¹` holds exactly for the live compression — this is the one hypothesis everything hangs on). Define frame indices from the walk's own layer ordering: the palindromic/ordered structure gives the two canonical symmetric splittings, hence `(ν₀, ν_π)` per asymptotic bulk, and for a wall the relative index `Δν₀, Δν_π` between the two sides, computed in the frame determined by the wall's site parity. Claim: *signed pinned-mode count at `+1` equals `Δν₀`; at `−1` equals `Δν_π`*, robust under finite-range `Γ`-respecting unitary perturbations that keep both gaps open away from the wall. The real-space (non-translation-invariant, finite-compression-friendly) version of this classification is Cedzich–Geib–Stahl–Velázquez–Werner–Werner, *Ann. Henri Poincaré* **19** (2018), arXiv:1611.04439, and its companion on complete homotopy invariants (Quantum **2**, 2018): their left/right symmetry indices `si_L, si_R` are Fredholm-type, defined for non-translation-invariant walks, and their gentle-perturbation stability theorem is exactly your requested perturbation class. Asbóth PRB **86**, 195414 (2012) and Asbóth–Obuse PRB **88**, 121406(R) (2013) — already in your reference list — are the two-frame origin.

**Decidable pre-registered check (do this first, it's an afternoon).** Recompute, for the two same-winding four-site fields, the winding in the *second* time frame. Prediction: they differ, and the difference matches the signature discrepancy, with the wall-position rule reproducing the fixed-site dependence. **Kill condition:** if the frame-2 windings are equal for the counterexample pair, this proposal is dead for your family, and the next candidate is the full CGGSVWZ symmetry-index for whatever symmetry type the live walk actually has (their classification covers all tenfold classes for walks, including the cases with extra reflection).

**Why this satisfies "derived from the carrier."** The two frames are not fixture-fitted: they come from the walk's own palindromic layer structure (the sanctioned transfer ordering), and `Γ` is the carrier's GW grading. Nothing is introduced to classify the fixtures.

**Lean ladder.** (1) `ΓWΓ = W⁻¹` for the live walk. (2) The two frame splittings and their windings as exact rational/algebraic data on your fixtures. (3) The finite bulk-boundary statement via transfer matrices: a mode at walk-eigenvalue `λ ∈ {±1}` exists iff the `λ`-transfer-matrix stable subspaces from the two sides intersect; chirality forces the intersection count to equal the frame-index difference. Off criticality the transfer matrices are hyperbolic with rational spectral data at your Pythagorean fixtures, which also gives the localization estimate you asked for (decay rate = log of the transfer-matrix spectral radius).

**What this would not prove:** anything about 3+1D, or protection under perturbations breaking `Γ`.

## H2 — A free-theory phase observable exists: the filled-sea window charge (finite Goldstone–Wilczek)

**The gap in the current frontier.** Your pair-kick result proves operational phase retention *given* a supplied interaction. But there is a phase-sensitive observable of the **free** carrier — it just lives at the many-mode (filled-sea) level rather than the one-particle level, and there's a structural reason the one-particle spectrum is blind: a constant chiral angle is a symmetry redefinition (`V = diag(e^{−iθ/2}, e^{iθ/2})` removes it while commuting with `kσ_z`), while a *varying* angle is an axial gauge field — removing `θ_n` site-by-site deposits link phases `e^{±i(θ_{n+1}−θ_n)/2}` with opposite signs on the two chiralities. The physics of a phase gradient is anomaly physics, and anomalies are sea-level effects. This is why your finding "spectrum blind, interference sensitive" is not an accident.

**Proposed observable (route 4 + route 3).** For the chain carrier with mass field `z_n = μ e^{iθ_n}`, half-filled Fermi sea `P_<` (a Slater determinant — your determinant-minor second quantization already covers this), and window `W`, define `Q_W = tr(P_< Π_W)`. The Goldstone–Wilczek statement: the induced sea charge density is `∂θ/2π`, so for two **equal-modulus** fields — `θ ≡ const` (control) versus a kink winding by `π` across `W` — the difference `Q_W(kink) − Q_W(const)` is `1/2` up to `O(e^{−μd})` corrections (`d` = window-edge-to-kink distance). The held-out coefficient is `Δθ/2π`: universal, rational, `|z|`-independent (the modulus enters only the error rate), not absorbable into `|z|` or any basis choice.

- **Stated before fitting:** observable `Q_W`, fields declared, prediction `1/2`.
- **Gauge/basis invariance:** `Q_W` is invariant under global phase, site-local charge-diagonal `U(1)`, and the constant axial rotation `V` (diagonal in position ⊗ internal, commutes with `Π_W` and total number). So the control invariance — constant phase invisible — is itself a one-line theorem, which is the right consistency check.
- **Kill alignment:** this directly attacks your kill condition — it is a gauge-invariant observable of the free theory that depends on `θ` beyond `|z|`. If it *fails* (the exact fixture gives `0` separation), the "assembly bookkeeping" verdict gains a sea-level leg, which is much stronger than the current one-particle statement.

**Exact finite version.** Sharp real-sign kink (`z: +μ → −μ`) at a reflection-symmetric site is the Jackiw–Rebbi/SSH configuration where the `1/2` is pinned exactly by the symmetry (fermion-number fractionalization; the classic polyacetylene half-charge). A small-`N` Pythagorean fixture with exact rational spectrum, exact eigenvectors, and `Q_W` an explicit rational is very much your house style; the general-profile statement is a `T|H` bridge via a Combes–Thomas resolvent-decay bound (banded-matrix Combes–Thomas is quite Lean-able).

**Route 1 (deriving the quartic) — sketch worth pre-registering.** Promote `θ_n` to a dynamical decoration with a minimal stiffness (disclose this as the one added input; it's arguably already your D5 ensemble layer). The turn term couples `e^{iθ_n}` to the chirality-raising bilinear `S⁺_n`. Second-order elimination of Gaussian `θ`-fluctuations generates `J_{nm} S⁺_n S⁻_m` plus, around a condensed phase, `e^{2iθ̄}`-weighted `S⁺S⁺` terms — an even quartic CAR operation with exactly the phase-sensitive pair-kick shape. Gate: match the generated four-mode structure against `PlueckerQuarticInteraction`; the coupling is `μ²/stiffness`, held out. If it matches, the pair kick is *derived from the same carrier plus one disclosed decoration-kinetic input*, which is route 1.

**A cross-link that buys you an H3 diagnostic for free.** The dynamical counterpart — charge pumped through a bond under one adiabatic `2π` rotation of uniform `θ` — equals the net spectral flow, which equals a winding of your all-zone determinant. With even aliases present, the alias cones contribute *opposite* flow and the net pump cancels. So the pump is an **alias thermometer**: an operational, pre-registrable observable whose deviation from the single-cone value measures exactly the doubling content H3 is fighting. The static kink charge, being window-local, is the safer H2 observable; the pump is the H2/H3 bridge.

## H1 — Don't select a point; quotient. Equivalence = carrier automorphisms ⋉ constraint-exact shifts

Your negative spine (trace-only linear selectors, metric-dependence of quadratic minimization, the `(4,2)` signature, the disk of complements) collectively says: *no selector exists*. The move that respects that is to stop looking for one and instead define the physical **quotient** — which your own gate explicitly allows ("a theorem identifying the quotient").

**Proposed datum and relation.** Datum `𝒟 = (V, J, Γ, {γ_e} with the edge structure, D, gauge action ρ, constraint differential Q_G)` — everything on your candidate list, nothing else. Two refinements are equivalent iff related by a composition of:

1. **Automorphism conjugation:** `U` J-unitary, commuting with `Γ` and `D`, mapping the soldering set to itself up to the gauge action, normalizing `ρ(G)`. This is deliberately far smaller than the abstract `O(4,2)` — commuting with `D` and preserving edge-locality of the `γ_e` are the constraints your "what does not count" list demands be checked, and computing this stabilizer on the six-coordinate normal form is exact rational linear algebra.
2. **Constraint-exact shifts:** a zero-sum shift `s` is invisible iff it is exact for the carrier's own differential — `s = {Q_G, r}` in the graded sense — so that every Gauss-sector expectation kills it. This is the principled version of "invisible to physics": BRST-exactness, not vanishing under a hand-picked functional.

**Why this escapes your no-gos.** The commutator-blind classification kills scalar-*linear* selectors; the invariants of this quotient are nonlinear trace words in the full datum — `tr(J Q_X D Q_Y D)`, `tr(Γ Q_X Q_Y)`, compressed spectra — which are coordinate-free (no preassigned matrix entries), automorphism-invariant by construction, and see commutators. The positivity no-go isn't contradicted because no complement is chosen; the disk itself gets quotiented.

**The theorem, with both outcomes acceptable.** Compute `Moduli = ShiftTorsor / (ExactShifts ⋊ Aut(𝒟))` on the live sector. If it's a point: the decomposition is physically unique — gate closed positively, and the named channels are vindicated as the unique physical split. If it's `k`-dimensional: the split carries `k` genuine physical moduli — *new structural coupling constants* — and the honest statement becomes "channel-dependent observables must be labeled by the moduli coordinates," which is your sharp no-go and equally closes the gate. Either way the deliverable is a finite rational computation on the normal form.

**The example/control triple.** (1) Inequivalent pair: two points of the disk separated by a computed trace-word invariant. (2) Equivalent nontrivial pair: conjugation by a nontrivial gauge element acting nontrivially on the six coordinates, or addition of a nonzero exact shift `{Q_G, r}`. (3) Invariant selector: any trace word, or the ground-state budget with the positive complement transported covariantly (`P ↦ UP` — state and operator transform together, so the shares are manifestly orbit-invariant).

**Literature fit.** This is the NCG-native notion: gauge equivalence of finite spectral triples = unitary equivalence + inner fluctuation (Chamseddine–Connes; van Suijlekom's *Noncommutative Geometry and Particle Physics*; Krajewski's classification of finite triples and its moduli-space treatments). "Refinements related by inner fluctuation of the datum are equivalent" is the one-sentence version, and framing your paper's relation as the finite-Krein instance of it gives the referee an anchor.

## H3 — One lemma and one reframing

**Lemma (Lean-ready, an afternoon):** a Laurent polynomial `p` on `T^d` with `|p| ≡ 1` is a monomial `c·z^m` (one variable: `p(z)·p*(1/z̄) = 1` as a Laurent identity forces no zeros in `ℂ*` and zero degree spread; induct on variables). Consequence: `det U(k) = c·e^{i m·k}` for any strict QCA — the integer vector `m` is a flow/GNVW-type invariant, **additive under composition**, and equal to `0` for every coin (k-independent layer) and every layer of Hamiltonian-exponential form. Only conditional shifts carry it.

**Reframing:** your three separate kills — momentum-independent onsite coins retain aliases, no degree-one nearest-neighbor factor, integer-frequency commutator-Wilson harmonics retain aliases — are all instances of one structural fact: *index-zero moves cannot de-alias*. Nielsen–Ninomiya applies to the `m = 0` sector (the "band below the gap" is globally defined and the Chern-slice argument runs); the **only** escape corridor is `m ≠ 0` — genuinely Floquet-anomalous, non-Hamiltonian architectures, whose 1D prototype is the pure shift (one right-mover, no partner: the minimal existence proof that the loophole is real, and already in your checkerboard's chiral halves). Practical protocol: every candidate architecture should declare its determinant monomial `m` *first*; `m = 0` ⇒ reject without running the zone classification. Whether a strict 3+1 QCA with `m ≠ 0` and one clean cone exists is, to my knowledge, genuinely open — the place to look is the chiral-Floquet literature (Rudner–Lindner–Berg–Levin winding; GNVW; Po–Fidkowski et al.), and a proof either way would be exactly your requested cross-architecture theorem. Compute `m` for the D4 control as the first data point.

## H4 — The missing piece is an intermediate cutoff, and the theorem then assembles from your landed modules

Take `S_a` = Fourier truncation to modes `|k| ≤ π/a` composed with your DFT (you already have restriction/zero-padding and multiplier convergence; point-sampling needs `s > 3/2` and a Poisson-summation aliasing term — offer both, but truncation is the cleaner Lean object, with `S_aI_a = id` exact and `I_aS_a = Π_a`). The obstruction to the naive telescope is that symbol consistency fails near the zone edge. Split at `K(a) = a^{−θ}`:

- consistency on `|k| ≤ K`: per-step error `≤ C a^{p+1}K^{p+1}` (order-`p` accurate symbol; `p = 2` for palindromic splittings), telescoped over `t/a` steps: `C t a^p K^{p+1}`;
- tail on `|k| > K`: `≤ K^{−s}‖ψ‖_{H^s}`.

Balancing gives `K = a^{−p/(p+1+s)}` and the final bound `‖I_a U_a^{[t/a]}S_aψ − e^{−itH_D}ψ‖ ≤ C(t)·a^{ps/(p+1+s)}‖ψ‖_{H^s}` — for `p = 2`, rate `a^{2s/(3+s)}`, with every constant and both maps explicit. This is Lax–Richtmyer (stability from unitarity + consistency) with the cutoff split doing the physical-scaling work. Two remarks worth putting in the paper: the doublers sit at `|k| ≈ π/a`, i.e., *inside the tail term for any fixed `H^s` datum* — so **H4 closes independently of H3** (doubling obstructs locality and matter content, not the smooth-data continuum limit); and the theorem consumes your existing modules as lemmas — the only new content is the three-term assembly.

## H7 — Retarget the gate at hierarchy, and the mechanism is already in your RG data

Absolute scale is behind your own event horizon (ratios only, ever), and the scale-collapse no-go is the finite avatar of Derrick's theorem — homogeneous minimization was never going to work. But *dimensional transmutation is a flow phenomenon, not a minimization*, and your landed 3-coupling RG contains the required ingredient: the eigenvalue `−1` **marginal** direction. Along a period-2 marginal direction with `R(g) = −g + bg² + cg³ + …`, the doubled map is `R²(g) = g − 2(c + b²)g³ + …` — the quadratic terms cancel identically and the leading flow is cubic. Nonzero `c + b²` gives marginal (ir)relevance with `g(ℓ)² ∼ 1/(2(c+b²)ℓ)` and an RG-invariant scale `ξ ∼ exp(const/g₀²)` — a KT-type essential singularity: an exponential hierarchy generated from dimensionless data, with the held-out coefficient `(c + b²)` computed exactly from your rational map, no inserted dimensionful constant. If `c + b² = 0` identically, you instead have an exactly marginal line — continuously varying exponents, also a theorem, also interesting. The computation is exact rational Taylor expansion of `R3` along the `−1` eigenvector — precisely your house style. **This meets the gate as stated:** dimensionless microscopic inputs, a stable RG-invariant scale (as a ratio to the cutoff), a held-out coefficient, all matching inputs disclosed; and it correctly does *not* promise the electron mass in MeV.

## H6 — You don't need Lieb–Robinson for the gate layer; circuits give exact cones

For a *schedule of gates* the light cone is combinatorial and exact, and the graded subtlety dissolves because your gates are even: an even CAR operator commutes (ordinary commutator) with *any* operator of disjoint support, odd included — the grading sign `(−1)^{|g||A|}` is `+1` when `|g| = 0`. So the ladder is: (L1) even-gate disjoint-support commutation, provable from your existing CAR sign lemmas; (L2) depth-1 layer conjugation lemma — support grows into the union of blocks meeting it; (L3) induction over layers: radius `≤ r·depth`, exact, with exact outside-cone commutation (not an LR tail); (L4) controls you mostly have — the global-number-operator counterexample plus one gate on a geometrically spread four-mode set as the explicit nonlocal-block failure witness. Genuine Lieb–Robinson enters only at your *next* step (continuous-time local quartic Hamiltonian); import the fermionic LR of Nachtergaele–Sims–Young (and the Bru–de Siqueira Pedra treatment) rather than proving it, and keep all stated observables even so ordinary commutators suffice — reserving the graded statement or a controlled Jordan–Wigner for odd correlators, exactly as your brief anticipates.

---

If I had to order these by expected value per week of effort: the **H5 frame-index check** is first — it's decidable on fixtures you already have, and either outcome sharpens the paper immediately. The **H2 kink-charge fixture** is second — it converts the program's most vulnerable criticism ("a reparametrized Dirac mass") into a free-theory theorem with a held-out universal coefficient. The **H1 quotient computation** is third — it's the one that turns your beautifully complete negative spine into a positive classification, and both of its possible outcomes are publishable closures of the gate.
