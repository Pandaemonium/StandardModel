# Aristotle semantic context pack

Generated: 2026-07-05T08:09:25
Query: `Q6 pairSum_le_expBound finite rooted tree exponential inequality PolymerKPConclusion canonical root deletion multinomial multiplicity`

Use this as context, not as proof. Verify every imported theorem
statement and source convention against the live repo before relying on it.

## Repo docs and Lean hits

### 1. `PhysicsSM/StandardModel/SpinorFockHypercharge.lean` [toMultiplet_weak]

Score: `0.761`

```text
theorem toMultiplet_weak :
    toMultiplet ({3, 4} : Finset (Fin 5)) = AllLeftMultiplet.e_Rc := by decide
```

### 2. `PhysicsSM/Draft/CheckerboardCornerCountAristotle.lean` [pathWeight_eq_pow_turnCount]

Score: `0.749`

```text
theorem pathWeight_eq_pow_turnCount [Semiring S] (mu : S)
    (d : Direction) (h : List Direction) :
    pathWeight mu d h = mu ^ turnCount d h := by
  induction' h with e rest ih generalizing d;
  · simp +decide;
  · cases d <;> cases e <;> simp +decide [ *, pow_add ];
    · rw [ turnCount_cons ] ; simp +decide;
    · simp +decide [ *, turnWeight, turnCount ];
      rw [ pow_add, pow_one ];
    · simp +decide [ turnWeight, turnCount ];
      rw [ pow_add, pow_one ];
    · rw [ turnCount_cons ] ; simp +decide

/-! ## Target 2: the path sum as a corner-counting polynomial -/

/-
**Discrete kernel expansion.**  The finite checkerboard path sum is the
polynomial in the corner weight `mu` whose `k`-th coefficient is the number
of length-`n` histories with the prescribed endpoint data and exactly `k`
corners.
-/
```

### 3. `PhysicsSM/Draft/CheckerboardCornerPolynomialAristotle.lean` [histories_map_flip_perm]

Score: `0.747`

```text
theorem histories_map_flip_perm (n : Nat) :
    ((histories n).map (fun h => h.map Direction.flip)).Perm (histories n) := by
  induction' n with n ih <;> simp_all +decide [ List.map_map, Function.comp ];
  refine' List.Perm.trans _ ( List.Perm.append ( List.Perm.map _ ih ) ( List.Perm.map _ ih ) );
  simp +decide [ List.map_map, Function.comp_def ];
  exact List.perm_append_comm

/-- A polynomial regrouping: a list-sum of `mu`-powers indexed by a weight
`w a ≤ N` equals the sum over `k ≤ N` of the number of elements of weight `k`
times `mu ^ k`. -/
```

### 4. `PhysicsSM/Draft/CheckerboardKernelClosedFormsAristotle.lean` [turnHistories_poly_eq_closed]

Score: `0.747`

```text
theorem turnHistories_poly_eq_closed (n : Nat) (startDir : Direction)
    (dx : Int) (finishDir : Direction) (k : Nat) :
    PhysicsSM.Draft.CheckerboardCornerPolynomial.turnHistories
        n startDir dx finishDir k =
      PhysicsSM.Draft.CheckerboardCornerClosedForms.turnHistories
        n startDir dx finishDir k := by
  rfl

/-!
## Nat-level vanishing helpers

These say the binomial run-counts vanish once the requested corner count
exceeds the path length, so the closed-form coefficient sums can be reindexed
without changing their value.
-/

/--
The even run-count vanishes once `2*r` exceeds the path length `p + q`
when `0 < q`.
-/
```

### 5. `PhysicsSM/Draft/NullEdgeOvernightSynthesisAristotle.lean` [rlClosedForm]

Score: `0.746`

```text
def rlClosedForm [Semiring S] (mu : S) (p q : Nat) : S :=
  if q = 0 then
    0
  else
    ∑ r ∈ Finset.range (p + 1),
      ((p.choose r * (q - 1).choose r : Nat) : S) * mu ^ (2 * r + 1)

/-- Directed endpoint kernel for a right-incoming checkerboard path. -/
```

### 6. `PhysicsSM/NullStrand/Ergodic/RefreshChain.lean` [refreshKernel_invariant]

Score: `0.742`

```text
theorem refreshKernel_invariant
    (r : ℝ) (π : Ω → ℝ) (hπ : SimplexLaw π) :
    ∀ ω', ∑ ω, π ω * refreshKernel r π ω ω' = π ω' := by
  intro ω'
  rcases hπ with ⟨_, hsum⟩
  have hDelta :
      (∑ ω, π ω * (if ω = ω' then (1 : ℝ) else 0)) = π ω' := by
    calc
      (∑ ω, π ω * (if ω = ω' then (1 : ℝ) else 0))
          = ∑ ω, if ω = ω' then π ω else 0 := by
            refine Finset.sum_congr rfl ?_
            intro ω _hω
            by_cases h : ω = ω' <;> simp [h]
      _ = π ω' := by simp
  have hConst :
      (∑ ω, π ω * π ω') = π ω' * (∑ ω, π ω) := by
    rw [← Finset.sum_mul]
    ring
  calc
    ∑ ω, π ω * refreshKernel r π ω ω'
        = ∑ ω, π ω *
            ((1 - r) * (if ω = ω' then (1 : ℝ) else 0) + r * π ω') := by
          rfl
    _ = (1 - r) * (∑ ω, π ω * (if ω = ω' then (1 : ℝ) else 0)) +
          r * (∑ ω, π ω * π ω') := by
          calc
            ∑ ω, π ω *
                ((1 - r) * (if ω = ω' then (1 : ℝ) else 0) + r * π ω')
                = ∑ ω,
                    ((1 - r) * (π ω * (if ω = ω' then (1 : ℝ) else 0)) +
                      r * (π ω * π ω')) := by
                  refine Finset.sum_congr rfl ?_
                  intro ω _hω
                  ring
            _ = (∑ ω, (1 - r) * (π ω * (if ω = ω' then (1 : ℝ) else 0))) +
                  (∑ ω, r * (π ω * π ω')) := by
                  rw [Finset.sum_add_distrib]
            _ = (1 - r) * (∑ ω, π ω * (if ω = ω' then (1 : ℝ) else 0)) +
                  r * (∑ ω, π ω * π ω') := by
                  rw [← Finset.mul_sum, ← Finset.mul_sum]
    _ = (1 - r) * π ω' + r * (π ω' * (∑ ω, π ω)) := by
          rw [hDelta, hConst]
    _ = π ω' := by
          rw [hsum]
          ring

/-- Exact detailed-balance identity for a fixed `r` and target `π`. -/
```

### 7. `PhysicsSM/Draft/NullEdgeOvernightSynthesisAristotle.lean` [rrClosedForm]

Score: `0.740`

```text
def rrClosedForm [Semiring S] (mu : S) (p q : Nat) : S :=
  if q = 0 then
    1
  else
    ∑ r ∈ Finset.Icc 1 (p + q),
      ((p.choose r * (q - 1).choose (r - 1) : Nat) : S) * mu ^ (2 * r)

/-- Right-starting, left-terminal closed form including the straight boundary case. -/
```

### 8. `PhysicsSM/Draft/CheckerboardCornerPolynomialAristotle.lean` [pathWeight_eq_pow_turnCount]

Score: `0.740`

```text
theorem pathWeight_eq_pow_turnCount [Semiring S] (mu : S)
    (d : Direction) (h : List Direction) :
    pathWeight mu d h = mu ^ turnCount d h := by
  induction' h with e rest ih generalizing d;
  · cases d <;> simp +decide [ pathWeight, turnCount ];
  · cases h : e == d <;> simp_all +decide [ pow_add, turnCount_cons ];
    cases d <;> cases e <;> tauto

/-- The finite checkerboard path sum is the polynomial in `mu` whose
coefficients count histories with fixed endpoint data and fixed corner count. -/
```

## Scoped paper hits

### 1. Tri-partitions and Bases of an Ordered Complex

Score: `0.727`
Zotero key: `D7352JCI`
DOI: `10.1007/s00454-020-00188-x`
URL: https://doi.org/10.1007/s00454-020-00188-x

### 2. Random Walks on Simplicial Complexes and the Normalized Hodge 1-Laplacian

Score: `0.697`
Zotero key: `N7T76U5H`
arXiv: `1807.05044`
DOI: `10.1137/18M1201019`
URL: https://doi.org/10.1137/18M1201019

### 3. Local d'Alembertian for causal sets

Score: `0.696`
Zotero key: `I72KXVQA`
arXiv: `2506.18745`

### 4. Higher-order Laplacian renormalization

Score: `0.688`
Zotero key: `RA8QNNKW`
arXiv: `2401.11298`
DOI: `10.1038/s41567-025-02784-1`
URL: https://doi.org/10.1038/s41567-025-02784-1

### 5. Laplacian Coarse Graining in Complex Networks

Score: `0.686`
Zotero key: `UR5ADCBP`
arXiv: `2302.07093`
URL: http://arxiv.org/abs/2302.07093
