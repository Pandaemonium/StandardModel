# Aristotle semantic context pack

Generated: 2026-07-20T07:53:48
Query: `HNU exact Weyl walk adaptive changing momentum window normalized cell coefficient L2 convergence inverse Fourier`

Use this as context, not as proof. Verify every imported theorem
statement and source convention against the live repo before relying on it.

## Repo docs and Lean hits

### 1. `AgentTasks/model-calls/claude/2026-07-12-200701-afpl-exact-flow-cell-integral-audit.md` [Scaled live-walk convergence for changing-cell coefficients]

Score: `0.828`

```text
# Scaled live-walk convergence for changing-cell coefficients

This module composes the two previously separate halves of the changing-
lattice momentum program:

1. representative-safe normalized cell coefficients extracted from an actual
   four-component `L2(R^3)` field;
2. the live quartic-step `3+1` split walk evaluated at the physical center of
   every scheduled momentum cell.

The main theorem proves that the live-versus-exact coefficient error, embedded
back into the same normalized momentum cells, converges strongly to zero in
the sum of the four component `L2` norms. The coefficient family is defined
from the input field by `cellCoefficient`; it is not an arbitrary sequence or
an assumed convergent discretization.

Honest scope: the exact comparison flow is the landed momentum-space Dirac
multiplier. This module does not apply a continuum inverse Fourier transform,
identify a position-space PDE solution, or prove Lorentz restoration.

Provenance: clean-room composition of
`ChangingMomentumCellCoefficientBridge`, `ScaledChangingMomentumWalk`, and
Mathlib's Euclidean-spinor norm, July 12, 2026.
-/

noncomputable section

open scoped BigOperators ENNReal Matrix.Norms.L2Operator
open MeasureTheory Set Filter Topology

namespace PhysicsSM.Draft.NullEdge.ChangingCellScaledLiveWalk

open ChangingMomentumCellIsometry
open ChangingMomentumCellCoefficientBridge
open ChangingMomentumCellProjectionStrongScaffold
open ChangingModeEmbedding
open ScaledChangingMomentumWalk
open Compact3Plus1DiracRate

/-- Four-component coefficient space used by the live matrix walk. -/
abbrev Spinor := EuclideanSpace Complex (Fin 4)

/-- Coordinate bridge between the cell code's functional `Z^3` presentation
and the walk code's nested-product presentation. -/
def mode3Equiv : Mode3 ≃ Mode
```

### 2. `AgentTasks/aristotle-downloads/da29672d-gate1-extract/output-final_aristotle/POSITION_SPACE_REPORT.md` [What is delivered]

Score: `0.825`

```text
m fibre
     bound `bulk_bound`, pointwise vanishing `bulk_ptwise`).
   * `hnu_position_space_L2_convergence` (**capstone**): the band-restricted
     HNU-walk approximation converges in `L²` to the exact Weyl-flow field,
     assembled through `ChangingLatticePDECore.band_approx_tendsto_zero`.  The
     high-frequency tail is discharged *unconditionally* inside that lemma by
     `ChangingLatticePDECore.uv_tail_tendsto_zero`.

4. **All conventions exposed** (see the module header): spinor normalization and
   `L²` inner product, the inherited HNU sign convention
   `Uⱼ⁺(θ)=e^{-iθ}Pⱼ⁺+Pⱼ⁻`, the Weyl generator `Hweyl k = k₀σ₁+k₁σ₂+k₂σ₃` and
   flow `weylFlow t k = exp(-(t)(i·Hweyl k))`, the `t/(n+1)` time/step scaling,
   the axis-3-doubled lattice spacing inherited from `HNURealSpace`, and the
   discrete/real Fourier dictionary `kR`.

5. **Nonzero compact-momentum witness.** `witness_packet` (a fixed nonzero spinor
   on the unit momentum ball, zero outside): `witness_packet_mem`
   (`CompactMomentumPacket`) and `witness_packet_ne_zero`.

6. **Infrared tangent (reused).** `endpoint_tangent_is_weyl_generator` re-exposes
   `HNUInfraredTangent.endpoint_ray_hasDerivAt`, and `weylFlow_hasDerivAt_zero`
   shows the Weyl flow has exactly the same generator `-i·Hweyl` at `t=0`, so the
   limit object is genuinely the flow of the HNU infrared tangent.
```

### 3. `AgentTasks/aristotle-downloads/da29672d-telescope-extract/output-final_aristotle/POSITION_SPACE_REPORT.md` [What is delivered]

Score: `0.825`

```text
m fibre
     bound `bulk_bound`, pointwise vanishing `bulk_ptwise`).
   * `hnu_position_space_L2_convergence` (**capstone**): the band-restricted
     HNU-walk approximation converges in `L²` to the exact Weyl-flow field,
     assembled through `ChangingLatticePDECore.band_approx_tendsto_zero`.  The
     high-frequency tail is discharged *unconditionally* inside that lemma by
     `ChangingLatticePDECore.uv_tail_tendsto_zero`.

4. **All conventions exposed** (see the module header): spinor normalization and
   `L²` inner product, the inherited HNU sign convention
   `Uⱼ⁺(θ)=e^{-iθ}Pⱼ⁺+Pⱼ⁻`, the Weyl generator `Hweyl k = k₀σ₁+k₁σ₂+k₂σ₃` and
   flow `weylFlow t k = exp(-(t)(i·Hweyl k))`, the `t/(n+1)` time/step scaling,
   the axis-3-doubled lattice spacing inherited from `HNURealSpace`, and the
   discrete/real Fourier dictionary `kR`.

5. **Nonzero compact-momentum witness.** `witness_packet` (a fixed nonzero spinor
   on the unit momentum ball, zero outside): `witness_packet_mem`
   (`CompactMomentumPacket`) and `witness_packet_ne_zero`.

6. **Infrared tangent (reused).** `endpoint_tangent_is_weyl_generator` re-exposes
   `HNUInfraredTangent.endpoint_ray_hasDerivAt`, and `weylFlow_hasDerivAt_zero`
   shows the Weyl flow has exactly the same generator `-i·Hweyl` at `t=0`, so the
   limit object is genuinely the flow of the HNU infrared tangent.
```

### 4. `PhysicsSM/Draft/NullEdge/ChangingCellScaledLiveWalk.lean`

Score: `0.817`

```text
import PhysicsSM.Draft.NullEdge.ChangingMomentumCellCoefficientBridge
import PhysicsSM.Draft.NullEdge.ChangingMomentumCellProjectionStrongScaffold
import PhysicsSM.Draft.NullEdge.ScaledChangingMomentumWalk

/-!
# Scaled live-walk convergence for changing-cell coefficients

This module composes the two previously separate halves of the changing-
lattice momentum program:

1. representative-safe normalized cell coefficients extracted from an actual
   four-component `L2(R^3)` field;
2. the live quartic-step `3+1` split walk evaluated at the physical center of
   every scheduled momentum cell.

The main theorem proves that the live-versus-exact coefficient error, embedded
back into the same normalized momentum cells, converges strongly to zero in
the sum of the four component `L2` norms. The coefficient family is defined
from the input field by `cellCoefficient`; it is not an arbitrary sequence or
an assumed convergent discretization.

Honest scope: the exact comparison flow is the landed momentum-space Dirac
multiplier. This module does not apply a continuum inverse Fourier transform,
identify a position-space PDE solution, or prove Lorentz restoration.

Provenance: clean-room composition of
`ChangingMomentumCellCoefficientBridge`, `ScaledChangingMomentumWalk`, and
Mathlib's Euclidean-spinor norm, July 12, 2026.
-/
```

### 5. `AutonomousLab/reviews/CLAUDE_REVIEW_HNUManyStepContinuum_2026-07-13.md` [Bottom line]

Score: `0.810`

```text
## Bottom line

APPROVE-SUBSET. A genuine, kernel-clean (standard-three, independently rebuilt),
non-vacuous fixed-momentum result: the n-step HNU endpoint word with step `t/n`
converges in L2 operator norm to the exact Weyl flow `exp(-i t (q.sigma))` at rate
`O(1/n)`, with `Hw = q.sigma` derived (not assumed) as the endpoint's first-order
generator. Faithful to the live endpoint (hand-verified equal, same 8-factor word,
same signs/projectors/order/half-steps). Integrate via an EXPLICIT BRIDGE
(reproduced factorization = live endpoint) rather than rebuilding on the live file,
keep the rotation layer continuum-local, and convert the bare `#print axioms` to
`#guard_msgs`. Manuscript may state the fixed-momentum O(1/n) finite-time
convergence with forbidden claims 1-5 respected.
```

### 6. `AgentTasks/aristotle-downloads/da29672d-telescope-extract/output-final_aristotle/ARISTOTLE_SUMMARY.md` [Summary of changes for run fe61a4d3-42b1-4ac7-86e4-020d9c3bb3a3]

Score: `0.810`

```text
edTangent`).
- The real-space HNU step realizes exactly the endpoint symbol (`realspace_step_has_endpoint_symbol`, re-exposing `HNURealSpace.schedule_symbol`).
- Fixed-momentum many-step convergence to the Weyl flow with no exponential-in-n loss (`manystep_bound`, `manystep_tendsto`), reusing `FixedMomentumManyStepContinuum.unitary_pow_telescope`.
- Bulk (in-band) L² multiplier convergence by dominated convergence (`bulk_bound`, `bulk_ptwise`, `bulk_tendsto`).
- Capstone `hnu_position_space_L2_convergence`: the band-restricted HNU-walk approximation converges in L² to the exact Weyl-flow field, assembled through `ChangingLatticePDECore.band_approx_tendsto_zero`, with the high-frequency tail discharged unconditionally via `ChangingLatticePDECore.uv_tail_tendsto_zero`.
- A nonzero compactly-supported-in-momentum witness (`witness_packet`, `witness_packet_mem`, `witness_packet_ne_zero`).

Honest conditional structure and remaining obstruction: the capstone is conditional on exactly one named uniform multiplier estimate, `ShortStepWeylEstimate` (the endpoint symbol deviates from the Weyl flow it generates by O(step²), with a per-momentum constant — the exact analogue of the `Dkm` one-step bound already proved for the split-step walk). There is no semantic gap: given this one estimate, the L² convergence is fully proved and the tail is discharged unconditionally. The estimate is genuine and non-vacuous (e.g. trivially holds at k=0, cf. `manystep_at_zero`); proving it in general requires a quantitative second-order remainder for the depth-eight matrix product and matrix exponential, which is stated as the precise remaining gate.

All conventions (spinor/L² normalization, HNU sign convention, Weyl generator, t/(n+1) time-step scaling, axis-3-doubled lattice spacing, Fourier di
```

### 7. `AgentTasks/aristotle-downloads/da29672d-gate1-extract/output-final_aristotle/ARISTOTLE_SUMMARY.md` [Summary of changes for run fe61a4d3-42b1-4ac7-86e4-020d9c3bb3a3]

Score: `0.810`

```text
edTangent`).
- The real-space HNU step realizes exactly the endpoint symbol (`realspace_step_has_endpoint_symbol`, re-exposing `HNURealSpace.schedule_symbol`).
- Fixed-momentum many-step convergence to the Weyl flow with no exponential-in-n loss (`manystep_bound`, `manystep_tendsto`), reusing `FixedMomentumManyStepContinuum.unitary_pow_telescope`.
- Bulk (in-band) L² multiplier convergence by dominated convergence (`bulk_bound`, `bulk_ptwise`, `bulk_tendsto`).
- Capstone `hnu_position_space_L2_convergence`: the band-restricted HNU-walk approximation converges in L² to the exact Weyl-flow field, assembled through `ChangingLatticePDECore.band_approx_tendsto_zero`, with the high-frequency tail discharged unconditionally via `ChangingLatticePDECore.uv_tail_tendsto_zero`.
- A nonzero compactly-supported-in-momentum witness (`witness_packet`, `witness_packet_mem`, `witness_packet_ne_zero`).

Honest conditional structure and remaining obstruction: the capstone is conditional on exactly one named uniform multiplier estimate, `ShortStepWeylEstimate` (the endpoint symbol deviates from the Weyl flow it generates by O(step²), with a per-momentum constant — the exact analogue of the `Dkm` one-step bound already proved for the split-step walk). There is no semantic gap: given this one estimate, the L² convergence is fully proved and the tail is discharged unconditionally. The estimate is genuine and non-vacuous (e.g. trivially holds at k=0, cf. `manystep_at_zero`); proving it in general requires a quantitative second-order remainder for the depth-eight matrix product and matrix exponential, which is stated as the precise remaining gate.

All conventions (spinor/L² normalization, HNU sign convention, Weyl generator, t/(n+1) time-step scaling, axis-3-doubled lattice spacing, Fourier di
```

### 8. `AgentTasks/aristotle-downloads/da29672d-gate1-extract/output-final_aristotle/POSITION_SPACE_REPORT.md` [What is delivered]

Score: `0.810`

```text
## What is delivered

An honest **conditional capstone** for the position-space (momentum-space `L²`)
continuum limit of the exact HNU depth-eight Floquet update, built by reusing the
requested existing infrastructure and adding the missing analytic composition:

1. **Fourier-multiplier bridge for a stated dense class.**  Momentum space is
   `Mom = Fin 3 → ℝ` with Lebesgue `volume`; the spinor fibre is
   `Spinor = EuclideanSpace ℂ (Fin 2)` with its genuine `L²` inner-product norm;
   a `2×2` symbol acts fibrewise by `act M v = Matrix.toEuclideanCLM M v`.  The
   dense class is `CompactMomentumPacket f := HasCompactSupport f ∧ MemLp f 2`
   (compactly supported in momentum, square-integrable).  `approxField` is the
   `n`-step HNU-walk multiplier field, `exactField` the exact Weyl-flow field.

2. **Real-space step ⇒ exact endpoint symbol (reused, exact).**
   `realspace_step_has_endpoint_symbol` re-exposes `HNURealSpace.schedule_symbol`:
   the finite real-space spin-conditioned schedule has, on every Fourier mode,
   exactly the momentum-space symbol `HNUExactCore.endpoint`.

3. **Compact-momentum many-step + high-frequency tail ⇒ `L²` convergence.**
   * `manystep_bound` / `manystep_tendsto`: at every fixed momentum, the `n`-fold
     composite of the short-step endpoint symbol converges to the exact Weyl flow
     with no exponential-in-`n` loss — proved by reusing
     `FixedMomentumManyStepContinuum.unitary_pow_telescope`.
   * `bulk_tendsto`: the in-band `L²` multiplier error tends to zero for any
     compact-momentum packet — proved by dominated convergence (uniform fibre
     bound `bulk_bound`, pointwise vanishing `bulk_ptwise`).
   * `hnu_position_space_L2_convergence` (**capstone**): the band-restricted
     HNU-walk approximation converges in `L²` to the e
```

## Scoped paper hits

### 1. Random Walks on Simplicial Complexes and the Normalized Hodge 1-Laplacian

Score: `0.743`
Zotero key: `N7T76U5H`
arXiv: `1807.05044`
DOI: `10.1137/18M1201019`
URL: https://doi.org/10.1137/18M1201019

### 2. Finite-Difference Approach to the Hodge Theory of Harmonic Forms

Score: `0.736`
Zotero key: `TSAQXS9N`
DOI: `10.2307/2373615`
URL: https://doi.org/10.2307/2373615

### 3. Dirac equation with an ultraviolet cutoff and a quantum walk

Score: `0.725`
Zotero key: `G7NXEZBU`
DOI: `10.1103/physreva.81.012314`

### 4. Weyl, Dirac and Maxwell Quantum Cellular Automata: analytical solutions and phenomenological predictions of the Quantum Cellular Automata Theory of Free Fields

Score: `0.724`
Zotero key: `KCQGEDJE`
arXiv: `1601.04842`
URL: http://arxiv.org/abs/1601.04842v1

### 5. Relativistic effects and rigorous limits for discrete- and continuous-time quantum walks

Score: `0.722`
Zotero key: `QSB24VR9`
DOI: `10.1063/1.2759837`
URL: https://doi.org/10.1063/1.2759837
