# Aristotle semantic context pack

Generated: 2026-07-04T14:51:15
Query: `YM1 boundary-circuit expectation theorem RectBoundaryLasso RectTreeGauge tree gauge bridge lasso ensemble partition Theorem 2`

Use this as context, not as proof. Verify every imported theorem statement and
source convention against the Lean files.

## Selection Note

The automatic semantic search returned noisy top hits from unrelated Hamming
and anomaly files.  After running the required preflight, this pack was curated
from live repo inspection of the actual Q11/YM1 dependency surface.

## Relevant Repo Context

### `RectBoundaryLasso.lean`

The integrated Q11 lasso layer defines:

- `rectBoundaryWalk Lx Ly`: the counterclockwise full-rectangle boundary walk.
- `rectBoundary_hol_formula`: boundary holonomy order is bottom, right,
  inverse top, inverse left.
- `IsCombTreeSlice Lx Ly U`: all comb-tree links are identity.
- `reversedRowMajorPlaquetteProd Lx Ly U`: row-major product of all plaquette
  holonomies with horizontal index reversed inside each row.
- `rectBoundary_hol_eq_reversedRowMajorPlaquetteProd_of_treeSlice`: on the
  comb tree slice, boundary holonomy equals the reversed-row-major plaquette
  product.
- `apply_rectBoundary_hol_eq_reversedRowMajorPlaquetteProd_of_treeSlice`: the
  same identity after applying an arbitrary function.

Important guardrail: this module does not claim the pointwise identity at
general tree-coordinate values.

### `RectTreeGauge.lean`

The concrete open rectangle uses:

- vertices `Fin (Lx + 1) x Fin (Ly + 1)`;
- horizontal links `Sum.inl (i, j)`;
- vertical links `Sum.inr (i, j)`;
- `rectPlaquette Lx Ly (i, j)` with a counterclockwise pinned formula;
- `RectTree Lx Ly`, all horizontal links plus the leftmost vertical column;
- `rectCoordinatization`, a `TreeGaugeBridge.PlaquetteCoordinatization`;
- `rect_wilson_loop_expectation_area_law`, the exact area law for the
  link-ensemble observable
  `R.character (IndependentPlaquetteEnsemble.orderedProd fun k =>
  (rectPlaquette Lx Ly (e k)).hol U)`.

Remaining gap recorded in the module docstring before this submission:
identify the boundary-circuit Wilson observable with the ordered plaquette
observable at the expectation level.

### `TreeGaugeBridge.lean`

Useful APIs:

- `linkPartition`, `linkNumerator`, `linkExpectation`;
- `linkPartition_eq`;
- `linkNumerator_eq`;
- `linkExpectation_eq_loopExpectation`;
- `wilson_link_loop_expectation_area_law`.

Likely useful helper: an observable-congruence lemma for `linkNumerator` or
`linkExpectation`, if two observables agree pointwise or agree after a
change-of-variables preserving the weight.

### `GaugeCoreGeneral.lean`

Useful APIs:

- `OrientedLattice.gauge`;
- `OrientedLattice.gaugeEquiv`;
- `hol_gauge`;
- `hol_gauge_closed`;
- `classFunction_hol_gauge_closed`.

These are the expected tools for removing conjugations introduced by a rooted
gauge transformation: closed-walk holonomies are conjugated at the basepoint,
and class functions of closed-walk holonomies are gauge-invariant.

## Target

Target file in the focused package:

```text
PhysicsSM/Draft/NullEdge/GateYM/RectBoundaryExpectation.lean
```

Target theorem:

```text
TreeGaugeBridge.linkExpectation (rectPlaquette Lx Ly)
  (Theorem2AreaLaw.wilsonLocalWeightC beta rho)
  (fun U => R.character (OrientedLattice.hol U (rectBoundaryWalk Lx Ly)))
= R.character 1 * Theorem2AreaLaw.wilsonNormalizedGamma beta rho R ^ (Lx * Ly)
```

The intended result is finite and algebraic.  It is not an infinite-volume,
continuum, mass-gap, or transfer-matrix claim.
