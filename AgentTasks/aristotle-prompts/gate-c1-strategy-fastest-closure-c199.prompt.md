Project name: gate-c1-strategy-fastest-closure-c199

You are Aristotle, working on the StandardModel Lean/null-edge research project.

This is a high-level strategy job, not a narrow proof job.

Current status:

```text
We have a recommended first physical architecture:
  Wilson/Neuberger overlap reference
  + CKM internal branch/flavor mass texture
  + null-edge endpoint
  + gapped homotopy and certificate stack.

C193 is running on the combined free mass-window theorem:
  0 < m0 < min(2 r_W, 16 r_b).

The biggest unresolved object is the actual null-edge Wilsonized kinetic
endpoint and its kappa mismatch bound.
```

Task:

1. Audit whether this is the fastest credible route to `GateC1_NU`.
2. Identify the top five theorem obligations by value and dependency order.
3. Identify any assumptions that should be relaxed, removed, or frozen to avoid
   wasted work.
4. Decide whether the next Lean work should prioritize C193 integration,
   kappa-bound formalization, source certificates, or SMActsInternally.
5. Suggest one high-risk/high-reward job and one conservative job for the next
   round after this.

Success criteria:

```text
Be concrete.
Do not propose novelty for its own sake.
Prefer importing known working Wilson/Neuberger/domain-wall machinery where
possible.
Keep the null-edge contribution as the endpoint, sector map, CKM texture,
path-sum control, and homotopy certificate.
```

Please finish with:

```text
Verdict:
Top theorem obligations:
Frozen assumptions:
Recommended next Lean target:
Recommended next Aristotle jobs:
Risks:
```
