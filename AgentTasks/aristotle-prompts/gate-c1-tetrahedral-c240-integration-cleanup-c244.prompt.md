Gate C1 tetrahedral lattice-duality / Brillouin-torus theorem, C244

You are Aristotle helping the StandardModel null-edge Gate C1 program.

Context:

C240 completed the finite branch-window theorem for the tetrahedral free symbol
and flagged the load-bearing caveat:

```text
The 16-branch count requires that the four k_A are independent 2*pi-periodic
angle coordinates on the null-edge Brillouin torus.
```

C240 recommended the next local formalization target:

```text
Define the dual Brillouin torus T for Lambda = span_Z {n_A}, where
n_A = (1, v_A), and prove that

  p |-> (k_A(p))_A = (n_A dot p)_A

descends to an isomorphism

  T ~= (R / 2*pi Z)^4.
```

This turns the C240 branch-count assumption into a theorem.

Task:

Design and, if feasible, prove the finite Lean theorem needed for this
lattice-duality step.

Pro feedback sharpens the required model:

```text
The free null-edge lattice is the rank-4 abelian translation group
Lambda_tet ~= Z^4 generated by the four edge shifts e_A = a n_A.

Its dual Brillouin torus is Hom(Lambda_tet, U(1)) ~= (R / 2*pi Z)^4,
with independent coordinates k_A defined by T_A -> exp(i k_A).
```

Do not treat `p` as ordinary hypercubic Euclidean momentum.  If using
`k_A = p(e_A)`, make `p` a dual covector / character coordinate on the lattice.
Do not import a cubic Brillouin cell.

Acceptable levels:

Level 1, fully finite matrix theorem:

```text
The 4 x 4 matrix with rows (1, w_A), where w_A are the unscaled tetrahedron
vertices from C240, is invertible over R.
```

Use this to prove the coordinate map:

```text
p |-> N p
```

is a real-linear equivalence.

Level 2, integer lattice theorem:

```text
The same matrix maps Z^4 to an index-|det N| sublattice.
```

Explain whether the actual null-edge lattice should be this sublattice or a
rescaled primitive lattice. Identify any normalization required to make the
k_A genuinely 2*pi-periodic independent angles.

Level 3, torus theorem:

```text
The dual torus of Lambda = span_Z {n_A} is equivalent to (R / 2*pi Z)^4
under p |-> (n_A dot p)_A.
```

If Lean's quotient/torsor infrastructure is too heavy, return the exact theorem
statement and a proof plan with the finite matrix lemmas proved.

Relevant integrated file:

```text
PhysicsSM/Draft/NullEdge/GateC1/TetrahedralBranchWindow.lean
PhysicsSM/Draft/NullEdge/GateC1/TetrahedralLatticeDuality.lean
```

Codex has added a Draft scaffold in `TetrahedralLatticeDuality.lean` with:

```text
homogeneousTetraMap;
homogeneousTetraMap_eq_zero_imp;
homogeneousTetraMap_injective;
TetrahedralAngleCoordinateContract;
IndependentTetrahedralAngles.
```

The most valuable near-term proof targets are:

```text
homogeneousTetraMap_eq_zero_imp;
homogeneousTetraMap_injective.
```

Then upgrade the source-contract structure into an actual quotient-lattice or
torus theorem.

Constraints:

- Do not claim GateC1_NU.
- Do not assume cubic Brillouin geometry.
- Make all normalization and periodicity assumptions explicit.
- If the theorem depends on choosing the lattice `span_Z {n_A}` by definition,
  state that clearly.
- Treat nullness as part of the physical interpretation, not as a relation among
  the four rank-4 translation generators.

Requested output:

- Lean theorem(s), preferably in a standalone file;
- exact lattice and dual-lattice definitions used;
- determinant/index/normalization facts;
- what remains as a modeling assumption versus theorem;
- recommended integration target file.
