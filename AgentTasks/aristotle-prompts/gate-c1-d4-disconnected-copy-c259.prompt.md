Gate C1 C259: D4 sites with only h_A shifts decompose into 8 copies

You are Aristotle helping the StandardModel null-edge Gate C1 program.

Context:

C250/Pro say:

```text
L_H subset D4,
D4 / L_H ~= (Z/2)^3.
```

Therefore if fields live on D4 sites but the kinetic graph uses only the
original `h_A` null shifts, the graph splits into 8 disconnected cosets. This
is a guardrail against treating full D4 as a harmless relabeling of the active
tetrahedral theory.

Task:

Formalize the cleanest finite/group-theoretic statement of this disconnected
copy theorem.

Suggested abstraction:

```text
Let G be an additive commutative group.
Let H <= G be the subgroup generated by allowed shifts.
The graph on G with edges x -> x + h for h in H-generators has connected
components contained in cosets of H.
If the generators span H, components are exactly cosets of H.
```

Then instantiate, if feasible, with:

```text
G = D4,
H = L_H,
G/H has 8 elements.
```

If full graph formalization is too heavy, prove the algebraic core:

```text
Every path using only h_A shifts preserves the D4/L_H coset.
Conversely any two points in the same L_H coset differ by a finite sum of h_A
shifts.
```

Constraints:

```text
This is a D4 side-lane guardrail.
Do not replace L_H by D4.
Do not run a full lake build.
Do not claim GateC1_NU.
```

Requested output:

```text
1. Lean theorem statements/proofs if feasible.
2. If not feasible, a precise theorem skeleton and blocker.
3. Recommended integration target.
```
