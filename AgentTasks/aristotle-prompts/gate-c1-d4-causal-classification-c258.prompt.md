Gate C1 C258: D4 added-step causal classification

You are Aristotle helping the StandardModel null-edge Gate C1 program.

Context:

The active microscopic null-edge lattice is `L_H`, generated by four Hadamard
body diagonals:

```text
h_A = (1, +/-1, +/-1, +/-1).
```

Full D4 adds root-neighbor steps represented by half-sums and half-differences:

```text
(h_A + h_B)/2,
(h_A - h_B)/2,
A != B.
```

Pro says these are non-null under the physical Lorentz form:

```text
q(x) = -x_0^2 + (1/3)(x_1^2 + x_2^2 + x_3^2).
```

Specifically:

```text
q(h_A) = 0,
q((h_A + h_B)/2) = -2/3,
q((h_A - h_B)/2) = +2/3,
for A != B.
```

Task:

Formalize this causal classification in Lean. This can be a small standalone
finite computation over rationals.

Priority targets:

```text
1. Define the four h_A as Fin 4 -> Int or Rat.
2. Define q over Rat.
3. Prove q(h_A) = 0 for each A.
4. Prove q((h_A+h_B)/2) = -2/3 for A != B.
5. Prove q((h_A-h_B)/2) = +2/3 for A != B.
```

If a fully quantified `A != B` proof is annoying, a finite theorem by cases over
all pairs is acceptable, as long as the statement is semantically clear.

Constraints:

```text
This is a D4 guardrail theorem.
Do not use it to replace L_H by D4.
Do not run a full lake build.
Do not claim GateC1_NU.
```

Requested output:

```text
1. Lean file(s), if produced.
2. Theorem names and exact formulas.
3. Recommended integration target.
```
