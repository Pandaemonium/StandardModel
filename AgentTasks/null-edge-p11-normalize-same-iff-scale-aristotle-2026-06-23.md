# Aristotle task: P11 normalized equality is trace scaling

Target project: `null-edge-p11-normalize-same-iff-scale-20260623`

Target file:

```text
NullEdgeP11NormalizeSameIffScale/Core.lean
```

```yaml
aristotle:
  project_id: dcb87aab-08ca-4a47-87ce-ef30258747ba
  target_file: NullEdgeP11NormalizeSameIffScale/Core.lean
  expected_module: NullEdgeP11NormalizeSameIffScale.Core
  submission_project: AgentTasks/aristotle-submit/null-edge-p11-normalize-same-iff-scale-20260623-project
  status: integrated
```

Goal: close the proof holes in a focused standalone Mathlib package.

Scientific value: this sharpens the calibrated-readout claim. Equality of
normalized momentum states means the unnormalized blocks differ only by trace
scaling, so normalized channel state alone cannot carry invariant mass scale.

Required final report:

- solved target names;
- whether any theorem statement or definition changed;
- any remaining proof holes or placeholder constructs;
- exact Lean command run;
- a x i o m profile if available.
