# Summary of changes for run 67b6d8cb-6d78-4d8a-bb97-56fad2148459
Designed and fully proved (no proof placeholders, only standard kernel dependencies `propext`/`Classical.choice`/`Quot.sound`) the concrete Gate C1_NU `NonultralocalControl` instantiation for the null-edge overlap/sign kernel. All content is in `RequestProject/Main.lean`, which builds cleanly with no warnings, organized into 5 sections.

Core abstraction: a `ShellKernel` records per-shell operator-norm contributions `K n ≥ 0` (matrix elements grouped by graph/path distance n). A `PathShellCertificate sk` is a summable amplitude profile `amp` dominating `K` shell-by-shell, and `HasNonultralocalControl sk := Nonempty (PathShellCertificate sk)` is the gate predicate.

1. Path-shell certificate (requirement 1): `normBound` (certified operator-norm total `∑' amp`) with `normBound_nonneg`; shell `tail N`; `kernel_summable`; `tsum_kernel_le_normBound`; the amplitude tail-to-zero theorem `tail_tendsto_zero`; `kernel_tail_le_tail`; and the kernel tail-to-zero theorem `kernel_tail_tendsto_zero` (proved by squeeze through the certified tail). Finite propagation is handled by `hasControl_of_finitePropagation` (vanishing beyond a fixed radius ⇒ control via finite support).

2. Resolvent / sign-kernel certificate (requirement 2): `SignKernelCertificate` packages a gapped base kernel whose sign function / Riesz projector has a polynomial/resolvent expansion with gap-controlled geometric coefficient decay `|coeff n| ≤ A·ρ^n`, ρ<1 (no ultralocality assumed, only a gap). `SignKernelCertificate.hasControl` gives Gate C1_NU control of the induced sign kernel; `truncation_bound` gives the explicit geometric error bound `∑' n, |coeff (n+N)| ≤ A·ρ^N/(1-ρ)`; `truncation_tendsto_zero` is the truncation-to-zero theorem.

3. Bridge theorem (requirement 3): `OverlapAdmissible sk` encodes standard overlap admissibility / exponential locality (`K n ≤ const·rate^n`, rate<1), and `OverlapAdmissible.toControl` shows it supplies a Gate C1_NU control certificate.

4. Regime taxonomy (requirement 4): the four control regimes are realized as distinct constructors — finite propagation (`hasControl_of_finitePropagation`, plus `hasControl_of_ultralocal` as the degenerate special case), exponential locality (`hasControl_of_exponential`), generic summable path-shell control (the `PathShellCertificate` API itself), and power-law / regulated control (`hasControl_of_powerLaw`, `K n ≤ C/(n+1)^s`, s>1) — together with the explicit `ControlRegime` inductive (with a `controlled` predicate) whose `nonlocalFallback` branch is the declared non-control fallback, certified non-controlled by `nonlocalFallback_not_controlled`.

5. Gate distinctions (matching the stated constraints): `const_kernel_not_controlled` is the formal C171R witness that a bounded "sign-like" kernel (amplitude 1 on every shell) is NOT controlled — control is not inferred from the existence of sign(H). `geometric_not_ultralocal` together with `geometric_kernel_controlled` show a genuinely infinite-range (non-ultralocal) kernel is admissible — strict ultralocality is not required.

Lean-ready API structures and theorem statements (requirement 5) are exactly the `ShellKernel`, `PathShellCertificate`, `SignKernelCertificate`, `OverlapAdmissible`, and `ControlRegime` declarations with their theorems. Each theorem was verified to compile.
