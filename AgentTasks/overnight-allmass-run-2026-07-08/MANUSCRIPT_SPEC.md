# MANUSCRIPT_SPEC: "All mass from null edges" (overnight 2026-07-08)

Target file: `Sources/Null_Edge_All_Mass_Manuscript_2026-07-08_v1.md`.
Audience: expert physicists + one college-accessible Part I (the v3
P1-manuscript register). This is the ALL-mass manuscript: it subsumes and
cites the P1 (origin-of-mass) draft rather than replacing it.

## Structure (sections; grade every claim inline)

1. **Thesis and reading guide.** Mass is the obstruction to coherent
   null transport; "unification is decomposition". Claim calculus table.
2. **Part I (accessible): what is a particle, what is mass.** The
   zigzag/corner picture; charge as strand bookkeeping; stability as
   topology. (Prose register of the 07-07 session's particle
   explanations; no new claims.)
3. **The kinematic layer (TRUSTED).** det P = sum |psi_i wedge psi_j|^2;
   massless iff collinear. The one theorem everything else orbits.
4. **The budget theorem (the organizing spine).** The Weitzenboeck
   4-slot split; the S6 fraction theorem f_A + f_C + f_T + f_E = 1 (cite
   at whatever grade K4 achieved TONIGHT - if not landed, state as the
   registered target, grade C).
5. **Turn mass (Higgs-shaped).** Corner amplitude = mass; the
   weak-charge bookkeeping argument for why the turn field exists;
   index protection (chiral surplus cannot turn). REPORT THE KOIDE KILL
   honestly (kappa = 3/2, Route A dead, sec 4a) - a kill reported is
   evidence of discipline, not weakness.
6. **Closure mass (QCD-shaped).** Wilson action = squared closure
   defect (A0, pinned); area law + slab/OS gap (pinned); the S1 exact
   Krein square + S1-CC gate status; the CHROMOMAGNETIC rail (Q_C =
   hyperfine share; |F|^2 = defect Gram - never conflate); Banks-Casher
   count identity at tonight's grade; strong-coupling scope + Clay
   boundary named.
7. **Soldering mass (gravity-shaped).** E-slot status: constant-solder
   vanishing (pinned), trinity split (open, pre-registered), Malament
   split discipline. Shortest section; mostly honest boundary.
8. **Protected masslessness.** The index-protection family (pinned);
   symmetry-forced zero modes (sec 4b + tonight's T1/T2/T3 grade);
   neutrino-shaped readings at MEMO grade only.
9. **Dynamics: mass generation under coarse-graining.** The RG-Schur
   witness (pinned): decimation converts disagreement into mass;
   collinear control; one-step boundary stated.
10. **Boundaries and kills.** Continuum (Q06 ladder only), scale
    ([import] only), Clay named, the kill list, the spectral rail, the
    open-crux table with gates.
11. **The Lean anchor table.** EVERY theorem cited in sections 3-9 with:
    exact declaration name, file, guard-pin status, axiom footprint.

## The anchor rule (constitution-grade; the v3 false-anchor lesson)

No Lean name enters the manuscript until it has been verified THAT
NIGHT by grep against the repo (`Grep` the exact declaration name) and
its guard-pin status checked. The auditing agent re-runs the sweep
independently. A citation of a nonexistent or unpinned-but-claimed-
pinned declaration is a P0 finding.

## Audit gates (all must pass before MORNING_REPORT declares the
manuscript delivered)

- G1: cross-audit by the non-drafting agent - four over-claim modes
  (vacuity / hollow telescoping / docstring-outruns-kernel / false
  shape) per section, findings ledgered and fixed or downgraded.
- G2: independent anchor sweep (both agents, independently).
- G3: at least one external review call (send_claude_review.py,
  `--source-file` for the anchor table's load-bearing declarations,
  budget >= 1.50) with findings triaged in the ledger.
- G4: kill-list check - every killed route that a reader might expect
  (Koide, Tr E, defect-Gram-as-Q_C, one-sided GW) is explicitly
  reported as killed, with pointers.
- G5: lit check - every external-physics comparison cites a paper in
  the graph, verified at chunk level (Codex: Spark searches; Claude:
  scholarly MCP), logged in LIT_SEARCH_LOG.md.
