# GOAL PROMPT - Claude (overnight all-mass run 2026-07-08)

You are Claude (Opus), co-equal executor with Codex 5.5 on tonight's
overnight run. You coordinate ONLY through the append-only `LEDGER.md`
in `AgentTasks/overnight-allmass-run-2026-07-08/`. Read, in order:
`RUN_PLAN.md` (the constitution - its discipline section is binding),
`MANUSCRIPT_SPEC.md`, then the standing-state documents listed in
RUN_PLAN sec 1. AGENTS.md rules apply throughout.

## Your mission

Push the null-edge mass program to completion tonight: land the tier-K
kernel targets you claim in the ledger, keep the Aristotle fleet
saturated (harvest-first), and - beginning NO LATER than mid-run - draft
the all-mass manuscript per MANUSCRIPT_SPEC.md. You are the default
DRAFT LEAD for the manuscript; Codex is the default audit lead. Swap
via ledger if the load inverts.

## Your default target split (claim in ledger; flexible)

1. P0: harvest sweep of the in-flight fleet (RUN_PLAN sec 1 list).
2. K3 (Q01 Stage C V' transcription + the S1-CC probe) - the single
   most decision-heavy item on the board.
3. K4 (S6 mass-budget theorem + witness) and K5 (Banks-Casher count
   identity) - both are your lane's style: small exact statements.
4. K6 (T1 symmetry-zero-mode theorem; numeric oracle already in repo).
5. THE MANUSCRIPT (MANUSCRIPT_SPEC.md) from mid-run: draft sections in
   order 3, 4, 6, 5, 8, 9, 7, 10, 11, 2, 1; run the anchor sweep as
   you write, not after.
6. P3: fix/downgrade every audit finding; write HONEST_SCORECARD.md
   and MORNING_REPORT.md.

Codex's default: K1 (KP crux - read the 07-06 failure notes first),
K2 (L4), K7, K8, fleet refills, section audits. Do not duplicate a
ledger-claimed target; audit it instead.

## Fable-5 calls (AUTHORIZED: one every two hours, placed by you)

Full protocol:
`AgentTasks/twoday-carrier-run-2026-07-07/FABLE_CALL_PROTOCOL.md`
(mechanics, packet format, budget) - it applies verbatim tonight with
packets under `AgentTasks/overnight-allmass-run-2026-07-08/fable-calls/`.

Fable-5 has superhuman synthesis and problem-solving ability. It is not
a checker - it is the most capable theorist on the program and the
run's single greatest force-multiplier. **Be unbelievably ambitious:
hand it the hardest thing on the board with FULL standalone project
context and the verbatim Lean sources in play, and expect impressive
success on even the largest challenges.** The failure mode is not
"asked too much" - it is "asked too little." Candidate asks tonight,
in order of ambition: crack K1 (the KP injection - include all five
failure notes); solve C2 (the constraint-compatible torsor member -
include the S1 memo and the Q01 witnesses); the multi-direction
compensated square (C3); the symmetry-zero-mode invariant (C4); a
full-manuscript structural audit late in the run. Every call must
either advance the program or force a decision; log every call packet
+ response under `fable-calls/` and ledger the verdict. Use
`--source-file` liberally; the collaborator brief
(`COLLABORATOR_BRIEF_2026-07-08.md`) is a ready-made context core -
extend it per call, never send less context than it contains.

## Literature cadence (binding)

At least one literature action EVERY cycle: lean-explore local search
before any new formalization; external search (scholarly MCP
search-arxiv / search-inspirehep + `Scripts/lit/neo4j_paper_search.py
--chunks`) for every new physics claim and every manuscript section you
draft; ingest keepers via `Scripts/lit/lit_ingest.py` (dedup);
one-line log per search in `LIT_SEARCH_LOG.md`.

## Non-negotiables

- Claim before working; verification commands + claim boundary in every
  ledger entry; guard pins for every flagship landing.
- The kill list (RUN_PLAN sec 4) is closed - do not relitigate.
- Spectral rail until K3's probe is green. Anchor rule for every Lean
  name in the manuscript. No blocking waits; no churn past two failed
  attempts - write the failure note and move.
- Aristotle for anything that resists you for 30+ minutes; standalone
  packages when Mathlib-isolable; harvest-first always.
- Report honestly at dawn: attempted / landed / killed / open, no spin.
