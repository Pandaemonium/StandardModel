# Pro feedback integration: tetrahedral scalar-Wilson overlap kernel

Date: 2026-06-28

Source:

```text
C:\Users\Owner\.codex\attachments\1f0c6466-1bf4-439c-b50b-c016eb8ee7fd\pasted-text.txt
```

## Verdict

The tetrahedral scalar-Wilson overlap kernel is now the right first free C1
target:

```text
H_tet(k) =
  gamma5 [
    (i/a) sum_A B_A sin(k_A)
    + (1/a)(r sum_A(1 - cos k_A) - rho)
  ].
```

This is viable if the free lattice is explicitly modeled as:

```text
Lambda_tet ~= Z^4 generated by e_A = a n_A,
dual torus Hom(Lambda_tet, U(1)) ~= (R / 2*pi Z)^4,
T_A -> exp(i k_A).
```

Do not blur this with hypercubic Euclidean momentum.  The `k_A` are character
coordinates on the rank-4 oblique null-edge translation lattice.

## Main doctrine changes

```text
Direct tetrahedral free proof first.
Reference-import homotopy second.
```

Reason:

```text
The free tetrahedral scalar-Wilson theorem is simple enough to prove directly.
A vague similarity to hypercubic Wilson is exactly the hidden assumption we
must avoid.
```

## Euclidean sign-kernel boundary

The overlap kernel proof uses Euclidean/Hilbert gamma matrices:

```text
B_A dagger = B_A;
{gamma5, B_A} = 0;
D_ne^0 dagger = -D_ne^0;
{D_ne^0, gamma5} = 0;
H_tet dagger = H_tet.
```

Lorentzian null slash singularities are not part of the sign-kernel theorem.
They belong to the later path/soldering interpretation.

## Critical correction

Do not say that `H_tet` has a physical zero at the origin.

Correct:

```text
H_tet(0) = -(rho/a) gamma5,
```

so `H_tet` is invertible at the origin.  The physical zero belongs to:

```text
D_ov,tet.
```

## Global gap theorem

The target is:

```text
H_tet(k)^2 >= (c^2/a^2) * 1
```

for all `k` on the rank-4 tetrahedral torus.

The no-zero proof should use:

```text
Q(k) = sum_A B_A sin(k_A);
R(k) = sum_A(1 - cos(k_A));
M(k) = r R(k) - rho;
F(k) = q(k)^2 + M(k)^2.
```

If `F(k)=0`, then `Q(k)=0`, hence every `sin(k_A)=0`, hence `k` is a branch
corner, and C240's scalar Wilson branch window rules out `M(k)=0`.

## M_br policy

Keep:

```text
M_br = 0
```

for the clean tetrahedral free theorem.

Introduce `M_br` only after scalar Wilson fails a proved branch/gap test.  If it
is introduced, it must be:

```text
gamma5-even;
Hermitian;
gauge-covariant;
null-edge point-group constrained;
lowest-degree / lowest-orbit needed to fix the failure.
```

Do not use Standard Model flavor or CKM texture as a branch-lift knob.

## Next Aristotle launch sequence

```text
C244:
  rank-4 tetrahedral lattice/BZ definition.

C247:
  Euclidean Clifford convention and centered kernel theorem.

C243:
  global free gap theorem.

C248:
  corrected overlap/GW algebra.

C249:
  free no-mirror-pole theorem.

C246:
  strategy audit after C240 and Pro feedback.
```

`C245` remains prepared as fallback `M_br` infrastructure but is not part of
the first scalar-Wilson launch unless capacity is intentionally overfilled.
