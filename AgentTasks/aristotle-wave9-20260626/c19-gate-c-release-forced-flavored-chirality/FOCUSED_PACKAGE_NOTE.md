Focused package note:
This Aristotle package is a focused snapshot, not the full Lean project, because the full repository contains a local dependency that Aristotle cannot upload directly. Treat the included files as context. If imports do not build inside this focused package, return a patch/report intended for integration into the full repository.
