# Stage A3f-R3: complete-family bulk-saturation scaling

## Status and claim boundary

**Disposition:** the frozen finite-size scaling gate passes on fresh,
interleaved densities. Both adjacent buffer-rung pairs pass, and the accessible
balanced complete-family route remains open.

**Claim grade:** `M [orig]`, finite stochastic measurement on manifold-generated
flat controls. This is evidence about the complete order-selected candidate
family only. It is not a selected or growing atlas, an operator-locality result,
G2, a tetrad, curvature convergence, stress-energy, or Einstein dynamics.

Provenance incident `INC-2026-07-16-A3F-R3-DUPLICATE-RUN` applies to every
citation of this result until Director review. Two concurrent Codex lanes
blindly launched the same hard-pinned seed and inputs about 13 seconds apart.
The second writer's artifact is retained under the independent provenance
ruling; the first payload was overwritten. No output-derived inspection,
retuning, or code change occurred between launches. This is an archival
protocol failure, not an additional statistical trial or a license to rerun.

## Frozen protocol and provenance

- Random events: `N=(6000,12000)`, each with the standard top endpoint.
- Realizations: five per density.
- Buffer-radius multipliers: `(0.80,1.00,1.25)`.
- Complete count-band candidate family: `[0.90,1.10] n_R`.
- Candidate ceiling: `4000`; no truncation or sampling.
- Peak process working-set ceiling: `6 GiB`.
- Per-realization wall-time ceiling: `600 s`.
- Raw artifact:
  `AgentTasks/causal-atlas-scaling-stage-a3f-r3-2026-07-16.json`.
- Retained second-writer raw SHA-256:
  `d56b92b05100a2db3a12a1df6fa1cc8532ae8454f54a7b5bf61f41935ffeb688`.
- Retained second-writer runtime-normalized scientific SHA-256:
  `4ae695e27f62d9cd1cbdec2418e61e457f6b16c71e85547147deac06b3f35e1d`.
- First-writer reported raw SHA-256:
  `13782c953cef70db0411ce67bff54c64b5e221cb84725abbc522cf1a4c2dfa5b`.
- First-writer reported runtime-normalized scientific SHA-256:
  `0b21886bbdffd131772282bce1e63805585d60043831ceebc8ad44ce155e7e67`.
- Retained run-invariant deterministic-content SHA-256:
  `2bddbd2e26a24598a04252d68b776bd8685a8cfeaca1303e9eb45f27348b8085`.
- Artifact size: `642716` bytes.

The scientific hash recursively removes only fields named `runtime_seconds`,
then hashes compact sorted UTF-8 JSON with no trailing newline. Independent
post-run recomputation matched both hashes.

The deterministic-content hash recursively removes both `runtime_seconds` and
`phase_peak_working_set_bytes`, then applies the same compact sorted UTF-8 JSON
canonicalization. It is the falsifiable run-invariant fingerprint for the
pinned seed and implementation; its independent recomputation matches the
value above. The first process's full stdout was not written to a repository
artifact and is unavailable in this session. Its two printed hash values were
preserved in the incident and transactional mailbox.

## Exact gates before execution

The guarded Lean modules
`AtlasCoreBulkContainment.lean` and `GreedyAtlasCoverage.lean` built
successfully. The Python regression suite passed 60 tests, Ruff was clean, and
scoped pre-commit checks passed. The pre-run skeptic audit approved the exact
count convention, streaming implementation, resource failures, clustered
gates, replay layout, and phase firewall.

An additional seed-free scratch audit covered all 357 distinct transitive
closures generated by naturally labeled five-event DAGs. Streamed unions
matched the inherited protected-core predicate in every case; 868 empty-bulk
cells remained explicitly inadmissible.

## Frozen result

Let `S_N(beta)` be complete-family coverage of the independent order bulk and

```text
D_N(beta) = sqrt(N) * (1 - S_N(beta)).
```

All table entries are realization-clustered medians.

| `N` | `beta` | valid | bulk fraction | flat `F_4` | abs. error | all-event union | `S_N` | `D_N` | `D_N/a_beta` |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 6000 | 0.80 | 5/5 | 0.694884 | 0.683958 | 0.010926 | 0.590235 | 0.848681 | 11.7211 | 0.9121 |
| 6000 | 1.00 | 5/5 | 0.541076 | 0.543743 | 0.002666 | 0.427429 | 0.790308 | 16.2427 | 0.9465 |
| 6000 | 1.25 | 5/5 | 0.372105 | 0.366805 | 0.005299 | 0.254124 | 0.701149 | 23.1489 | 0.8660 |
| 12000 | 0.80 | 5/5 | 0.730106 | 0.727995 | 0.002111 | 0.649863 | 0.894278 | 11.5812 | 0.9013 |
| 12000 | 1.00 | 5/5 | 0.609199 | 0.602869 | 0.006330 | 0.514874 | 0.855699 | 15.8074 | 0.9212 |
| 12000 | 1.25 | 5/5 | 0.445880 | 0.439173 | 0.006707 | 0.346888 | 0.782871 | 23.7853 | 0.8898 |

The relative cross-density drifts in `D_N` are respectively `0.01193`,
`0.02680`, and `0.02676`, all well below the frozen `0.20` ceiling. Bulk
saturation and all-event complete-union coverage increase strictly at every
rung. Every median bulk fraction is within `0.011` of the external flat `F_4`
calibration, below the `0.05` ceiling.

The narrowest high-density rung reaches all-event coverage `0.649863`, above
the frozen capability floor `0.60`.

## Resource and replay audit

- All 10 realizations have status `completed`; none is resource-inconclusive.
- Candidate counts are `111` to `195` at `N=6000` and `1046` to `1714` at
  `N=12000`, below the complete-family ceiling.
- Runtimes are `1.86` to `2.05 s` and `7.61` to `8.30 s` respectively.
- Peak working sets are at most `199.8 MiB` and `408.3 MiB` respectively.
- All 10 seed replay checks pass.
- All candidate-matrix and induced carrier-count checks pass.
- All 30 rungs are admissible.
- All core-carrier and core-bulk containment checks pass.
- All integer and floating factorization checks pass.

The core-carrier check is definitional because each core is formed by
intersecting with its carrier. The independent core-bulk check is the empirical
counterpart of the guarded finite containment theorem. The integer
factorization tripwire reduces to equality of all-event and bulk-union counts
once containment is known; the floating coverage identity is checked
separately.

The flat `F_4` prediction uses the planned random-event denominator `N`, while
the empirical bulk fraction uses the actual `N+1` events including the top
endpoint. This is an `O(1/N)` convention mismatch, far below the frozen gate,
and is recorded rather than retrofitted after output.

## Interpretation and next gate

The fresh data confirm the preregistered finite-size form

```text
S_N(beta) = 1 - a_beta N^(-1/2) + o(N^(-1/2))
```

at the two tested densities in the limited sense encoded by the frozen drift,
monotonicity, and `F_4` gates. The R2-derived design centers were descriptive,
not fit targets; the fresh scaled deficits are roughly `5%` to `13%` below
those centers while remaining stable across density.

This does not evade the fixed-cardinality no-go. It establishes that the
*complete* candidate family increasingly saturates the independently defined
order bulk and has enough all-event capability for a separate growing-atlas
test. The next admissible stage is therefore the already specified
`GRAV-GROWING-ATLAS-001`: preregister `K_N=Omega(N^(1/4))`, a
saturation-aware headroom-capture score, overlap connectivity, resource
ceilings, and fresh development/held-out seeds. Source rows, operators, G2,
tetrad/spin reconstruction, curvature, sources, and Einstein dynamics remain
closed regardless of this pass.
