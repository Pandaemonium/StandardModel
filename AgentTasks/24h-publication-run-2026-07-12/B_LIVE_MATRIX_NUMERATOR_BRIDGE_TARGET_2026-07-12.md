# Live stationary-Weyl matrix-to-numerator bridge target

Date: 2026-07-12
Owner: Codex
Status: strongest typechecking Aristotle skeleton; computational matrix
normalization remains a proof handoff.

## Scope

This target supplies only the missing converse bridge from the live matrix
symbol to the exact tangent-chart numerator system. It does not edit or copy
the walk fixture, prove the generated elimination quotients, prove sextic
positivity, classify chart boundaries, or change a manuscript claim.

Lean target:

```text
AgentTasks/aristotle-targets/codex_24h_b_live_matrix_numerator_bridge.lean
```

Actual import:

```lean
import PhysicsSM.Draft.NullEdge.StationaryAmplitudeWeylAlgebraicOffAxisAlias
```

Thus `weylStep`, `unitPhase`, the six live projectors, `sigmaX/Y/Z`, `walkN`,
and the expanded axis lemmas are used from the current project source. No
standalone or copied walk fixture is present.

## Exact normalization

Let

```text
U = weylStep (unitPhase tx) (unitPhase ty) (unitPhase tz)
D = (1+tx^2)(1+ty^2)(1+tz^2).
```

The coefficient convention is exactly the oracle convention

```text
u0 = trace(U)/2,
wj = trace(sigma_j U)/(2 i),
U  = u0 I + i(wx sigmaX + wy sigmaY + wz sigmaZ).
```

Exact symbolic cancellation gives

```text
3125 D u0  = F0,
15625 D wx = -6 Fx,
3125 D wy  =  6 Fy,
15625 D wz = -6 Fz.
```

`Fx`, `Fy`, and `Fz` are textually the integer polynomials displayed in
`B_STATIONARY_WEYL_TANGENT_ELIMINATION_2026-07-12.md` and generated by
`Scripts/oracle/certify_stationary_weyl_tangent_elimination.py`. The signs are
important: the analyzer's primitive normalization negates the memo's `Fx` and
`Fz`, which is why the cleared coefficient formulas carry `-6` there.

For real finite tangent coordinates, every factor `1+t^2` is positive. Hence
`D > 0`; no extra denominator hypothesis is needed for the basic bridge.

## Theorem surface

The target separates four layers:

1. `matrix_eq_one_iff_coefficients` proves that scalar and Pauli traces are
   exact coordinates on complex `2 x 2` matrices.
2. `live_coefficients_cleared` is the sole large live-matrix normalization.
3. `live_eq_one_iff_numerators` states the strongest exact result:

```text
U = I iff F0 = 3125 D and Fx = 0 and Fy = 0 and Fz = 0.
```

4. `live_eq_one_implies_numerators_zero` is the precise converse input needed
   by elimination.

`live_coefficients_cleared` is the sole declaration with an executable proof
handoff marker. The Pauli-basis extraction, denominator positivity and
cancellation, exact iff, converse bridge, and corrected-certificate
compositions are proved and checked by Lean.

## Nondegenerate composition

`NondegenerateTangent tx ty tz` means

```text
tx != 0 and ty != 0 and tz != 0.
```

The corrected certificate has the mandatory pointwise shape

```text
(1+tz^2)^2 tz rootPoly(tz) excludedPoly(tz)
  = Qx Fx + Qy Fy + Qz Fz.
```

`corrected_eliminant_of_live_alias` composes this identity with the live bridge
to obtain

```text
tz = 0 or rootPoly(tz) = 0 or excludedPoly(tz) = 0.
```

The proof cancels `(1+tz^2)^2` only after using real positivity. It never states
the false bare polynomial ideal-membership identity. In the nondegenerate
scope, `corrected_eliminant_of_nondegenerate_live_alias` removes `tz = 0` and
returns the exact quintic-or-sextic alternative. A later theorem may remove the
sextic branch only after a kernel-checked real nonvanishing certificate.

The live bridge is integrated as
`PhysicsSM/Draft/NullEdge/StationaryAmplitudeWeylLiveMatrixNumeratorBridge.lean`.
Its exact coefficient theorem and all downstream matrix/numerator equivalences
build against the imported live `weylStep` definition. The separate generated
522-term certificate remains guarded on demand; a final capstone may instantiate
`qx`, `qy`, and `qz` with those exact quotient values.

## Provenance

- Live definitions:
  `PhysicsSM/Draft/NullEdge/StationaryAmplitudeWeylTangent.lean` and
  `PhysicsSM/Draft/NullEdge/StationaryAmplitudeWeylAlgebraicOffAxisAlias.lean`.
- Exact matrix and Pauli extraction:
  `Scripts/oracle/analyze_stationary_amplitude_weyl.py`.
- Exact polynomial normalization and corrected ideal certificate:
  `Scripts/oracle/certify_stationary_weyl_tangent_elimination.py`.
- Omitted-chart audit, kept outside this finite-chart bridge:
  `Scripts/oracle/classify_stationary_amplitude_weyl_tangent_boundaries.py`.
- CAS environment recorded by the source sidecars: Python 3.12.10, SymPy
  1.14.0, exact rational arithmetic.

The scripts are external oracles, not kernel proofs. Aristotle must preserve
the imported live definitions, coefficient convention, constants, signs,
chart factors, theorem statements, and nondegenerate scope.

```yaml
aristotle:
  project_id: a0cfd33b-01b1-41fc-9033-163ca37688b3
  task_id: 18bb8c1f-4943-4b0c-9581-a4b7d74da654
  target_file: PhysicsSM/Draft/NullEdge/StationaryAmplitudeWeylLiveMatrixNumeratorBridge.lean
  expected_module: PhysicsSM.Draft.NullEdge.StationaryAmplitudeWeylLiveMatrixNumeratorBridge
  submission_project: AgentTasks/aristotle-submit/codex-24h-b-live-matrix-numerator-bridge-20260712-project
  output_dir: AgentTasks/aristotle-output/a0cfd33b-01b1-41fc-9033-163ca37688b3
  status: integrated
  run: 24h-publication-run-2026-07-12
  owner: Codex
```

Verification: returned statement unchanged; direct Lean PASS; targeted build
PASS (8,032 jobs). Aristotle reported only `propext`, `Classical.choice`, and
`Quot.sound`; the aggregate guard pins the two headline bridge theorems.
