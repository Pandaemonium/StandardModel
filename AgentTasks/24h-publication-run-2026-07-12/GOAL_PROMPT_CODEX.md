# GOAL PROMPT - Codex, 24-hour publication run 2026-07-11 to 2026-07-12

You are Codex, co-equal executor with Claude/Fable. Execute
`AgentTasks/24h-publication-run-2026-07-12/RUN_PLAN.md` continuously until
09:45 PDT on July 12, 2026. Coordinate only through the append-only
`LEDGER.md` in that directory. `AGENTS.md` and all repository convention,
trust, provenance, and build rules remain binding.

## Startup

Read, in order:

1. the new `RUN_PLAN.md`;
2. the prior run's `MORNING_REPORT.md`, `HONEST_SCORECARD.md`, and
   `HELP_NEEDED_2026-07-11.md`;
3. `Sources/Null_Edge_Publication_Portfolio_2026-07-10.md`;
4. exact source and guards for the lane you claim.

Immediately run `aristotle list --limit 40`. Harvest inherited jobs before
submitting replacements, especially `cb16b747` and `573430f4`. Record every
claim and verdict in the new ledger.

## Mission

Your default technical lanes are Papers D, F, and B:

1. compose the refined `3+1` bound with an explicit growing window,
   sampling/interpolation, Sobolev tail, and Dirac PDE multiplier;
2. package Paper F's negative classification and extend the finite Ward
   quotient toward the full decorated carrier, returning either a physical
   selector or a sharp residual-moduli theorem;
3. bridge strict complex finite-range walks to Laurent matrices and the
   one-particle GNVW exponent at an explicit Fourier convention;
4. integrate and independently audit Fable's Paper C/E results;
5. own Lean guards, claim matrices, simulations, and the final reproducibility
   artifact.
6. co-own the Jordan-Clifford theorem lane: prepare and submit the exterior
   module, five-mode generation, and representation-level `Z6` targets in
   `JORDAN_CLIFFORD_BRIDGE_PROGRAM.md`, then integrate only semantically aligned
   results.

Paper A is allowed to freeze as a superb specialist paper. Do not hold it
hostage to prestige upgrades. Update its formal claims when new theorems land,
but never turn a planned composition into a stated result.

The Furey-Baez manuscript is an independent lane, but no longer merely an
editorial audit. Read `JORDAN_CLIFFORD_BRIDGE_PROGRAM.md`. Test whether the
nested Jordan flag canonically supplies the weak/color spaces, Furey exterior
module, complete generation, and exact fermion-action kernel. Treat the reported
Baez-Schwahn theorem as external until primary-source verification and a local
formalization. Audit exact Lean anchors and octonion conventions, and never use
null-edge results as evidence for this paper.

## Aristotle

Keep about 5-7 useful `codex-24h-` jobs running when capacity permits:

- 3-4 proofs/compositions;
- 1 grand or focused strategy job;
- 1 adversarial audit.

Submit a full-project grand-strategy job at startup and every 90 minutes. Keep
at least one audit job active across the combined fleet. Apply the two-hour
stall rule. A local proof-complete result can supersede a remote build, but its
provenance must say so.

Every proof target must already typecheck, preserve its statement, contain a
nonzero witness and boundary control, prohibit weakening, and name its
manuscript consequence. Use a context pack for nontrivial project targets.

## Literature and Lean references

Run or delegate a literature/public-package pass at least every 30 minutes.
Use Spark when available; search directly if Spark is unavailable. Read primary
full text before relying on theorem content. Consult Mathlib and PhysLean before
inventing physics APIs. Log exact sources, modules, versions, licenses, and
convention gaps.

## Landing rule

A theorem is landed only after:

- semantic and convention audit;
- direct Lean verification;
- witness and negative control;
- trust/axiom scan and guard pin;
- root import where appropriate;
- claim matrix, manuscript, provenance, and ledger update;
- `git diff --check` and pre-commit.

Reject vacuous, hollow, false-shape, or prose-inflated results even when the
kernel accepts them.

## Final phase

At 08:00 PDT stop broad construction and become an adversarial referee.
Harvest only, rebuild all new modules and guards, inspect every headline
declaration, rerun deterministic fixtures, verify manuscript anchors, and
produce:

- `HONEST_SCORECARD.md`;
- `FINAL_REPORT.md`;
- two identical final publication-verifier runs;
- an explicit list of release decisions that still require the user.

The one rule that matters most: ambition is measured by the strength of the
result and the clarity of its failure conditions, never by how much of a
completed theory the prose appears to promise.
