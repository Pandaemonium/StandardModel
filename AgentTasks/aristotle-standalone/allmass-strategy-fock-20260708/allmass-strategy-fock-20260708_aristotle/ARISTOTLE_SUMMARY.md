# Summary of changes for run 15f3a6a2-a502-4308-b3ce-5e57c2a109c6
Delivered the finite second-quantized mass gap as a landed, kernel-checked Lean theorem (a proof, not just an essay). Also fixed a pre-existing build break: `src/FockGradedRadical.lean` imported the non-resolving prefix `PhysicsSM.Draft.NullEdge.Carrier.FockQuotientPairing`; corrected to the actual module name `src.FockQuotientPairing`, so the whole project now builds.

New module `src/FockMassGap.lean` (no `sorry`; `secondQuantized_massGap` depends only on `propext, Classical.choice, Quot.sound`).

1) Finite 2nd-quantized types. The one-particle sector is given by its Hermitian spectrum `d : Fin N → ℝ` (diagonal one-particle basis, WLOG for Hermitian `B`). The fermionic Fock space `⊕_k Λ^k(sector)` is modeled by its occupation basis `FockState N := Fin N → Bool` (bit i = mode i occupied; the k-particle summand `Λ^k` = states with k set bits). The free many-body Hamiltonian `dΓ(B)` acts diagonally: `fockEnergy d occ = ∑ i, if occ i then d i else 0` (sum of occupied one-particle eigenvalues). This is the concrete finite shadow of the `ExteriorAlgebra`/`⨁ n, ⋀[K]^n W` construction already used in `FockQuotientPairing`/`FockGradedRadical`.

2) Sharpest true statement (delivered as Lean). `ground_isLeast`: with `0 ≤ d i`, the ground energy is `0`, attained by the vacuum, and is the least of the whole many-body spectrum. `excited_isLeast`: if the gap `lam - kappa ≥ 0` bounds `d` from below and is attained at mode `i₀`, the least excited energy is exactly `lam - kappa` (one particle in the ground mode). Flagship `secondQuantized_massGap`: for `kappa < lam`, `∀ i, lam - kappa ≤ d i`, and `d i₀ = lam - kappa`, the gap `massGap d = firstExcitedEnergy d - groundEnergy d = lam - kappa`. So the free second-quantized gap equals the one-particle gap — a genuine "first excited minus ground", not a definitional artefact.

3) Free vs interacting boundary. `fockEnergy_twoParticle` proves the free two-body energy is exactly `d i + d j` (sum of constituents) — so no below-threshold bound state exists for the free `dΓ(B)`. `twoBody_bound_below_threshold` shows the binding defect `Delta = -kappa` added to that threshold drops the two-body energy strictly below the sum of constituents exactly when `kappa > 0`: the honest finite seed for "hadron mass ≠ sum of constituents". This is a seed, not a hadron, because `Delta` is inserted by hand rather than derived as the least eigenvalue of an interacting two-body operator on `Λ²(sector)`.

4) Feasibility, ranked sub-lemmas, and blocker (documented in the module footer). Done: vacuum/single-particle energies, `Finset.sum` bounds, both `IsLeast` results, the flagship gap, the two-body free energy, and the mass-defect seed. Open/hard: (6) build the interacting two-body form `H₂ = dΓ(B)|_{Λ²} + V` with `V` seeded by `Delta = -kappa`; (7) prove its least eigenvalue lies strictly below the free threshold `min_{i≠j}(d i + d j)`; (8) identify it as a hadron mass. Blocker: a finite variational/min-max spectral estimate ("least eigenvalue of the compressed interacting form is below the sum of one-particle eigenvalues") that is not packaged in Mathlib and must be built; the free gap is earned, the interacting hadron mass is not.

All work committed and pushed to `main`.
