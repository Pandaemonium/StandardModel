# Summary of changes for run 0a8ee0ea-b1fb-4e2e-80e0-41e6f5d6a8fb
Completed the adversarial over-claim audit of the four carrier dynamics/spectral flagship modules (no proofs required; kernel-correctness assumed). Full per-theorem table with class + verdict + remedies is in `AUDIT_REPORT.md`. Highlights answering your three sharp probes:

**1. CarrierUnitaryFlow "energy conservation" — is it a genuine dynamical fact or a commuting-observable triviality?** It is the commuting-observable triviality, and the Lean statement admits it. `carrier_orbit_energy_conserved` quantifies over an *arbitrary* observable `E` under the explicit hypothesis `CommutesWithStep (carrierFlowStep …) E`, concluding `⟨Uⁿψ,EUⁿψ⟩ = ⟨ψ,Eψ⟩` — the standard `[U,E]=0 ⇒` invariance of a unitary flow. `E` is never instantiated at the generator `H=B`; even that instance would be the `[H,e^{-itH}]=0` tautology. So it is genuine linear algebra, not a nontrivial constant of motion, and "energy"/"observableEnergy" is decorative. Because the honesty is carried by a visible hypothesis (not hidden) and the module docstring adds grade caveats, this is **MINOR** (rename/annotate), *not* the load-bearing sin. The flow's unitarity is the real `∈ Matrix.unitaryGroup` / `LinearIsometryEquiv` fact (not a weaker surrogate), and generator-as-Hamiltonian is flagged grade-C.

**2. MassGapWitness carrier tie at (2,1) — docstring-canonicity over-claim?** No. `B_spectrum` is the genuine set-theoretic ℝ-spectrum `{λ−κ,λ,λ+κ}` from the characteristic determinant (no hollow encoding; multiplicity correctly absent from a `Set`, carried separately by `B_least_eigenvalue`). `M6_topBlock_eq_B`/`M6_botBlock_eq_B`/`M6_offBlock_eq_zero` are exact identities, and the docstrings explicitly present `(2,1)` as a *chosen* fixed point (kernel-checked there, oracle-grade elsewhere) and disclaim canonicity. **CLEAN.**

**3. MassSpacingPrediction "equal spacing" — tautology of the {λ−κ,λ,λ+κ} ansatz?** Yes. `spec_equal_spacing`/`spec_spacing_ratio`(=1)/scale-invariance are proved by `ring` directly on the definitions, never touching `B`; the genuine content lives only in `levels_eq_spectrum` (tie to `B_spectrum`), i.e. "B's spectrum is an arithmetic progression." But it is honestly scoped (kill condition + explicit "not the neutrino ratio / honest boundary"), so **CLEAN with an optional MINOR** (restate the ratio as a corollary of `B_spectrum`, soften "prediction").

**Single most load-bearing finding:** `SectorGroundMassWitness.sector_krein_form_eq_one`'s docstring claim that it is "the resolution of 'Krein-unitary ≠ norm-unitary' for CarrierUnitaryFlow, kernel-certified." The theorem proves only the *static* metric identity `Pisoᴴ·Jmet·Piso = 1` (Krein = Euclidean *on* the J-positive sector). Advertising it as *resolving* the Euclidean-vs-physical(Krein)-evolution equivalence outruns the kernel: that equivalence is a *dynamical* claim requiring the Krein flow to leave the sector invariant, which is proved nowhere in these files and is openly conceded missing by CarrierUnitaryFlow caveat (0). This is the pivotal joint connecting the trivial Euclidean conservation to the claimed physical conservation. Remedy (docstring-only, no statement changes): restate it as "the static identity J|_sector = Euclidean; does not show the Krein flow preserves the sector (open), hence does not by itself identify the Euclidean flow with the physical Krein evolution." Closing the gap requires proving J-unitarity of exp(-itHAC) plus invariance of range(Piso).

Also verified positively: the lam=0 massless subtlety is handled honestly; T2_positive_mass rests on an exact Gram certificate (M6 = 1 + BwitᴴBwit), is not circular, and its axioms are the allowed set; unitarity is the genuine strong form.
