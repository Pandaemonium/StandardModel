# P1/P2 bridge suite (no-double-counting bridge + walk coherence)

Complete every `sorry` in `P1Bridge/Bridge.lean`. Do not change any
statement. All targets are exact finite 2x2 complex linear algebra and real
trigonometry; the module docstring contains hand-verified proof notes for
every target, including the two nontrivial ones (the walk eigenvector
equation and the coherence identity). Deliver kernel-checked proofs, no
`sorry`, no `native_decide`, axiom footprint
`[propext, Classical.choice, Quot.sound]`. If any statement appears false,
stop and report it instead of altering it.
