# Summary of changes for run 16e480c6-6b40-45fa-8e85-29af5d61a1ca
Completed the adversarial over-claim audit (no proofs written; no source files modified). Full per-file per-theorem tables and reasoning are in `AUDIT_REPORT.md`. All matrix/spectral claims were re-derived by hand (including deriving `dGamma2` column-by-column from the wedge-basis second-quantization rule, and checking every eigenvalue, discriminant, unitary conjugation, and boundary condition).

Snapshot facts I verified: `src/InteractingTwoBody.lean` imports only Mathlib, builds here, and is axiom-pinned to the standard axioms; no `sorry`/`admit`/`axiom`/`implemented_by` anywhere. `DerivedInteraction.lean` and `CarrierGradedBudget.lean` reference original-project modules (`…Carrier.InteractingTwoBody`, `…Carrier.EquivariantGradedIndex`, `…Carrier.CarrierKreinSquare`) that are NOT present anywhere in this snapshot, so they are not rebuilt in this sandbox — I audited them mathematically instead.

Per-file verdicts:
- InteractingTwoBody.lean — all 10 theorems CLEAN. The file honestly self-grades (M earned = the below-threshold eigenvalue is a real computation; C disclaimed = the rank/form of V is inserted). One MINOR wording nit: `interaction` is called "rank-one" but its off-diagonal block has rank 2.
- DerivedInteraction.lean — every theorem is a correct matrix fact (CLEAN statements); `dGamma2` IS the honest antisymmetric second quantization, `Vderived_conj` is a genuine unitary equivalence (real work, not the literal same matrix), and `derived_wrongPlane_no_binding` is a genuine geometric no-go. But the docstring framing around `derived_boundState_below_threshold` is a LOAD-BEARING over-claim.
- CarrierGradedBudget.lean — `carrier_graded_budget` is a thin trace-linearity wrapper; its real content (the operator identity and its hypotheses) lives entirely in `carrier_krein_square`, which is ABSENT from the snapshot and cannot be certified here. The file is honest that it is a wrapper, so it is not itself an over-claim, but the audit chain has an unavailable link.

THE single most load-bearing over-claim: DerivedInteraction.lean's "C→M / first-principles hadron / V is exactly the second-quantized closure operator OF THE CARRIER". The kernel proves only that `dΓ` of a hand-CHOSEN curvature (the {1,2} excited-mode generator `closureCurvature`) reproduces the modelled V up to a phase gauge, hence binds. The identification of that curvature with the carrier's actual K is only a docstring assertion — the carrier module isn't even imported — and the file's own `derived_wrongPlane_no_binding` proves this choice is exactly what decides binding. So the result is really conditional ("IF the closure acts among the excited modes THEN binding"), not an unconditional first-principles derivation. Remedy (either): (1) import the real carrier and prove which plane its K occupies, closing the gap; or (2) downgrade the docstrings to the conditional statement and drop "first-principles"/"exactly the closure operator of the carrier".
