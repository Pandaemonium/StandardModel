# Summary of changes for run 05d587a8-078e-40cf-9223-c8540b76ced9
Completed the requested adversarial over-claim audit (no proofs required). Read all four `src/` files line-by-line, checked every theorem's *statement* (not just kernel-validity) against the four failure modes, and confirmed the decisive structural fact by grepping the source. Full per-theorem table + findings are in `AUDIT_REPORT.md`. Note: the external `PhysicsSM.*` imports (`Spinor.PluckerMass`, `Carrier.FiniteUnitaryEvolution`) are not vendored in this repo, so I audited source-as-landed and local proofs and took the axiom-pins at face value; claims resting on those imports are flagged as trusted-not-re-verified.

Headline: the kernel content is uniformly true and (with hypotheses satisfiable, so non-vacuous) genuine. Every over-claim lives in docstrings/theorem framing, not in a false or vacuous kernel statement. Two are load-bearing.

THE single most load-bearing over-claim — `CarrierUnitaryFlow.lean`'s "D2 fires on the actual carrier / conservation along the real carrier orbit is kernel-checked". What is actually proved is that `exp(-it·B(λ,κ))` is *Euclidean*-unitary on the positive-definite `EuclideanSpace ℂ (Fin 3)`, with the ONLY carrier-specific input being `B_isHermitian`. It is not the carrier's Krein evolution, for three independent reasons: (1) Krein (indefinite J) vs Euclidean inner product — the file never restricts to the J-positive sector where they coincide, and Krein evolution generically mixes the ± sectors; (2) it uses a single 3×3 half-block B(λ,κ) on Fin 3 at arbitrary couplings, not the actual 6-dim J-positive sector M6 = B(2,1)⊕B(2,-1) on Fin 6 — and MassGapWitness's own disclaimer restricts the carrier tie to (2,1); (3) B/M6 is the D#D mass-squared form, never shown to generate transport/time evolution — exp(-it·(mass form)) is unitary for any Hermitian matrix. Remedy: regrade the docstrings/names to "exp(-itH) is Euclidean-unitary for any Hermitian H, instantiated at the mass block"; to earn the physical claim, add the flow on the actual Fin-6 sector M6, a lemma that the Krein evolution preserves the J-positive sector, and an identification of the D2/transport generator with M6.

Strongest secondary (also load-bearing) — `SectorGroundMassWitness.lean`'s "explicit two-edge Cl(4) carrier". `HAC`, `Jmet`, `Piso`, `M6`, `Bwit` are hand-typed literal matrices; the whole Clifford derivation (gammas, Kronecker/tensor products, Js, J=Js⊗I3, Q_A, Q_C, HAC=J(Q_A+Q_C)) exists only in docstrings — confirmed no such token appears in any kernel declaration. What is verified is internal to the hand-typed matrices (HAC_isHermitian, Piso_isometry, compression_eq, M6_posDef); this correctly answers the "positivity built-in by 1+BᴴB?" probe (compression_eq ties M6 to HAC, so PosDef is a proved property, not baked in). The gap is provenance: nothing certifies HAC/Jmet equal the Clifford assembly, so "T2 proves a Cl(4) carrier has positive mass" rests on an unverified hand-transcription. Remedy: prove HAC = (Js⊗I3)·(Q_A+Q_C) in-kernel, or regrade the docstring.

Everything else is CLEAN or MINOR (self-disclosed). Notably MINOR: `MassGapWitness` whole-plane spectral theory is honestly quarantined from the carrier tie; `gauge_covariance` is generic conjugation algebra over-narrated as "gauge covariance of the four blocks" (no block/D/Clifford appears in the statement); `free_mass_operator_eq_plucker`'s only physical content is the imported Plücker identity (the 2×2 "mass operator is scalar" is automatic). Exemplary honesty: `B_massless_iff` correctly includes the λ=0 disjunct, and `B_least_eigenvalue` flags its unused hypothesis. Report committed and pushed as AUDIT_REPORT.md.
