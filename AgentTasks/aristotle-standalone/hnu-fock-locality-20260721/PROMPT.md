# Proof job: locality of the finite fermionic second quantization

Work in Lean 4.28 with Mathlib. Formalize the reusable finite theorem that a
local one-particle unitary induces a locality-preserving free fermionic update.

Let finite modes carry a declared neighborhood relation `Near i j`. Let a
one-particle matrix `U` satisfy `U i j = 0` whenever `Near i j` is false, and
let `Gamma(U)` be the determinant-minor second quantization on finite fermionic
Fock space.

Required outputs:

1. Prove the conjugation law for creation and annihilation operators:
   `Gamma(U) a_i^* Gamma(U)^* = sum_j U j i a_j^*`, with the corresponding
   annihilation formula and conventions stated exactly.
2. Deduce that the image of the local CAR algebra at `i` is contained in the
   algebra generated by modes in its declared neighborhood.
3. Prove composition enlarges support only by relational composition of
   neighborhoods, giving a finite-depth light cone.
4. Supply a nonidentity sparse unitary control and a wrong-neighborhood
   counterexample showing the locality hypothesis is load-bearing.
5. If the full algebra-containment packaging blocks, land the operator
   conjugation law and an exact support theorem for monomials.

This is the missing free-Fock locality rung for the HNU `3+1` schedule. It must
not infer locality merely from one-particle unitarity or antisymmetry. Keep
fermionic ordering signs explicit. Prefer a self-contained Mathlib-only model,
but a clean theorem design mapping to the repository's
`FiniteCARSecondQuantization` is useful.

Return all source, exact convention notes, and a completion report. No hidden
assumptions, proof placeholders, compiler-trusted finite evaluation, or
weakening to a tautological support predicate.

Semantic context:
`AgentTasks/context-packs/hnu-fock-locality-20260721-20260721-002814.md`.
