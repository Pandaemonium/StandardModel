# Summary of changes for run 58018684-826a-4023-bd66-e38ef8a76c82
I delivered both the requested strategy/no-go analysis and the bonus kernel lemma.

**Deliverables**
- `NEUTRINO_RATIO_STRATEGY.md` — the full written verdict (all four requested items).
- `RequestProject/Main.lean` (namespace `NeutrinoRatio`) — Mathlib-only Lean formalization of the spectrum `{lam−kappa, lam, lam+kappa}` and the claims below. Builds with no `sorry`; `spec_spacing_ratio` uses only `propext`, `Classical.choice`, `Quot.sound`.

**1. Mass-value-map definition.** A map `M : couplings → carrier decorations → particle label → ℝ` (mass or ratio). The complete finite data of one carrier `B(lam,kappa)` is: the two reals `(lam,kappa)` plus a three-valued *sector level index* (formalized as `spectrum : Fin 3 → ℝ`). Candidate discrete invariants (edge count, Clifford dimension, sector multiplicity, graded index) are all per-carrier scalars — none is a family/replication label, so generation structure is simply absent from the current data.

**2. Verdict: (c) category error at this generality, with a (b) input-only fallback.** The theory fixes ratios only of the form `(lam−kappa):lam:(lam+kappa)` within one carrier and only up to overall scale. Two kernel facts force this: `specMid_surjective` (no absolute value is fixed — the central level can be any real) and `spectrum_scale_underdetermined` (any second carrier can reproduce the whole spectrum scaled by an arbitrary factor, so no cross-block scale exists). A neutrino `m₂/m₃` needs three distinct carriers + a scale between them; supplying that scale is external Yukawa-like input (b), not a derivation. The shortcut of identifying the three levels of one block with three generations fails twice: those levels are one sector's closure/mean/aperture (not a replication), and even if forced they predict an *arithmetic* Δm² ratio of 1, contradicting the observed hierarchical neutrino splittings.

**3. Honest positive (kernel lemma delivered).** Within one carrier the three squared-mass levels are an arithmetic progression, giving one sharp dimensionless prediction: `spec_spacing_ratio` — for `kappa ≠ 0`, `(m²_mid−m²_lo)/(m²_hi−m²_mid) = 1` exactly, and `spec_spacing_ratio_scale_invariant` shows this is independent of the unknown overall scale. Supporting lemmas: `specMid_eq_mean`, `spec_equal_spacing`. Kill condition: if the two within-sector squared-mass gaps of a single carrier measure to a ratio ≠ 1, that carrier's structural model is falsified. This contrasts with the neutrino ratio, which is cross-generation and outside what the data fixes.

**4. Missing ingredient (no-go).** The neutrino ratio is not predictable. Exactly two things are missing: (i) a generation/family-replication index selecting three distinct carriers as the mass eigenstates, and (ii) a cross-carrier scale map (a Yukawa-like texture) relating distinct carriers' normalizations — undetermined by the structural data. Until both are supplied AND derived, the neutrino ratio is an honest boundary of the program, not a pending calculation.
