# Summary of changes for run 51462257-2f1b-402c-9daf-115b16332643
Completed the adversarial over-claim audit (no proof produced/required). Full report written to `AUDIT_REPORT.md`. Two vacuity claims were mechanically confirmed by elaborating them in Lean.

PER-THEOREM VERDICTS
- `compression_balanced` — CLEAN. Not vacuous: the anticonjugation `d J d = -J` forces `J` to be block-off-diagonal w.r.t. the ±grading (`J_ij=0` on same-sign blocks), a genuinely nonempty class (the nonzero 6×6 witness is a member). `Q_G`-irrelevance is real — `Q_G` never appears and `r` is arbitrary.
- `compression_balanced_eigbasis` — CLEAN (minor caveat). Genuine strengthening, NOT coordinate-restricted: the proof uses only the intertwining `d·P = P·e` and Hermiticity, never assumes `P` is a submatrix or even that `Pᴴ P = 1`. Sign/transpose chain (`hLeft`, `diagonal_pm1_conjTranspose`, `hAnti`) checked — no slip. Caveat: it imposes no rank/complement condition on `P`, so it says nothing about *which* space is compressed; that burden falls entirely on the existence lemma.
- `compression_has_neg_eigenvalue` — CLEAN. Not circular (no-neg ⇒ by balance no-pos ⇒ all-zero ⇒ det=0, contradicting IsUnit).
- Balance engine (`anticonj_charpoly_eq`, `hermitian_balanced_count_of_neg_charpoly`) — CLEAN.
- `witness_balanced_via_general` — CLEAN. Genuine instance of `compression_balanced`; `bg_eq_diagonal` is an honest identity (σz⊗1 = diagonal dWitness), not a hand re-derivation, and it feeds the witness's own `bg_anticonj`, never the specific `Q_G`.
- `physical_sector_b_eigenbasis_exists` — LOAD-BEARING. Vacuous as stated: provable with `κ = Empty` using none of `hQGherm/hQGnil/hbQG/hd` (verified in Lean), so the `sorry` is unnecessary for the stated goal. It also only pins columns to `ker Q_G` (`QG·P=0`), NOT to a complement of `range Q_G` there, nor `card κ = dim(V'/N)` — strictly weaker than its docstring.
- `physical_sector_balanced` — LOAD-BEARING. Vacuous as stated, independent of the `sorry`: `κ = Empty` gives `0 = 0` (verified in Lean). The glue is sound (it does not smuggle the conclusion), but the inherited `P` is under-specified.

SHARP RULING (ker Q_G vs V'/N): "balanced on ker Q_G" and "balanced on V'/N = ker Q_G/range Q_G" are DISTINCT statements. Hermitian inertia is not additive across a subspace quotient in general; they coincide only because range Q_G is b-invariant (b commutes with Q_G) and hence itself balanced in ± pairs. So the result is mathematically TRUE, but the Lean nowhere establishes the descent (range Q_G in the M-radical / its balanced pairing), never quotients, and never forces span P = ker Q_G. The identification is asserted in prose, not proved in the kernel. Therefore the manuscript's "the crux closes to general M once existence is transcribed" is OVER-CLAIMED on two counts: (1) as written both statements are vacuous (empty κ), so filling the sorry is neither necessary nor sufficient; (2) the honest remaining gap is the STRONGER "existence of a correctly-dimensioned orthonormal b-eigenbasis of a complement of range Q_G in ker Q_G (i.e. of V'/N)" plus the ker→quotient descent — not the weaker ker-only existence the statement encodes.

SINGLE MOST LOAD-BEARING FINDING: `physical_sector_balanced` (the grade-M "prize") is vacuous as stated — its existential conclusion is satisfied by κ = Empty giving 0 = 0, independent of the sorry — and even after an honest sorry-fill it would certify balance only on a b-eigen-subspace of ker Q_G (P is pinned to ker Q_G, not to dim V'/N nor to a complement of range Q_G), not on the physical quotient V'/N. Remedy: pin `card κ = finrank(ker Q_G) − finrank(range Q_G)` and add complementarity (`range P ⊕ range Q_G = ker Q_G`, or `Pᴴ·QG=0` + rank), and either restate on V'/N directly or add the descent fact identifying ker-inertia with quotient-inertia. This is a statement/docstring defect, not a false theorem.
