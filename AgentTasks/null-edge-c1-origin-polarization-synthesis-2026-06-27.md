# Gate C1 origin-polarization synthesis

Date: 2026-06-27

Purpose: fold the latest Pro-style C1 analysis into a durable working note for
the next autonomous-loop rounds.

## Verdict

Gate C1 remains open. The current blocker is not scalar regulator health. It is
origin polarization plus release audit:

```text
Find or refute a native, gauge-safe way to select one Weyl line from the
balanced origin kernel, then prove true bad-sector inverse gaps, ghost safety,
Krein health, anomaly accounting, locality, and regulator stability.
```

## Decisive finite theorem

Let:

```text
K0 = ker D_+(0)
Gamma0 = origin chirality on K0
J = balance symmetry with J Gamma0 J^-1 = -Gamma0
```

Then:

```text
If P0 commutes with J, then Tr(Gamma0 P0) = 0.
```

Therefore:

```text
A C1 origin projector with nonzero target chiral index must fail to commute
with the balance symmetry J.
```

This is the finite theorem to prove first because it decides what any native
`T_br` or non-scalar Wilson term must do at the origin.

## Native decision fork

Escape outcome:

```text
Find T0 in the native gauge-safe origin algebra such that:
  T0^2 = 1,
  T0^dagger = T0,
  [T0, gauge] = 0,
  Tr(Gamma0 (1 + T0)/2) != 0.
```

Then extend:

```text
T0 -> T_br(q,U)
```

and prove branch-germ separation, gauge safety, locality or controlled
quasi-locality, true bad-sector inverse gap, no gauge-coupled propagator-zero
substitution, Krein-positive physical residue, anomaly accounting, and
regulator stability.

No-go outcome:

```text
Every native gauge-safe origin endomorphism commutes with J.
```

Then native finite/local `T_br` and non-scalar Wilson C1 release are impossible
under those assumptions. The remaining live routes are certified projected
overlap or a null-edge-native domain-wall/bulk-boundary construction.

## C106 stack

```text
C106a_OriginPolarizationEscapeHatch:
  balanced-commutant zero-index theorem.

C106b_NativeOriginCommutantNoGo:
  if native origin algebra lies in the balance commutant, no native C1.

C106c_OriginPolarizerCertificate:
  define T0/P0/nonzero-index/allowed-bad-mass data.

C106d_BranchClassifierExtensionContract:
  analytic/local/gauge-safe extension T0 -> T_br(q,U).

C106e_BadSectorMassGap:
  non-scalar bad-sector mass gives true inverse-propagator gap.

C106f_GhostZeroNoRelease:
  propagator-zero mirror removal is not C1 removal.

C106g_LocalityCertificateSoundness:
  formal projector does not imply locality.

C106h_NativeC1ReleaseAssembly:
  conditional native C1 from the complete certificate chain.
```

## Route ranking after this analysis

```text
1. Projected overlap after certified branch split.
2. Domain-wall/boundary if there is a null-edge-native bulk/boundary map.
3. Native T_br: highest payoff, high no-go risk.
4. Controlled quasi-local projector as a certificate layer.
5. Non-scalar Wilson only when generated by native T0.
```

## Hard guardrails

Do not claim:

```text
C0 scalar Wilson species health implies C1.
Bare D_+ releases a physical chiral operator.
Internal chirality fixes the balanced external origin.
Route labels, taste labels, or formal projectors select a physical Weyl branch.
A projector is local without a finite-range, decay, or analytic-functional
calculus certificate.
Residue positivity implies ghost safety.
A propagator zero is an acceptable mirror mass.
```

## Immediate autonomous-loop action

Unless a completed Aristotle result is ready to integrate first, submit
C106a_OriginPolarizationEscapeHatch as the next independent Aristotle job and
send Claude an adversarial review packet on the C106a statement/strategy in the
same cycle.
