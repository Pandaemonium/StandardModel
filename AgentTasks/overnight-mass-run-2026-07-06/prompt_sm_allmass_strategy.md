Mission-focused grand-strategy job (NO Lean proving, NO lake build). Deliver a
decisive, ranked strategy for getting AS CLOSE AS POSSIBLE to "explaining all of
mass using null-edge theory" at kernel-checked finite-theorem grade, given the
CURRENT state below. This is forward-looking and mission-scoped - NOT a repeat of
the two whole-project audits (which already graded the whole tree).

## Current state (ground truth)

The null-edge mass thesis (read
`AgentTasks/fourday-ym-run-2026-07-05/NULL_EDGE_MASS_UNIFICATION.md`): mass is a
RELATIONAL OBSTRUCTION to free null transport, in three modes - T (turn/matter),
C (closure/gauge), A (aperture/composite) - kept distinct per F-YM-CONFLATE.

PROVED and bundled this run in
`PhysicsSM/Draft/NullEdge/GateI1/AllMassFromNullEdges.lean` (`allMassFromNullEdges`,
standard axioms, build-enforced guard): (A) `compositeMassSq_eq_zero_iff_collinear`
(massless iff one null edge - strongest conjunct); (T) spin-vertex channel
separation `chiralOdd(massVertex)=-γμ` / `chiralEven(massVertex)=(m+1)•1` plus the
operator-grade `gamma5_mass_diff_comm`; (C) `massWithoutMass` (positive Z2
glueball gap, definitional zero quark mass); (co-location)
`charge_grading_mass_compatible` (octonion charge does NOT enter the mass).

IN FLIGHT (9 Aristotle jobs this run): two independent attacks on the Q6 KP crux
`pairSum_le_expBound`; the two downstream KP sorries; the M1/M3 CONNECTED Wilson
slab + holonomy factorization; transfer-gap-from-RP design (NE-U4); multi-link
product-Haar RP; fermionic RP-F crux; NE-U6 electroweak rung; mass-taxonomy
separation theorem.

PERMANENT boundaries: continuum/QMF8 out; no numerical mass values; the physical
YM mass gap is off-ladder; regulator mass must be SEPARATED not absorbed.

## The asks (answer each decisively, grounded in the actual tree)

1. **The single highest-leverage NEXT theorem** (beyond the 9 in flight) to
   maximize honest coverage of "all mass". Name it concretely (statement shape +
   which existing modules it builds on) and say why it dominates.
2. **The biggest HIDDEN RISK** in the current all-mass story - a conflation, a
   vacuity, a missing-object dependency, or an over-claim that the bundling
   capstone or the mass doc still carries. Be adversarial; the project rewards a
   kernel-checked NEGATIVE.
3. **What "all mass" can and cannot mean here.** State the strongest DEFENSIBLE
   one-paragraph "all mass from null edges" claim reachable this run if the 9
   jobs mostly succeed, and the strongest that is reachable EVEN IF the Q6 crux
   and the connected slab both fail. (The floor matters as much as the ceiling.)
4. **Ranked wave-2 queue** (top 5) by expected value / effort, assuming the 9
   in-flight jobs return, each with its trigger condition.
5. **The cheapest kill-test** for the mass thesis: the single finite check most
   likely to expose that "mass = relational obstruction to null transport" is
   over-claimed as a UNIFICATION rather than a shared mechanism SHAPE.

## Output

Write `output-final_aristotle/sm-allmass-strategy-FINDINGS.md`: the ranked
answers with concrete theorem names/shapes and file citations, an EV table for
ask 4, and a blunt bottom line. Skip lake build; source-grounded strategy only.
