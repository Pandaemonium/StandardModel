# Summary of changes for run ff73446b-5a95-4d47-8d9f-101ddcb2605a
Created `SplitStepSchurJet.lean` (Mathlib-only), a new self-contained module that machine-checks the tie between the crossing-charge census and the split-step walk symbol at the central node — closing the audit finding "nothing machine-checked ties the charge census to the walk". The file builds cleanly with no `sorry`, no added axioms, and every key theorem depends only on the standard `[propext, Classical.choice, Quot.sound]`.

What is derived (from the walk symbol, not supplied):

Setup. The Dirac generators `alpha1, alpha2, alpha3, beta` are copied verbatim from `context/Pluecker3Plus1ComplexMass.lean`. The walk symbol is `U(qx,qy,qz) = E(beta,theta45)·E(alpha3,qz)·E(alpha2,qy)·E(alpha1,qx)` with `E(M,t) = cos t·1 + i sin t·M` and the exact 3-4-5 coin `Bmat = (4/5)·1 + (3/5) i beta` (no transcendental). `walkNode` proves `U(π/2,π/2,π/2) = U0 = Bmat·(iα3)·(iα2)·(iα1)`.

T1 (exact node structure). `U0` is shown Hermitian-unitary hence an involution (`U0_mul_self : U0*U0 = 1`) and traceless (`U0_trace`). Using the spectral projectors `P± = (1±U0)/2` (idempotent, matrix-trace 2) and `LinearMap.IsProj.trace`, `kerDim_gap0` and `kerDim_gapPi` prove `dim ker(U0-1) = 2` and `dim ker(U0+1) = 2`. Explicit orthonormal kernel bases `V0plus`, `V0minus` are given (`V0plus_orthonormal`, `V0minus_orthonormal`) with `Wplus_kernel`, `Wminus_kernel` certifying the columns lie in the eigenspaces.

T2 (the jet). The exact directional derivatives at q0 are obtained algebraically: `jet_q1/q2/q3` prove `U(q0 + t·e_j) = cos t·U0 + sin t·D_j`, so `D_j` is literally the coefficient of the linear term (only the j-th factor contributes). The reducing-subspace compressions `V0ᴴ D_j V0` are computed (`compress_plus`, `compress_minus`) — identical for both gaps. The real 3×3 Jacobian `J[a,j] = -(s/2)·Im tr(σ_a · V0ᴴ D_j V0)` is proved to equal `(4/5)·diag(1,-1,1)` at gap 0 (`Jac_gap0`, s=+1) and to flip sign at gap π (`Jac_gapPi`, s=-1). The gap-orientation factor `s` is documented as load-bearing (the compressions coincide, so s alone produces the flip). The documented naming `J_recorded = -J_here` is proved against the mirrored landed fixtures (`recorded_eq_neg_here_gap0/gapPi` vs `Jplus/Jminus`).

T3 (the tie). `chargeOf_gap0 = -1` (det -64/125) and `chargeOf_gapPi = +1` (det +64/125), and `census_floquet_opposition_derived` proves the two gap charges at the central node are opposite — deriving the landed `census_floquet_opposition` instance at this node from the walk symbol.

All computed constants matched the gate table (the 3-4-5 coin, |w|=1 kernel structure, and ±(4/5)diag Jacobians), so no corrected identity was required. All arithmetic is Gaussian-rational; the 1/√2 normalisations cancel to 1/2 via `compress_scale`. The `context/` files are left unchanged (they are not on the build path); the small `chargeOf`/`Jplus`/`Jminus` definitions were mirrored locally with provenance noted so the derived Jacobians can be compared to the supplied fixtures.
