# Summary of changes for run 8f2ddd35-be37-4ecc-97e6-f3c1ba6740c1
Closed both documented analytic sorries from `context/TwoBandEigenphaseCount.lean` in a new self-contained file `TwoBandEigenphaseAnalytic.lean` (the project's build root). The hinge (Part 1), the semicircle count `countAt`, the `CrossingData`/`TwoBandFamily` interface, periodicity (c) and the reduction (d) are copied verbatim; the hinge and reduction proofs are untouched. The whole file builds with no `sorry`/`admit`, and the key results are axiom-clean (only `propext`, `Classical.choice`, `Quot.sound`).

(a) `TwoBandFamily.countAt_locally_constant` — proved EXACTLY as stated (hypotheses included). The route avoids the (unpackaged) continuity of `Polynomial.roots`: for a 2×2 matrix the two eigenvalues are `(tr ± s)/2` for a square root `s` of the discriminant `tr² − 4·det`, and the count is pinned down by the signs of two continuous, formula-based functions of the matrix — `imProd` (product of the eigenvalues' imaginary parts, `= ((tr.im)² − (‖Δ‖ − Δ.re)/2)/4`) and `imSum = tr.im`. Supporting lemmas proved: `exists_roots`, `countAt_indicators`, `unitary_root_abs_one` (eigenvalues of a unitary matrix lie on the unit circle), `imProd_ne_zero_of_no_pm_one`, the three count/sign lemmas, continuity of `imProd`/`imSum`, and `sign_const` (a nonvanishing continuous function keeps its sign on an interval, via IVT). Local constancy then follows: `imProd∘U` never vanishes on `[a,b]` so it keeps its sign, and when positive `imSum∘U` also keeps its sign.

(b) `TwoBandFamily.jump_law` — the statement as literally given in the reference is under-specified: nothing forces the crossing list to enumerate every ±1-eigenvalue event in a bracket, nor pins the second eigenvalue away from ±1 at the crossing, so the count can jump by other amounts (an automated search in fact pursued a counterexample). Per the job's allowance for a documented strengthening (never silent), I added, with explicit docstrings, the honest missing hypotheses that hold for a genuine two-band walk: a `hclean` field on `TwoBandFamily` (the crossing list is complete on each closed bracket) and, on `CrossingData`, the second eigenvalue as a continuous local branch `other` with the local root identity `roots(U k) = {e^{i·branch k}, other k}` and `other` off the real axis at the crossing. Under these, `jump_law` is proved via a self-contained `countAt_local_jump` lemma: local constancy reduces the sample-to-sample difference to a one-sided difference across the crossing, the crossing branch's imaginary part `sin(branch k)` changes sign transversally (`eventually_sign_of_hasDerivAt`) while the second branch keeps its sign, giving the signed unit jump `if 0 < cos p · v then 1 else −1`, which matches `jumpOf` (`+sign` at a 0-crossing, `−sign` at a π-crossing).

Consequently the downstream `flowDiff_eq_zero` and `no_single_crossing'` are now also fully proved (no inherited `sorryAx`). The only remaining diagnostics are cosmetic `unusedSimpArgs` style warnings inside the automated proofs; there are no errors. All work is committed and pushed.
