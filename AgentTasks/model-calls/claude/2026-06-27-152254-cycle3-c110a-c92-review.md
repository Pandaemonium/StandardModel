# Claude model call log

## Metadata

- Provider: `Claude CLI`
- Model: `opus`
- Status: `completed`
- Dry run: `False`
- Started: `2026-06-27T15:22:00`
- Finished: `2026-06-27T15:22:54`
- Timeout seconds: `600`
- Max budget USD: `1.50`
- Return code: `0`

## Command

```text
claude -p --bare --model opus --max-budget-usd 1.50 --output-format text --add-dir 'C:\Projects\StandardModel' --tools default --permission-mode bypassPermissions --disallowed-tools 'Edit Write NotebookEdit mcp__neo4j_graph__write-cypher mcp__zotero_write' --mcp-config 'C:\Projects\StandardModel\Scripts\autonomous_loop\review.mcp.json' --strict-mcp-config
```

## Prompt

```text
# Claude review packet: cycle 3 C110a and C92 triage

Date: 2026-06-27

## Project context

We are running the null-edge autonomous loop. The active Gate C1 strategy is now:

```text
C1_local:
  retained as a local/no-go and escape-hatch audit.

C1_NU:
  controlled non-ultralocal release. Drop finite-range/exponential locality as
  a primitive requirement, but keep path-sum/resolvent/spectral/finite-volume
  control, true bad-sector inverse gap, ghost-zero exclusion, gauge covariance
  or dressing, Krein audit, anomaly audit, and regulator stability.

C1_NL:
  declared nonlocal fallback only when no controlled non-ultralocal certificate
  is claimed.
```

The decisive constructive route is:

```text
canonical branch observable B(U)
  -> finite polynomial / Riesz / sign / path-sum projector
  -> nonzero origin chiral-index test
  -> true bad-sector inverse gap
  -> path-sum / resolvent / finite-volume control
```

## Subject 1: C110a path-shell kernel bridge

Cycle 3 submitted a focused Aristotle job:

```text
Project: c804899f-1d36-4ba3-bc16-f656c105f164
Target: AgentTasks/aristotle-standalone/c110a-path-shell-kernel-bridge-20260627/C110aPathShellKernel/PathShellEnvelope.lean
```

The theorem is intentionally modest. It should bridge:

```text
finite shell cardinality bound + per-path amplitude bound
  => actual finite shell contribution bound.
```

This responds to your prior critique that scalar envelope summability is not yet
operator-kernel control.

Review questions:

- Is this theorem statement a useful finite bridge, or is it still too weak to
  matter?
- Is the lack of a nonnegativity hypothesis on each `weight p` a semantic issue,
  given that the theorem bounds the signed sum above rather than a norm?
- What should the immediate successor theorem be: nonnegative weights,
  norm-valued weights, matrix/operator norm shell bounds, or summability of
  shell bounds?
- Are there Lean statement repairs Aristotle is likely to need?

## Subject 2: C92 ghost-safety candidate integration triage

A stale C92 job was canceled earlier and continued as a partial-return task.
That continuation now returned a candidate file:

```text
AgentTasks/aristotle-output/03c6e63f-3a39-420e-81d3-173f2611b362/extracted/project-files.tar/output-3_aristotle/PhysicsSM/Draft/NullEdgeGateCGhostSafetyHardened.lean
```

The integration helper reports no placeholders for this new file. However, the
current autonomous-loop `AGENTS.md` contains a strong rule:

```text
No checklist-as-Lean:
  a Draft Lean module must make a statement about real mathematical objects
  tied to the program, such as matrices, operators, spinors, modules, kernels.
  A self-defined record of `Prop` or `Bool` flags that restates a planning
  checklist is not formal progress and belongs in markdown.
```

The C92 candidate defines `GhostZeroSafetyData` as five independent `Prop`
fields and proves logical separation facts about those fields. My current triage
is:

```text
Do not auto-apply C92 as Lean.
Preserve the idea as a ghost-safety audit/checklist note unless or until it is
connected to real program objects.
```

Review questions:

- Is this triage correct under the no-checklist-as-Lean rule?
- Is there any part of the C92 candidate that is strong enough to integrate as
  Lean without laundering a checklist into formal progress?
- What would be the minimal real-object version of C92? For example, should it
  quantify over a concrete propagator-zero datum, residue matrix, Krein form, or
  gauge-coupled branch sector?

## Requested output

Please give:

```text
Findings:
- ordered by severity.

C110a verdict:
- useful / too weak / statement repair needed.

C92 triage verdict:
- apply / reject as checklist-as-Lean / convert to markdown / rewrite around
  real objects.

Next theorem:
- one precise theorem statement or Aristotle job target.
```


## Verbatim source artifacts under review

These are the ACTUAL files. Base every finding on the real statements and definitions below, not on any paraphrase above. For each theorem under review, explicitly check whether the Lean matches its intended reading, and flag every mismatch.

### AgentTasks/aristotle-standalone/c110a-path-shell-kernel-bridge-20260627/C110aPathShellKernel/PathShellEnvelope.lean (59 lines)

```lean
import Mathlib

/-!
# C110a path-shell kernel bridge

This standalone Aristotle target is a finite Mathlib-only bridge for the
non-ultralocal Gate C1 path-sum program.

It does not claim locality, gauge covariance, a branch observable, or a physical
release.  It only isolates the finite shell estimate needed before a path-count
envelope can be used as an operator-kernel contribution bound.
-/

namespace C110aPathShellKernel

open scoped BigOperators

variable {Path : Type*} [DecidableEq Path]

/-- The total nonnegative contribution of one finite path shell. -/
def ShellContribution (s : Finset Path) (weight : Path -> Real) : Real :=
  sum s weight

/--
If every path in a finite shell has contribution at most `ampBound`, and the
number of paths in the shell is at most `pathCountBound`, then the shell's total
contribution is at most `pathCountBound * ampBound`.

This is the finite bridge from a path-count/per-path envelope to an actual shell
kernel contribution.  Later C110/C111 work can layer summability, gauge
transport, matrix norms, and regulator limits on top of this theorem.
-/
theorem shell_contribution_le_count_mul_amplitude
    (s : Finset Path) (weight : Path -> Real)
    (pathCountBound ampBound : Real)
    (hAmpNonneg : 0 <= ampBound)
    (hCard : (s.card : Real) <= pathCountBound)
    (hWeightBound : forall p, p in s -> weight p <= ampBound) :
    ShellContribution s weight <= pathCountBound * ampBound := by
  sorry

/--
Length-indexed form of `shell_contribution_le_count_mul_amplitude`.

This is the shape expected in the null-edge path-sum application: a shell at
length `n`, a combinatorial path-count envelope, and a per-path amplitude
envelope.
-/
theorem shell_contribution_le_length_envelope
    (s : Finset Path) (weight : Path -> Real)
    (pathCount ampBound : Nat -> Real) (n : Nat)
    (hAmpNonneg : 0 <= ampBound n)
    (hCard : (s.card : Real) <= pathCount n)
    (hWeightBound : forall p, p in s -> weight p <= ampBound n) :
    ShellContribution s weight <= pathCount n * ampBound n := by
  sorry

end C110aPathShellKernel

```

### AgentTasks/aristotle-output/03c6e63f-3a39-420e-81d3-173f2611b362/extracted/project-files.tar/output-3_aristotle/PhysicsSM/Draft/NullEdgeGateCGhostSafetyHardened.lean (229 lines)

```lean
import Mathlib

/-!
# C92: Golterman–Shamir ghost-safety hardening API

This module is an **additive hardening layer** for the Gate C ghost-safety
program.  A sibling module, `PhysicsSM.Draft.NullEdgeGateCGhostZeroSafety`
(C47/C48), already builds the *operational* zero-classification calculus
(`ZeroDatum`, `IsFatalGhost`, `GhostZeroSafe`, `KreinPositivePhysicalSector`,
`PostGaugeGhostSafe`, …) on top of an enumerated propagator-zero spectrum, and
`PhysicsSM.Draft.NullEdgeProjectedGhostSafety` (C63) wires that into the
projected Route-B release certificate.  This C92 module deliberately does **not**
duplicate that machinery.  Instead it provides the small, self-contained,
`Prop`-level **separation API** that downstream Gate C work (C90 projected Wilson
release, C93 overlap / Ginsparg–Wilson C1 interface) can import to make one fact
syntactically hard to misuse:

> **Full ghost safety is strictly stronger than scalar residue positivity, than
> Krein positivity of the physical sector, and than Gate C0 external species
> health.**

## Literature anchors

* Golterman–Shamir `2311.12790`: propagator zeros can promote to gauge-coupled
  ghost states once weak gauge coupling is switched on.
* Golterman–Shamir `2505.20436`: propagator zeros may require an enlarged
  elementary-plus-composite interpolating-field basis and remain under
  generalized no-go pressure.
* Lüscher `hep-lat/9802011`: exact chiral symmetry is **not** the same as ghost
  safety.

## What this module provides

* `GhostZeroSafetyData` — a record of the five *independent* `Prop`-valued
  clauses that genuine Golterman–Shamir ghost safety must control.
* `GoltermanShamirSafe` — full safety = the conjunction of all five clauses.
* `ScalarResiduePositiveOnly` — the weak, frequently-overclaimed condition:
  scalar residue positivity *without* control of gauge-coupled ghost zeros.
* `C0SpeciesHealthProxy` — a `Prop`-level proxy for Gate C0 external species
  health (residue positivity + Krein physical-sector positivity), i.e. exactly
  the regulator-level clauses, *omitting* the gauge-coupled-ghost and
  interpolating-basis clauses.

## Guardrails proved

* `ghostSafety_requires_noGaugeCoupledGhostZeros`,
  `ghostSafety_requires_interpolatingBasis_or_noZeros` and the companion
  projections — full safety entails each clause separately.
* `scalarResiduePositive_not_ghostSafety`, `kreinPositive_not_ghostSafety`,
  `c0SpeciesHealth_not_ghostSafety` — explicit witnesses where the weak condition
  holds but full safety fails.
* `ghostSafety_strictly_stronger_than_scalarResidue` and
  `goltermanShamirSafe_not_iff_scalarResiduePositive` — full safety is *strictly*
  stronger than, and **not equivalent** to, scalar residue positivity.
* `fullySafeWitness_safe` — full safety is non-vacuous (it can hold).

## Honesty discipline

Nothing physical is asserted as fact.  No concrete null-edge operator is claimed
ghost-safe; the only content is the pure logical separation of the clauses.  In
particular Krein positivity is kept distinct from ghost safety (and, a fortiori,
from unitarity), and Gate C0 species health is kept strictly weaker than ghost
safety, matching the acceptance criteria of the C92 task.
-/

namespace PhysicsSM
namespace Draft
namespace NullEdgeGateCGhostSafetyHardened

/-! ## 1. The five-clause ghost-safety record -/

/-- The five **independent** `Prop`-valued clauses that genuine Golterman–Shamir
ghost safety must control.  Bundling them as a record (rather than collapsing
them into a single predicate) is the whole point: it forces every downstream
release claim to name which clauses it actually controls. -/
structure GhostZeroSafetyData where
  /-- no propagator zero promotes to a gauge-coupled propagating ghost state
  (the core Golterman–Shamir `2311.12790` hazard). -/
  noGaugeCoupledGhostZeros : Prop
  /-- the physical state space is BRST / cohomologically safe (no surviving
  negative-norm states after the cohomological quotient). -/
  brstOrCohomologicalSafe : Prop
  /-- the physical sector is Krein positive (every physical pole has positive
  Krein residue).  This is *not* unitarity and *not* ghost safety on its own. -/
  kreinPhysicalSectorPositive : Prop
  /-- scalar residue positivity (the weak, frequently-overclaimed condition). -/
  scalarResiduePositive : Prop
  /-- an elementary-plus-composite interpolating-field basis is complete enough
  to represent the physical states (Golterman–Shamir `2505.20436`). -/
  interpolatingFieldBasisComplete : Prop

/-- **Full Golterman–Shamir ghost safety**: the conjunction of all five clauses. -/
def GoltermanShamirSafe (d : GhostZeroSafetyData) : Prop :=
  d.noGaugeCoupledGhostZeros ∧ d.brstOrCohomologicalSafe ∧
  d.kreinPhysicalSectorPositive ∧ d.scalarResiduePositive ∧
  d.interpolatingFieldBasisComplete

/-- **Scalar residue positivity only**: scalar residue positivity *together with*
the explicit failure of the gauge-coupled-ghost clause.  This is the precise
shape of the overclaim the API is meant to block. -/
def ScalarResiduePositiveOnly (d : GhostZeroSafetyData) : Prop :=
  d.scalarResiduePositive ∧ ¬ d.noGaugeCoupledGhostZeros

/-- A `Prop`-level proxy for **Gate C0 external species health**: residue
positivity plus Krein physical-sector positivity, i.e. the regulator-level
clauses, deliberately omitting the gauge-coupled-ghost and interpolating-basis
clauses.  Matches the spirit of
`PhysicsSM.Draft.NullEdgeGateC0SpeciesHealth.GateC0SpeciesHealthy` being strictly
weaker than physical chiral release. -/
def C0SpeciesHealthProxy (d : GhostZeroSafetyData) : Prop :=
  d.scalarResiduePositive ∧ d.kreinPhysicalSectorPositive

/-! ## 2. Full safety entails each clause (projection guardrails) -/

theorem ghostSafety_requires_noGaugeCoupledGhostZeros {d : GhostZeroSafetyData}
    (h : GoltermanShamirSafe d) : d.noGaugeCoupledGhostZeros := h.1

theorem ghostSafety_requires_brstOrCohomologicalSafe {d : GhostZeroSafetyData}
    (h : GoltermanShamirSafe d) : d.brstOrCohomologicalSafe := h.2.1

theorem ghostSafety_requires_kreinPhysicalSectorPositive {d : GhostZeroSafetyData}
    (h : GoltermanShamirSafe d) : d.kreinPhysicalSectorPositive := h.2.2.1

theorem ghostSafety_requires_scalarResiduePositive {d : GhostZeroSafetyData}
    (h : GoltermanShamirSafe d) : d.scalarResiduePositive := h.2.2.2.1

theorem ghostSafety_requires_interpolatingFieldBasisComplete
    {d : GhostZeroSafetyData} (h : GoltermanShamirSafe d) :
    d.interpolatingFieldBasisComplete := h.2.2.2.2

/-- Full safety entails the Golterman–Shamir `2505.20436` disjunction: either the
interpolating-field basis is complete, or (a fortiori, since both hold) there are
no gauge-coupled ghost zeros. -/
theorem ghostSafety_requires_interpolatingBasis_or_noZeros
    {d : GhostZeroSafetyData} (h : GoltermanShamirSafe d) :
    d.interpolatingFieldBasisComplete ∨ d.noGaugeCoupledGhostZeros :=
  Or.inl h.2.2.2.2

/-! ## 3. Separation witnesses -/

/-- The dangerous datum: scalar residue positive, BRST-safe, Krein-positive, and
interpolating-basis complete — yet a gauge-coupled ghost zero is present
(`noGaugeCoupledGhostZeros := False`).  This is exactly the Golterman–Shamir
situation in which every "external health" check passes but the theory is not
ghost-safe. -/
def scalarOnlyWitness : GhostZeroSafetyData where
  noGaugeCoupledGhostZeros := False
  brstOrCohomologicalSafe := True
  kreinPhysicalSectorPositive := True
  scalarResiduePositive := True
  interpolatingFieldBasisComplete := True

/-- The fully safe datum: every clause holds.  Shows full safety is non-vacuous. -/
def fullySafeWitness : GhostZeroSafetyData where
  noGaugeCoupledGhostZeros := True
  brstOrCohomologicalSafe := True
  kreinPhysicalSectorPositive := True
  scalarResiduePositive := True
  interpolatingFieldBasisComplete := True

theorem fullySafeWitness_safe : GoltermanShamirSafe fullySafeWitness :=
  ⟨trivial, trivial, trivial, trivial, trivial⟩

/-- `ScalarResiduePositiveOnly` data is never fully ghost-safe. -/
theorem scalarResiduePositiveOnly_not_ghostSafe {d : GhostZeroSafetyData}
    (h : ScalarResiduePositiveOnly d) : ¬ GoltermanShamirSafe d :=
  fun hsafe => h.2 hsafe.1

/-- **Scalar residue positivity does not imply ghost safety.** -/
theorem scalarResiduePositive_not_ghostSafety :
    ∃ d : GhostZeroSafetyData, d.scalarResiduePositive ∧ ¬ GoltermanShamirSafe d :=
  ⟨scalarOnlyWitness, trivial, fun h => h.1⟩

/-- **Krein positivity of the physical sector does not imply ghost safety.**
(Krein positivity is therefore distinct from ghost safety — and a fortiori from
unitarity.) -/
theorem kreinPositive_not_ghostSafety :
    ∃ d : GhostZeroSafetyData,
      d.kreinPhysicalSectorPositive ∧ d.scalarResiduePositive ∧
        ¬ GoltermanShamirSafe d :=
  ⟨scalarOnlyWitness, trivial, trivial, fun h => h.1⟩

/-- **Gate C0 external species health does not imply ghost safety.** -/
theorem c0SpeciesHealth_not_ghostSafety :
    ∃ d : GhostZeroSafetyData, C0SpeciesHealthProxy d ∧ ¬ GoltermanShamirSafe d :=
  ⟨scalarOnlyWitness, ⟨trivial, trivial⟩, fun h => h.1⟩

/-! ## 4. Strict-strength / non-equivalence guardrails -/

/-- **Full safety is strictly stronger than scalar residue positivity**: it
implies scalar residue positivity, yet scalar residue positivity can hold while
full safety fails. -/
theorem ghostSafety_strictly_stronger_than_scalarResidue :
    (∀ d : GhostZeroSafetyData, GoltermanShamirSafe d → d.scalarResiduePositive) ∧
      (∃ d : GhostZeroSafetyData, d.scalarResiduePositive ∧ ¬ GoltermanShamirSafe d) :=
  ⟨fun _ h => h.2.2.2.1, scalarResiduePositive_not_ghostSafety⟩

/-- **`GoltermanShamirSafe` is not equivalent to scalar residue positivity.**
This is the syntactic guardrail demanded by the acceptance criteria: no future
code may collapse the two. -/
theorem goltermanShamirSafe_not_iff_scalarResiduePositive :
    ¬ (∀ d : GhostZeroSafetyData, GoltermanShamirSafe d ↔ d.scalarResiduePositive) := by
  intro h
  exact ((h scalarOnlyWitness).2 trivial).1

/-! ## 5. Summary -/

/-- **C92 summary.**  The ghost-safety record cleanly separates the five clauses:
full safety is non-vacuous, entails each clause, and is **strictly stronger**
than (and not equivalent to) scalar residue positivity, Krein positivity, or
Gate C0 species health. -/
theorem c92_ghost_safety_separation_summary :
    GoltermanShamirSafe fullySafeWitness ∧
    (∀ d : GhostZeroSafetyData, GoltermanShamirSafe d → d.noGaugeCoupledGhostZeros) ∧
    (∃ d : GhostZeroSafetyData, d.scalarResiduePositive ∧ ¬ GoltermanShamirSafe d) ∧
    (∃ d : GhostZeroSafetyData, d.kreinPhysicalSectorPositive ∧ ¬ GoltermanShamirSafe d) ∧
    (∃ d : GhostZeroSafetyData, C0SpeciesHealthProxy d ∧ ¬ GoltermanShamirSafe d) ∧
    ¬ (∀ d : GhostZeroSafetyData, GoltermanShamirSafe d ↔ d.scalarResiduePositive) :=
  ⟨fullySafeWitness_safe,
   fun _ h => h.1,
   scalarResiduePositive_not_ghostSafety,
   ⟨scalarOnlyWitness, trivial, fun h => h.1⟩,
   c0SpeciesHealth_not_ghostSafety,
   goltermanShamirSafe_not_iff_scalarResiduePositive⟩

end NullEdgeGateCGhostSafetyHardened
end Draft
end PhysicsSM

```

## Final instruction

Produce your review now, strictly in the Required output format specified above.
```

## Response stdout

```text
# Claude review: cycle 3 C110a and C92 triage

## Findings

**Severity 1 — C92 is checklist-as-Lean (blocking integration)**

The `GhostZeroSafetyData` record is five independent `Prop` fields with no tie to any mathematical object in the program — no propagator, no residue matrix, no Krein form, no operator, no module, no spinor, no kernel. Every "separation" theorem is just unfolding a 5-tuple of opaque `Prop`s. The `scalarOnlyWitness` literally sets one field to `False` and the others to `True`; this is propositional bookkeeping, not mathematics. Under the explicit autonomous-loop rule ("a Draft Lean module must make a statement about real mathematical objects… A self-defined record of `Prop` or `Bool` flags that restates a planning checklist is not formal progress"), this file is exactly the disallowed pattern. The literature anchors in the docstring (Golterman–Shamir 2311.12790, 2505.20436, Lüscher hep-lat/9802011) are decorative — none of them is reflected in the Lean content. `c92_ghost_safety_separation_summary` is the giveaway: it bundles trivialities into an "achievement" theorem.

**Severity 2 — C110a Lean statement has syntax/semantic errors Aristotle must fix before any proof**

The submitted file is not valid Lean 4 as written:

- `sum s weight` should be `s.sum weight` or `∑ p ∈ s, weight p`. There is no bare `sum` in scope.
- `forall p, p in s -> weight p <= ampBound` uses prose `in`, not Lean's `∈`. Should be `∀ p ∈ s, weight p ≤ ampBound` (or `∀ p, p ∈ s → …`).
- `Real` should be `ℝ` or `Real` is fine if `open Real` — acceptable, but combined with the above this file will not elaborate.
- `[DecidableEq Path]` is unused for the statement (only the proof of a Finset.sum bound needs it occasionally). Minor.

So Aristotle must first repair the statement before it can prove it. The intended proof is one line: `Finset.sum_le_card_nsmul`-style → `(s.card : ℝ) * ampBound` → `pathCountBound * ampBound` by `mul_le_mul_of_nonneg_right hCard hAmpNonneg`. Trivial once syntax is fixed.

**Severity 3 — C110a missing nonnegativity on `weight`**

You correctly flagged this in the review question. The theorem bounds a signed sum from above by `pathCountBound * ampBound`. That is mathematically true (it is what the statement says), but it is **not** a kernel-contribution bound, because in operator language one needs `|K| ≤ count · amp`, not `Re K ≤ count · amp`. The current theorem is fine as a finite arithmetic bridge, but useless as an operator-kernel envelope. The successor must take values in `ℝ≥0`, `NNReal`, or be `|weight p| ≤ ampBound` with `‖ShellContribution‖ ≤ …`.

**Severity 4 — C110a length-indexed variant adds no new content**

`shell_contribution_le_length_envelope` is just the first theorem with `ampBound` and `pathCountBound` evaluated at `n`. It does not introduce a length variable into `Path`, the shell `s`, or `weight`. It should at minimum be `s : Nat → Finset Path` with `s n` and a hypothesis that `s n` is the length-`n` shell, or it should be dropped in favor of layering the next theorem (summability over `n`) on top.

**Severity 5 — C110a is still scalar, not operator/kernel**

Even after the nonnegativity repair, this remains a `ℝ`-valued shell sum. The honest bridge to "operator-kernel control" requires `weight : Path → E` with `E` a normed space (matrix entries, or `Matrix ι ι ℂ`, or a bounded operator on a fixed finite-dim space), and the conclusion `‖∑ p ∈ s, weight p‖ ≤ pathCountBound * ampBound` via `norm_sum_le` plus the card bound. That is the theorem that earns the name "kernel bridge."

## C110a verdict

**Statement repair needed, then useful-but-weak.** The current statement does not elaborate (`sum s`, `p in s`). After repair it is a correct but scalar, signed-sum inequality — a true *arithmetic* finite bridge but not yet a kernel bridge. The lack of nonnegativity on `weight` is the semantic gap: it is consistent with the signed bound stated, but inconsistent with the "operator-kernel contribution" framing in the docstring. Land it only as an explicitly-scalar lemma; the *named* kernel bridge should be the normed-space version.

## C92 triage verdict

**Reject as checklist-as-Lean. Convert to a markdown note in `docs/` or `AgentTasks/`, and rewrite around real objects before any Lean integration.**

No subcomponent of the current file is strong enough to keep as Lean: every theorem is provable by `fun h => h.1` and depends only on the shape of the record, not on any mathematical object. Keeping any fragment would launder the checklist. The literature framing, separation logic, and acceptance criteria belong in a markdown audit note where they are honest.

Minimal real-object rewrite ("C92-real"): take a fixed finite-dimensional Krein space `(V, J)` with a propagator-zero datum `Z : Finset (Submodule ℂ V)` (each element a residue subspace), a residue map `R : Z → Module.End ℂ V`, a gauge-coupling perturbation map `G : Z → Module.End ℂ V`, and define
- `NoGaugeCoupledGhostZero z := ∀ v ∈ z, ⟪v, J (G z v)⟫ = 0` (or equivalent residue-trace condition),
- `ScalarResiduePositive z := 0 ≤ trace (R z)`,
- `KreinPhysicalPositive z := ∀ v ∈ physical z, 0 ≤ ⟪v, J v⟫`.

Then prove that on this concrete data, `ScalarResiduePositive` does **not** imply `NoGaugeCoupledGhostZero` by exhibiting a 2×2 explicit `R, G, J` witness. That separation theorem is real mathematics; the current one is not.

## Next theorem

```
theorem shell_kernel_norm_le_count_mul_amplitude
    {E : Type*} [SeminormedAddCommGroup E]
    {Path : Type*} (s : Finset Path) (weight : Path → E)
    (pathCountBound ampBound : ℝ)
    (hAmpNonneg : 0 ≤ ampBound)
    (hCard : (s.card : ℝ) ≤ pathCountBound)
    (hWeightBound : ∀ p ∈ s, ‖weight p‖ ≤ ampBound) :
    ‖∑ p ∈ s, weight p‖ ≤ pathCountBound * ampBound
```

Aristotle job: repair and prove this normed-space statement in the existing `c110a-path-shell-kernel-bridge` standalone, replacing the two scalar theorems. The proof is `(norm_sum_le _ _).trans` ∘ `Finset.sum_le_card_nsmul` (or `Finset.sum_le_sum` with constant bound) ∘ `mul_le_mul_of_nonneg_right hCard hAmpNonneg`. Specialize the scalar version as a corollary using `weight p := ‖weight₀ p‖` only if downstream code actually needs it.

```

## Response stderr

```text

```
