# Claude call: round 019 adversarial after four-trace

Date: 2026-06-24
Model: sonnet
Command status: 0
Later usefulness rating: pending

## Query

Please adversarially choose the next single Aristotle proof job for the null-edge program.

Context: We just integrated the explicit finite four-reflection trace formula in the current real 2x2 branch-reflection API. Existing P2 ladder: one reflection trace zero; determinant products parity only; two-reflection trace continuous scalar; three-reflection trace zero; four-reflection trace explicit scalar, not automatically topological.

Question: Should we spend one more job in P2 on a concrete four-reflection nonconstancy witness or closure corollary, or pivot to P1-F observer normalization, P1/P4/P7 dynamics/proper-time bridge, or P9-F source visibility/noise? Pick exactly one proof target. Give precise Lean theorem shape, hidden risks, and why it is publication-worthy. Avoid broad speculation.

## Response

# Recommendation: P2 four-reflection-trace nonconstancy witness  Do **not** pivot yet. The ladder as it stands has a referee-fatal hole: an "explicit scalar, not automatically topological" four-reflection trace is logically compatible with being constant on the relevant parameter cell, in which case the n=4 rung adds nothing over the n=2 continuous-scalar rung. Close that hole in one job before touching P1-F / P4 / P7 / P9-F.  ## Precise Lean theorem shape  Target the *minimal separating* statement, decidable from the explicit formula:  ```lean theorem fourReflTrace_nonconstant :   ∃ (a b : ReflParams),       a.detSign = b.detSign ∧       fourReflTrace a ≠ fourReflTrace b := by   refine ⟨witness₀, witness₁, rfl, ?_⟩   unfold fourReflTrace witness₀ witness₁   norm_num ```  with two strengthenings, each one extra `have`:  ```lean theorem fourReflTrace_not_factoring_through_twoReflTrace :   ∃ a b, twoReflTrace a = twoReflTrace b ∧          fourReflTrace a ≠ fourReflTrace b  theorem fourReflTrace_not_determined_by_det :   ∃ a b, (∀ i, (a.R i).det = (b.R i).det) ∧          fourReflTrace a ≠ fourReflTrace b ```  Use rational angles (e.g. mirror normals at `(1,0),(0,1),(1,0),(1,1)/√2` reframed via `cos²/sin²` rationals) so the proof closes by `norm_num` / `decide` with zero `Real.sqrt` reasoning.  ## Hidden risks (adversarial)  1. **Silent parity collapse.** A casually chosen quadruple can land where the explicit formula reduces to `∏ det = ±1`; you "prove nonconstancy" but only separated parity classes. Mitigation: fix `a.detSign = b.detSign` in the statement. 2. **Conjugacy artifact.** If your witnesses are SO(2)-conjugate, you have not separated from the n=2 scalar invariant. Mitigation: include the `twoReflTrace a = twoReflTrace b` clause. 3. **Irrational closure.** Witnesses with `√2`, `√3` force `Real.sqrt` arithmetic that Lean cannot close by `norm_num`. Mitigation: choose angles whose `cos, sin` are dyadic-rational (e.g. Pythagorean triples 3-4-5, 5-12-13). 4. **API drift.** `fourReflTrace` currently takes four `Matrix (Fin 2) (Fin 2) ℝ`; if you refactor to a `ReflParams` record mid-job you re-do the witness. Freeze the signature first. 5. **Trace-zero coincidence at the witness.** If one witness happens to give trace `0` it mimics the n=1, n=3 rungs and reviewers will ask whether `fourReflTrace` is "secretly" odd-rung-like. Pick witnesses with traces of distinct nonzero magnitudes (e.g. `≈ 0.28` vs `≈ 1.6`).  ## Why publication-worthy  - It is the **smallest claim that licenses every downstream statement** the ladder paper wants to make: "n=4 is the first rung carrying angle-dependent (non-parity, non-cyclic-scalar) data." Without it, the table `1:0, 2:scalar, 3:0, 4:explicit` is decorative. - It separates the ladder from the determinant-parity invariant and from the n=2 continuous scalar in one stroke — the three corollaries above are the exact bullets a referee will demand. - It is **fully formal and self-contained**: one decidable arithmetic check, no new definitions, no extension of the API. That makes it a clean citation anchor ("Theorem X, formalized") for later P1-F / P4 dynamical lifts, which will *use* nonconstancy as a hypothesis rather than re-prove it. - Pivoting first to P1-F observer normalization or the P4/P7 proper-time bridge before nonconstancy is established means those papers inherit an unjustified "the n=4 invariant is nontrivial" assumption — a structural debt that compounds.  One job, one `theorem`, three corollaries on the same witness pair. Then pivot to P1-F.
