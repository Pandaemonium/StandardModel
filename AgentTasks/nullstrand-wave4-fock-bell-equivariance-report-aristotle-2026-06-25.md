# Summary of changes for run 2bcf3082-b08e-4bec-a289-3e28cfb2979c
Completed the wave-4 Fock/Bell direction-marginal equivariance task on `PhysicsSM/NullStrand/BellQFT/FockCutoff.lean`.

**Build constraint:** the package omits the `SpherePacking` dependency that the lakefile hard-requires, so `lake build`/`lake env lean` cannot configure the workspace (exact blocker: `SpherePacking: package directory not found`). Since the module only needs `import Mathlib` (which is present and prebuilt under `.lake/packages/mathlib`), I checked it by invoking `lean` directly with a `LEAN_PATH` over the prebuilt libs. The file elaborates with no errors or warnings, contains no `sorry`/`axiom`/`@[implemented_by]`, and `#print axioms` on the headline theorems reports only the standard `propext, Classical.choice, Quot.sound`.

**Naming-drift repair (requested):** the old `fockNullLift_equivariant` was only a total-mass identity, not an equivariance theorem. Renamed it to `fockNullLift_total_mass_preserved` (statement unchanged, honest docstring), and kept `fockNullLift_equivariant` as a `@[deprecated]` alias so nothing downstream breaks.

**Stronger honest results added and proved:**
- Honest predicate `PreservesDirectionMarginal`, with the requested composition/iteration helpers (`preservesDirectionMarginal_id`, `PreservesDirectionMarginal.comp`, `PreservesDirectionMarginal.iterate`) and total mass as a corollary (`.total_mass_preserved`).
- A genuine direction-marginal equivariance theorem `fockNullLift_configMarginal_equivariant`: a lift commuting with a direction relabeling `e : Γ ≃ Γ` transports the configuration-summed direction marginal equivariantly (`configMarginal (L (relabelDirection e ρ)) ω = configMarginal (L ρ) (e ω)`). This is conditional on an explicit `DirectionEquivariant e L` hypothesis — genuine structure that cannot be derived for an arbitrary lift, and is the strongest honest equivariance statement the current abstract operator API supports. Supporting lemmas `directionMarginal_relabelDirection`, `DirectionEquivariant.comp`, `DirectionEquivariant.iterate`, and `fockNullLift_directionMarginal_relabel_preserved` are also proved.

No statements were weakened or made vacuous; one unused hypothesis was dropped to keep a statement minimal. The roadmap note in `Sources/NullStrand_Lean_Roadmap_Improved.md` was updated from "needs a naming audit" to "resolved". A detailed completion report is at `AgentTasks/aristotle-output/nullstrand-wave4-fock-bell-equivariance-completion-report.md`. There are no remaining proof holes in this file.
