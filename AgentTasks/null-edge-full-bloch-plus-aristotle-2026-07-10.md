# Aristotle proof task: full-Bloch plus-one determinant

Prove only `det_splitStep_sub_one` in `NullEdgeBlochDet/Plus.lean` without
changing its statement.  Start with:

`lake env lean NullEdgeBlochDet/Plus.lean`

`Determinants.lean` is the preserved snapshot from stalled project `569e66ec`.
It already supplies `det_fin_four`, exact closed forms for all four factors,
and `splitStep_eq`; those helpers were generated by Aristotle and contain no
proof holes.  Use them to expand the determinant, normalize powers of `I`, and
close the real trigonometric polynomial with `sin_sq_add_cos_sq`.  Work only on
the `+1` theorem.  Do not alter the polynomial or substitute a numerical test.
If a harvested helper is malformed, repair it locally and report the exact
change.  Return the target file immediately if broad verification is slow.

```yaml
aristotle:
  project_id: d13856aa-98ef-466e-8284-42ef850b9dc0
  task_id: 75503e73-7a3f-48cf-b34f-d89ba2d12bda
  target_file: NullEdgeBlochDet/Plus.lean
  expected_module: NullEdgeBlochDet.Plus
  submission_project: AgentTasks/aristotle-submit/null-edge-full-bloch-plus-20260710-project
  output_dir: AgentTasks/aristotle-output/d13856aa-98ef-466e-8284-42ef850b9dc0
  status: submitted
```
