# codex-grand-strategy-0700-audit-pivot-20260709

You are Aristotle, acting as a strategic proof/audit advisor for the NullEdge
all-mass run. This is a no-build strategy job; do not attempt a Lean build.

Context:
- The user pushed the hard audit switch from 6am to 7am after a server glitch.
- Codex has just harvested/ported these M modules:
  `IncidenceCorank`, `WEPActionBridge`, `IndexAnomalyInterface`,
  `GateI1.MassEntropyMonotone`, `SuiteCDNextRungs`,
  `KMFamilyRankBridge`, `IndexProtectionBridge`,
  `WEPActionResourceBridge`, and `MassResourceConsistency`.
- Local follow-up added `SuiteCDNextRungs.channel_charges_pairwise_commute` and
  `SuiteCDNextRungs.channel_charges_commute_with_Bsum`, then threaded those
  through `MassResourceConsistency`.
- `codex-goalII-km-flagship-compose-20260709` is still running; it should bundle
  `KMPhaseCounting`, `FiniteKMCP`, and `IncidenceCorank`.
- Claude has landed Goal I/III modules and a future-direction-only
  `Goal4FieldEquation` result that overlaps Codex's WEP action/source lane.

Task:
Give a ranked 7am audit/strategy plan for the next two hours. Prioritize:
1. which exact theorem modules should be audited first for overclaim/vacuity;
2. which missing theorem piece would most improve manuscript honesty;
3. which landed theorem rows should be kept out of the manuscript or moved to
   future directions;
4. whether the remaining Codex effort should go to Goal II flagship composition,
   Goal IV/WEP-field reconciliation, resource-measure API cleanup, or C3 index
   protection;
5. one or two concrete Aristotle follow-up jobs that are small enough to be
   worth submitting.

Output format:
- Findings first, ordered by risk.
- Then a short prioritized action list.
- Explicitly mark any recommendation that is only strategic prose and not a
  Lean theorem.
