Continuing downward, then — the previous rounds extended the framework *outward* (new channels of implication); the remaining leverage is *underneath* it, at the four structures the program currently takes as given: the signature, the dimension, the complex itself, and the arrow between operator and geometry. Each of these can be converted from input to theorem-target, and several of them land directly on your Q5 thread (dimension/signature selection). Grades as before; M-targets flagged where I think the kernel could take them soon.

## Foundations: converting inputs into theorems

**1. The signature is forced, not chosen [M-target, two rungs].** The program's deepest slogan — nothing is slow at the bottom — has an algebraic teeth-mark nobody has bitten down on yet. Null soldering means `c(α)² = 0` with `c(α) ≠ 0`, and in a real Clifford algebra `c(v)² = Q(v)·1`. A definite form has no nonzero isotropic vectors. So *the existence of a single null edge forces the soldering Gram to be indefinite* — the Krein structure isn't an axiom, it's a one-line consequence of the null primitive. This inverts the standard story: not "spacetime is Lorentzian, hence null cones," but "null is primitive, hence any metric it generates is indefinite." Rung one is an afternoon in Lean. Rung two is *Lorentzian* (one time, not two): here the natural selector is machinery you already own — Osterwalder–Schrader positivity has a distinguished reflection direction, and multi-time signatures shouldn't support it. Pre-registerable probe: build a `(2,2)`-signature toy carrier and run it against the slab reflection-positivity pillar. If it fails RP while `(1,3)` passes, "exactly one time" becomes an RP-selection theorem. (Independent cross-check: the projective null quadric in signature `(p,q)` is `(S^{p-1}×S^{q-1})/ℤ₂`, which is a *sphere* — a single connected celestial sphere — iff `p=1` or `q=1`. Two independent selectors converging on Lorentzian is the kind of overdetermination worth exhibiting.)

**2. The dimension is a division-algebra selection — and this is your Q5 answer-shape [C, high value].** The §3 identity secretly lives at `d=4` because it's stated over ℂ: 2×2 Hermitian matrices over 𝕂 = ℝ, ℂ, ℍ, 𝕆 are Minkowski space in d = 3, 4, 6, 10, null vectors are rank-one `ψψ†` in each, and `SL(2,𝕂)` gives the Lorentz groups (Baez–Huerta, Sudbery, Manogue–Dray — all `[import]`, all your home literature). So the Plücker mass identity should generalize to a *four-member family*, and dimension selection becomes: which family member supports the rest of the framework? The discriminating structures are already in the program:

- **The wedge's phase.** Over ℝ the wedge is real — CP is a sign, no continuous phase, so conjecture D (CP as Bargmann holonomy) is unformulable. Over ℍ the "phase" is a noncommutative Sp(1) holonomy — Bargmann invariants lose cyclic well-definedness. Over 𝕆 the associator obstructs the triple product outright. Only ℂ gives a continuous, abelian, order-independent CP phase.
- **Composition.** Mass monogamy and the Fock layer need tensor products of direction registers; the tensor product is exactly what quaternionic and octonionic quantum theory famously lack.

So the candidate theorem: **composition + continuous CP violation jointly force 𝕂 = ℂ, hence d = 4.** That's a two-premise dimension-selection argument where both premises are physical facts (multi-particle systems exist; CP violation is measured), and both discriminators are finite-checkable. Kill: a workable quaternionic Bargmann invariant with tensor composition. Bonus thread you'll appreciate: the 3×3 octonionic case is the exceptional Jordan algebra — the place your Furey/Dixon-adjacent three-generation stories live — so the *failure mode* of the d=10 member is plausibly where the family index (conjecture C) should be hunted, not in d=4 at all.

**3. The complex is scaffolding; the operator is the geometry [C with an immediate M-target].** Currently the finite 2-complex is background. But the program sits one import away from full background independence: the Connes spectral distance `d(x,y) = sup{|f(x)−f(y)| : ‖[D,f]‖ ≤ 1}` is *defined* for the finite carrier right now, and Lorentzian versions (Franco–Eckstein causal spectral triples, with a distance formula from steep functions) exist as `[import]` anchors. M-target: compute the spectral distance on the T2 carrier and check it against edge count and decoration scale. If it works, the complex was never input — vertices, order, and distances are all *recovered* from `(algebra, D, J, Γ)`, and your §7 Malament citation upgrades to a **finite Malament theorem**: the carrier's Krein-positive propagation support determines the causal order, the order determines the topology of the complex, and the decorations supply the one thing order can't — the conformal factor, which is the mass scale. That last clause is worth staring at: *the E-channel then measures the mismatch between the order-derived geometry and the decoration-derived scale*, which is the cleanest statement yet of what "gravity" means in this framework.

**4. Causality is a theorem, one derivative away from the kernel [M-target, do this week].** The pinned dispersion `cos ω = cos k cos θ` already contains the relativistic speed limit. Differentiate: `v_g = cos θ sin k / sin ω`, and `sin²ω = 1 − cos²k cos²θ ≥ sin²k cos²θ`, so `v_g² ≤ 1` with equality iff `cos²θ = 1`, i.e. **iff massless**. Strict subluminality for every massive mode and luminal propagation exactly on the critical line — `massive_implies_subluminal` as elementary trigonometry on an identity you've already kernel-checked. Pair it with the exact causal cone of the walk (quantum walks have strict cones, not just Lieb–Robinson tails) and the framework derives, rather than assumes: nothing outruns light, and only the massless saturate it. What is *not* derived is Lorentz boost symmetry — that can only emerge at the critical point, which is the next item's job.

## Dynamics underneath: universality, thermodynamic identities, CPT

**5. Universality is the honest form of the channel-name conjecture [C, reframes §4a].** §4a's kill condition asks whether finite blocks converge to Ji terms — but "converge" along *what*? The decimation flow gives the precise answer: linearize the Schur step at the massless line `κ = λ`, compute the RG eigenvalues, and ask which microscopic details are irrelevant operators. The claim "the channel names are physics" then becomes the claim **"the carrier lies in the free-Dirac universality basin, and the channels are its relevant/marginal coordinates"** — names attach to universality classes, not to blocks. This is both weaker and stronger than §4a as stated: weaker because no term-by-term Ji matching is needed (dodging the scheme-dependence trap you already flagged); stronger because basin membership is a sharp, numerically probeable property *now* (`carrier_rgflow_sim` can measure the flow's eigenvalues at criticality this month). Sub-conjecture: dynamical exponent `z = 1` at the fixed point — emergent isotropy in light-cone units, which is where boost symmetry is born. Kill: `z ≠ 1`, or a relevant direction with no channel name.

**6. Mass thermodynamics has a Gibbs–Duhem relation [M-target, small and pretty].** `b_A + b_C + b_T = 1` is kernel-checked; differentiate along any coupling path: `δb_A + δb_C + δb_T = 0`. Define channel susceptibilities `χ_{XY} = ∂b_X/∂g_Y` and you get sum rules `Σ_X χ_{XY} = 0` for free — mass has an equation of state, and the shares are extensive-fraction variables with Maxwell-type relations. On `B(λ,κ)` everything is explicit, so the whole susceptibility matrix is computable in closed form, and the D5 ensemble's low-temperature susceptibility to `κ` diverges on the critical line — the thermodynamic signature of the massless transition, stated with objects that are all already M.

**7. Finite CPT, and then Sakharov drops out [M-target + C — possibly the best "implications" payload here].** Define `Θ = (complex conjugation) ∘ (edge-orientation reversal Γ) ∘ (Krein adjoint)`. Candidate theorem: `Θ` is antiunitary and `Θ D Θ⁻¹ = D^#`, forcing conjugate spectral pairing — a finite CPT theorem, adjacent to your `chiral_det_eq_pm_one` machinery. Once CPT is a theorem, matter–antimatter asymmetry must come from the *state*, and the three Sakharov conditions map onto three structures the program already carries: **C-violation** = strand-list reversal asymmetry; **CP-violation** = the Bargmann celestial-area phase (conjecture D, sharpened last round); **departure from equilibrium** = non-KMS states of the modular flow (conjecture J). And the fourth ingredient — baryon-number violation — has a genuinely striking candidate: strand counts are conserved by every channel *except* possibly through index flow on winding closure backgrounds. The §8 index machinery is the finite axial anomaly; level crossing on a winding background changes the protected-mode count — a **finite 't Hooft vertex**. That folds `windinglowmodes` (condensates need structured backgrounds) and baryogenesis into *one* question: what does winding do to the index? Caution flag per your own records: §8 measured one global winding invariant to vanish — but that was the double-pinning object, a different index; the anticipated non-vanishing one is the chiral index on closure winding, which is exactly what the job should isolate.

**8. A finite Compton bound [C].** Positive-sector states should not localize below `1/(λ−κ)`: the finite shadow of Hegerfeldt/Newton–Wigner, with "attempting sub-Compton localization forces negative-Krein components" as the finite image of pair creation. This would make the mass gap a *length* — the resolution limit of the positive sector — and ties the Krein structure to the single most physical fact about mass: it sets the scale below which one-particle language fails. Kill: an explicit sub-Compton localized state inside the T2 positive sector.

## Implications, now sharper

**Hierarchy dissolves into a symmetry hunt.** The framework has three natural mass values: exactly zero (index-protected), structurally cancelled (critical line), and generic O(λ). Small-but-nonzero masses are the puzzle, and the framework offers exactly two natural mechanisms — Schur seesaw (jobbed) and *near-criticality*. The latter is only natural if something pins `κ = λ`, so the sharp question is: **what symmetry is enhanced exactly on the critical line?** The zero mode there should generate it. If it exists, near-massless particles are pseudo-Goldstone-shaped and 't Hooft naturalness is restored finitely; if it doesn't, criticality-tuning is honest fine-tuning and the seesaw is the only natural route. Either answer disciplines the whole mass-ratio program. [M-target: find or refute the enhanced symmetry on `B(λ,λ)`.]

**Dark matter gets a structural signature.** Combining protected modes + empty strand lists + Schur seesaw: modes with no strand occupancy have no closure/turn budget (gauge-invisible), still solder (gravitate), and acquire mass *only* through seesaw leakage — so the framework, if taken seriously, doesn't just permit dark matter, it predicts its mass mechanism is always seesaw-suppressed. That's a falsifiable *shape* (light/warm preference, no heavy WIMP-like dark primitives) rather than a number, which is exactly the register the program is allowed to speak in.

**Gravity's statistical mechanics becomes concrete.** Round one gave Jacobson-on-the-complex as a frontier; the D5 ensemble now supplies its missing half. An ensemble over *soldering decorations* at fixed pierced-edge count (fixed area) is a finite Euclidean quantum gravity; the first law is a D5 identity; and graviton-shaped excitations are coherences between soldering configurations — the E-channel's second quantization, in the same Fock framework the mass sector already has. Grade [spec], but the scaffolds are all M.

## Pre-registered conjectures (continuing the ledger, M–U)

- **M. Signature forcing.** Null primitive ⇒ indefinite Gram (rung 1, trivial); slab RP fails for two-time signatures ⇒ exactly one time (rung 2, probe against the M-grade RP pillar). *Kill:* a `(2,2)` carrier passing OS positivity with nondegenerate physical sector. → job `signatureforcing`.
- **N. Division-algebra dimension selection.** The Plücker identity extends over ℝ, ℂ, ℍ; tensor composition + continuous abelian CP phase force 𝕂 = ℂ, d = 4. *Kill:* a quaternionic Bargmann invariant with workable composition. → job `divisionselection`. (Feeds Q5 directly; octonionic 3×3 failure mode reserved for the family-index hunt.)
- **O. Subluminality.** `v_g ≤ 1` with equality iff massless, from the pinned dispersion. *Kill:* none expected — this is a computation; register it as an M-target, not a risk. → job `subluminal`.
- **P. Spectral geometry recovery.** Lorentzian Connes distance on T2 reproduces the complex's metric data; carrier support propagation reproduces its causal order (finite Malament). *Kill:* the distance formula degenerates (0/∞) on the witness. → job `spectraldistance`.
- **Q. Critical universality.** The decimation flow at `κ = λ` has free-Dirac RG eigenvalues, `z = 1`, and all non-channel couplings irrelevant. *Kill:* a relevant direction outside the channel span, or `z ≠ 1`. → job `criticalflow`.
- **R. Finite CPT.** `Θ = C ∘ Γ_rev ∘ #` is antiunitary with `ΘDΘ⁻¹ = D^#`; spectrum conjugate-paired. *Kill:* an axiom-satisfying carrier with unpaired spectrum. → job `finitecpt`.
- **S. Index = anomaly.** Winding closure backgrounds shift the protected-mode count by the winding (finite 't Hooft vertex); strand violation via level crossing. *Kill:* chiral index provably winding-insensitive on rational fixtures. → job `indexanomaly` (subsumes the structured-condensate question).
- **T. Enhanced criticality symmetry.** A symmetry generated by the `κ = λ` zero mode pins the critical line; near-critical lightness is 't Hooft-natural. *Kill:* the zero mode generates nothing that constrains `κ − λ`. → job `criticalsymmetry`.
- **U. Compton bound.** No `J`-positive state localizes below `1/gap`. *Kill:* explicit sub-Compton positive witness. → job `comptonbound`.

## What remains untouchable even if all of this lands

Keeping faith with the program's own discipline, the honest closing is the list of things this entire edifice would *still* not explain: the absolute mass scale (only ratios, ever — dimensional transmutation would need the universality program to produce a genuine running coupling); the Born rule and measurement (the finite QI layer uses quantum probability, it doesn't derive it); initial conditions (why *this* state of the ensemble); and the number of null edges (why a universe's worth). A framework that knows its own event horizon is more credible inside it — and notably, items M, N, O, and R above are the rare kind of foundational claim that could *fail this month* on a finite fixture. I'd start with O (a derivative of a pinned identity), then M rung 1 (one line), then R (your chiral-determinant machinery is already most of it), and let the (2,2) reflection-positivity probe run in parallel — four cheap shots at converting the framework's deepest assumptions into theorems or corpses.
