# Summary of changes for run 1a5efa51-595b-4d39-90f7-173e64990928
Audited the five actual source files under `PhysicsSM/Draft/NullEdge/`. All five build with the narrow `lake build` (only style/lint warnings), contain no `sorry`/`admit`/`axiom` declarations, and each headline theorem is build-guarded to depend only on `[propext, Classical.choice, Quot.sound]`. Full report written to `AgentTasks/AUDIT_p0_closers_20260709.md`.

Findings by file (severity — exact theorem names):

- FiniteCPT.lean — CLEAN. `Theta_antiunitary`, `Theta_conjugates_D_to_sharp`, `spectrum_conjugate_paired` are all genuine and non-vacuous over the explicit ℂ⁴ witness. "Antiunitary" is correctly qualified as Krein-isometric w.r.t. the genuinely indefinite `Jmet` form; `spectrum_conjugate_paired` includes `Θ v ≠ 0` (non-degeneracy present), and eigenvectors genuinely exist (`Dop_sq = -1`). Keep the declared caveat: explicit-witness only, not arbitrary-carrier CPT.

- RPSelectsLorentzian.lean — CLEAN with scope caveats. `oneTime_reflectionPositive` proves both `ReflectionPositive` and `Nondegenerate` (strict), so the non-degeneracy fixture is present; `twoTime_reflectionPositive_fails` and the two `actionKernel` lemmas are genuine. LOW: `_h0 : sig 0 = true` is genuinely unused (convention hypothesis, correctly underscored). Caveats to keep: reflGram = single scalar `C 1 0` of a 2-site single-mode toy (not full OS reconstruction); direction 0 is postulated time, so it selects exactly-one-time vs ≥2-time and does not address the 0-time case; `twoTime_..._fails` is actually slightly stronger than its name (doesn't need `sig 0 = true`).

- BargmannCP.lean — CLEAN algebra. `bargmann_CP_odd`, `bargmann_CP_genuine_iff`, `bargmann_bloch_re/_im/_unit`, `bargmann_tan_arg_unit` are genuine exact identities. MEDIUM (docstring outruns statement): `bargmann_tan_arg_unit` only proves the algebraic `tan(arg B) = btriple/(1+dots)`; the Van Oosterom–Strackee `tan(Ω/2)` / "arg B = half solid angle" / "celestial solid angle" readings are NOT formalized (no solid-angle object, no arg branch handling) and must stay commentary. INFO: file omits the `autoImplicit false` header the others use; `Bargmann` is a mildly generic namespace.

- GradedDecompUniqueness.lean — CLEAN. `blocks_eq_eigenspaces`, `decomposition_unique`, `split_not_forced` are genuine and fully general; the "selecting axiom" is a hypothesis, not an `axiom` declaration. Keep the declared caveat: generic eigenspace-uniqueness, not carrier-specific uniqueness of 2(D#D) (no formal link to that carrier).

- FamilyRankNoGo.lean — genuine core but hollow candidate encodings. `forcing_iff_rankfixing` (any forcing predicate ⇔ `n = 2`) is the real, honest content and is general. MEDIUM (hollow predicates): `JordanStruct n := jordanDim 3 8 = 27 ∧ n = n` is independent of `n` (contains the tautology `n = n`, effectively `True`) and `TrialityStruct n` holds for every `n`, so `jordan_not_forces` and `triality_not_forces` are near-vacuous as statements about the actual structures; the header phrase "faithfully formalized" overstates fidelity for candidates (a) and (c). `AnomalyStruct`/`anomaly_not_forces` is genuinely faithful (nontrivial ∑q=∑q³=0 witness). LOW: `Completions n := Fin (n+1)`, so `completionCount_eq` ("null-edge count n+1") is definitional/an input, not derived (honestly noted in the docstring).

No false theorems, no vacuous-by-contradiction proofs, and no missing non-degeneracy fixtures. Namespaces are distinct and everything sits under `.../Draft/...`; no provenance/anchor drift. I did not add or strengthen any claims — only the audit report file was added and pushed.
