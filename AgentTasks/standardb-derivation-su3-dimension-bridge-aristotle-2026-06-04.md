# Aristotle task: dimension and su3 bridge for StandardB inner derivations

Date: 2026-06-04

## Goal

Make concrete progress toward identifying the Lie subalgebra generated by
inner derivations from `h3(C)` elements with an `su(3)`-shaped algebra.

Target module:

```text
PhysicsSM/Algebra/Jordan/InnerDerivationStandardBSU3Bridge.lean
```

## Context

Relevant modules:

- `PhysicsSM.Algebra.Jordan.InnerDerivationStandardBLieSubalgebra`
- `PhysicsSM.Algebra.Jordan.InnerDerivationStandardBStabilizer`
- `PhysicsSM.Algebra.Jordan.H3OComplexMatrix`
- `PhysicsSM.Algebra.Jordan.TraceForm`
- `PhysicsSM.Algebra.Jordan.DVTStabilizerPrelude`

## Preferred outcome

Define an explicit finite family of eight derivations inside
`innerDerivationStandardBSubspace` and prove a useful rank/dimension theorem.
Depending on the available API, good outcomes include:

```lean
theorem standardB_innerDerivation_eight_generators_mem :
    forall i : Fin 8, standardBInnerDerivationGenerator i in
      innerDerivationStandardBSubspace

theorem standardB_innerDerivation_eight_generators_linearIndependent :
    LinearIndependent Real standardBInnerDerivationGenerator
```

or a span theorem:

```lean
theorem innerDerivationStandardBSubspace_eq_span_eight_generators :
    innerDerivationStandardBSubspace =
      Submodule.span Real (Set.range standardBInnerDerivationGenerator)
```

## Stretch target

If feasible, define a clean comparison map from the eight-generator subspace to
the traceless anti-Hermitian `3 x 3` complex matrices already used elsewhere in
the project, and prove bracket compatibility on generators.

## Fallback

If dimension machinery is too expensive, create the explicit eight-generator
family and prove:

- every generator lies in the StandardB inner-derivation subspace,
- the family is closed under bracket up to the same subspace,
- a package records these concrete facts without claiming dimension equality.

## Verification

Run:

```bash
lake env lean PhysicsSM/Algebra/Jordan/InnerDerivationStandardBSU3Bridge.lean
```

## Submission

Status: submitted.

Submission project: `AgentTasks/aristotle-submit/paper-wave8-20260604`

First upload attempt hit a transient SSL error and did not appear in
`aristotle list`; retry succeeded.

Job ID: `26e0beba-18e6-4967-8c4c-420c5c425452`
