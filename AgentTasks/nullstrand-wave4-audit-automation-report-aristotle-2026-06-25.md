# Summary of changes for run e58437c4-82df-4d15-bbc4-f0571b7c4423
Turned the wave-4 NullStrand audit scaffold into mechanically enforced G0 gates, with no trusted escape hatches (no `axiom`, `@[implemented_by]`, `native_decide`, or `sorry`). The targeted build worked (deps were cached), so every Lean change was compiled and verified; the optional `SpherePacking` dependency, which the audit modules do not use, was temporarily removed only to build in isolation and has been fully restored (`lakefile.toml` and `lake-manifest.json` are unchanged from the original).

New/changed files:
- `PhysicsSM/NullStrand/Audit/CapstoneAxioms.lean` (new): capstone assumption-surface whitelist. Pins, via `#guard_msgs in #print axioms …`, the transitive axiom set of the two concrete capstones `finiteIIDNullStrand_master` and `checkerboardBohmBell_master` (plus the i.i.d. non-vacuity witness). All are exactly `[propext, Classical.choice, Quot.sound]`; any leaked `sorry`/`native_decide`/new `axiom` underneath now fails the build. I confirmed this is a genuine gate by perturbing the expected list and observing a build failure, then restoring it.
- `PhysicsSM/NullStrand/Audit/DuplicateNames.lean` (new): a base-name collision gate. It introspects the whole `PhysicsSM.NullStrand` environment (excluding the `Audit` subtree and compiler-generated companions) and fails the build on any base-name collision not on an explicit, documented allowlist. Baseline: 2 intentional collisions (`minkowskiInner`, `FiniteNullResolution`) and 3 tracked-debt duplicates (`realPos`/`realPos_nonneg`/`realPos_sub_realPos_neg`, defined identically in both `BellQFT` and `ZigZag.TransferCurrent`).
- `PhysicsSM/NullStrand/Audit.lean` (edited): now also aggregates `CapstoneAxioms`; documents that `DuplicateNames` is built separately to avoid an import cycle with the NullStrand root.
- `Scripts/check_nullstrand_draft_firewall.py` (new): polices the trusted/draft import graph — exactly four trusted importers, each pinned to one permitted draft core; everything else in `PhysicsSM/Draft` is quarantined. Detects new importers, wrong-core edges, audit leaks to quarantined modules, and stale allowlist entries (verified with a positive and a negative test).
- `Scripts/check_nullstrand_manifest.py` (new): reconciles `Sources/NullStrand_Lean_Theorem_Manifest.csv` against source.
- `Sources/NullStrand_Lean_Theorem_Manifest.reconciled.csv` (new): proposed reconciled manifest (the user's CSV is left untouched per the no-destructive-edit policy).
- `Sources/NullStrand_Audit_Plan.md` (new): full plan/report (firewall strategy, capstone whitelist approach, manifest reconciliation plan, duplicate-name baseline, and exact local commands).

Manifest reconciliation findings (4 genuine blocking drifts in the current CSV): NS09 (`uniformComponent_bounds_meanNorm` moved to `RegulatorMeanNorm` in the 2026-06-25 dedup), NS25 (realized as `Ergodic.IIDStrongLaw.iidNullSteps_empiricalMean_tendsto`), NS37 (`quantumCurrent_antisymm` implemented as `quantumCurrent_zero_of_block_zero`), NS08 (`fluxDirectionVariance_eq_massRatioSq` mislabeled `READY` though the `FluxMeasure` module does not exist). Both concrete capstones were entirely missing from the manifest and are added (NS49/NS50). The reconciled CSV passes the checker with zero blocking drift.

Verification (commands a human can run locally): `lake build PhysicsSM.NullStrand.Audit` and `lake build PhysicsSM.NullStrand.Audit.DuplicateNames` both succeed; `python3 Scripts/check_nullstrand_draft_firewall.py` reports the firewall intact; `python3 Scripts/check_nullstrand_manifest.py` exits 1 on the current manifest (correctly reporting the real drift) and exits 0 on the reconciled CSV; the forbidden-token scan (strict, including `native_decide`) finds nothing in the audit modules. These commands and a build-environment note are also recorded in `Sources/NullStrand_Audit_Plan.md`.
