# GOAL PROMPT - Codex, Section 7 six-gate run

Execute `AgentTasks/24h-six-gates-run-2026-07-13/RUN_PLAN.md` continuously
until 10:30 PDT on July 13, 2026. You are co-equal with Claude Opus when Opus is
available. Coordinate only through this run's append-only `LEDGER.md`.
`AGENTS.md`, the Lean kernel, convention locks, provenance requirements, and
trust rules are binding.

## Startup

Read `START_HERE.md`, the run plan, packet review, gate matrix, and the previous
run's final report/scorecard. Immediately:

1. run `aristotle list --limit 40`;
2. inspect project `b7405f03-7bf4-47c2-9b3a-71ce9040df3f` before duplicating
   the continuum target;
3. append lane claims and inherited-job verdicts to the ledger;
4. prepare focused semantic context packs;
5. launch a proof wave, a strategy job, and an adversarial audit job.

The configured Opus wrapper was out of credit at startup. Retry it no more than
once per hour through the repository wrapper. Continue independently between
retries and never claim Opus reviewed work it did not return.

## Primary lanes

### 7.1 Continuum

Close the arbitrary-`L2` projection theorem in
`ChangingMomentumCellProjectionStrongScaffold`, then compose it with
`ChangingMomentumCellProjectionThreeTerm` and `ScaledChangingMomentumWalk`.
First accept a compactly supported smooth dense-core theorem; use density to
reach arbitrary `L2`. Only claim a position-space Dirac PDE after the explicit
inverse-Fourier/sign/normalization square is proved.

### 7.2 Lorentz

Prepare a minimal exact 1+1 rational-boost package. Prove Minkowski-form
preservation, infinite orbit for a nonzero future vector, and the finite-support
invariance no-go. Separately formalize a finite count-law invariance shadow
under measure-preserving relabeling. Keep the continuum Poisson-sprinkling
theorem external and source-audited.

### 7.5 Gauge chirality/anomalies

Use the existing overlap/Ginsparg-Wilson and index APIs to prove gauge
conjugation covariance and index invariance, then instantiate a nontrivial
finite link/gauge witness. Do not substitute the Standard Model rational
anomaly sums for a gauge-field theorem. Consult PhysLean and primary lattice
sources before fixing conventions.

### Integration and verification

Own all new module integration, guard pins, root imports, exact fixtures,
manuscript claim mapping, aggregate builds, deterministic verifier runs, and
the final full build. Cross-audit Opus outputs for source support and
finite-to-continuum slippage.

## Shared high-value targets

- specialize maximum-entropy/modular selection to the actual Pluecker pair
  generator with a phase-sensitive observable;
- compose dimensional transmutation with the rest-gap operator while exposing
  every input scale and coupling;
- generalize finite Clausius universality from two coordinates to a separating
  finite variation family;
- prove a finite partition-response/count-variance identity for Lambda;
- construct an active-sector rest-operator/pair-generator intertwiner and a
  no-naive-extension control.

## Aristotle discipline

Keep approximately 5-7 useful `codex-6gate-` jobs when capacity permits:
3-4 proof/composition jobs, one strategy job, and one hostile audit. Use focused
packages when possible. Every statement typechecks before submission, prohibits
weakening, includes a nonzero witness and wrong-shape control, and names the
manuscript consequence and kill condition. Apply the two-hour stall rule after
preserving snapshots.

## Research cadence

Run or delegate a literature/public-Lean pass at least every 30 minutes. Use
primary full text for theorem claims. Search Mathlib and PhysLean before
inventing APIs. Log sources, theorem numbers, hypotheses, conventions, version
gaps, and licenses.

## Landing and final phase

No result lands without direct Lean verification, semantic review, guard pin,
root import where appropriate, provenance, and exact-scope manuscript wording.
At 08:30 PDT stop broad construction. Harvest only, inspect every headline
declaration, run the aggregate guards and full build, execute the deterministic
verifier twice, and complete `HONEST_SCORECARD.md` and `FINAL_REPORT.md`.

The one rule that matters most: each of the six gates must become more precise,
not merely more impressive-sounding.
