# Interactive Claude Code entry point

Read and execute `GOAL_PROMPT_OPUS.md` in this directory. Its filename is a
legacy artifact; its content now targets the user-started interactive Claude
Code session.

Do not invoke a Claude API or repository review wrapper. Record actual
availability and work through the AFPL mailbox and this run's `LEDGER.md`.
