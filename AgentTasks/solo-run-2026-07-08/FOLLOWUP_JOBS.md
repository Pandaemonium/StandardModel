# Follow-up jobs — open pieces surfaced by the harvest (2026-07-08)

Natural closers for the no-gos / partials that the harvest returned. To submit as a
new round once all completed jobs are integrated.

- **signatureforcing rung 2** [closes M]: prove the OS-lattice reflection-positivity
  selector on an explicit toy — `(2,2)` fails RP (degenerate physical sector) while
  `(1,3)` passes. Rung 1 (null=>indefinite) is M; rung 2 was delivered as a probe
  only, because the distinction lives in the OS lattice/tensor structure, not the
  quadratic form. -> job `siglorentz`.
- **carrierrigidity selecting axiom** [closes F2]: the four-block type-count is forced
  (no fifth block) but uniqueness is NOT. Identify + prove the ONE extra axiom that
  pins the split uniquely (kills the "two extra structures" that make it non-unique).
  -> job `rigidityaxiom`.
- **familyindex rank-fixing** [closes/refutes C]: count = n+1, three iff n=2, nothing
  pins n. Hunt for the structure that forces n=2 (candidate: the divisionselection
  octonionic/exceptional-Jordan d=10 failure mode reappearing internally as a rank-2
  family index). Either finds the rank-fixing axiom or a second no-go. -> job
  `familyrankfix`.
- **cpholonomy Bargmann CP-oddness + solid angle** [closes D]: the gauge-invariant
  Bargmann triple is already M (NullEdgeBargmannPhaseInvariance); the remaining pieces
  are (i) CP-oddness of the Bargmann triple under conjugation, (ii) its phase = half
  the celestial-sphere geodesic-triangle solid angle. -> job `bargmanncp`.
- **chiralindex exactly-one** [minor]: only `>= |index|` proved; the exactly-`|index|`
  refinement holds for a full-rank corner. -> could fold into a small follow-up.

## Submitted follow-up round (2026-07-08)
- siglorentz `265f327e` (signatureforcing rung 2: OS RP selects (1,3) over (2,2))
- rigidityaxiom `6f3f56de` (F2: the selecting axiom that makes the 4-block split unique)
- familyrankfix `79472461` (family index: what forces strand rank n=2 / three generations)
- bargmanncp `febae797` (Conj D: CP-oddness + celestial solid angle of the Bargmann triple)
