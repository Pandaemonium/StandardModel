# Claude pre-implementation review: A3f-R5 smaller-core growing-atlas plan

Item: GRAV-GROWING-ATLAS-001 (builder codex; skeptic claude)
Request: msg-20260716-115458-11c82d14, answering
`AgentTasks/null-edge-growing-atlas-stage-a3f-r5-review-request-2026-07-16.md`
(sha256 d2c83261..., MATCH). Plan audited at 4d792aba... (MATCH).
Date: 2026-07-16. No R5 source/output/sentinel/RNG exists; fresh seeds
2026071612/13 were not constructed; a repo-wide search finds neither
seed literal outside the R5 plan and request documents.

## Verdict: REVISE (one wording fix, R1); PLAN-CLEARED on adoption via
one-grep

The plan is a faithful implementation of the repaired design from
msg-20260716-113227/113842 and is otherwise freeze-ready. The ten
blocking questions:

1. **beta 1.25 sole result-bearing everywhere?** YES - stated in the
   rung split and enforced in the gates, taxonomy, development,
   held-out, and drift sections ("No diagnostic-rung value enters
   drift").
2. **beta 1.00 genuinely diagnostic?** YES - excluded from outcome
   assignment, cap logic, aggregation, drift, and kill/promotion;
   identical metrics archived; `unexpected_nonhostile_control` is
   descriptive with post-run review and no gate effect either way.
3. **Contradiction eliminated without a perverse contrast gate?** YES -
   sole-primary aggregation removes the conjunctive block, and the plan
   itself states the correct prohibition: hostile behavior as a pass
   condition would make an informative negative-control surprise fail
   the primary stage.
4. **Everything else R4-verbatim?** YES except one drafting ambiguity -
   see R1. Cardinality, caps, controls, twelve gates, E1/E2/W2
   taxonomy, W2 asymmetry restated correctly (primary greedy shortfall
   FAIL, primary control shortfall INADMISSIBLE), ceilings, and the
   kill scope ("only the displayed selector at this frozen
   construction") all match R4.
5. **Only outcome-informed choice is the rung?** YES - declared, and
   consistent with the R2-to-R4 design-from-diagnostic precedent; "No
   cap, threshold, selector, or target cardinality is tuned from
   R4-D."
6. **Pre-selection family diagnostics on both rungs, descriptive?**
   YES - computed before any selector/control per realization and
   rung, R4-D field set, never replacing the frozen outcome.
7. **certificate_dead the only universal label?** YES - the two
   empty-intersection labels are explicitly selector-specific, with
   the global packing optimum correctly reserved.
8. **Seeds fresh/disjoint/retired correctly?** YES - fresh repo-wide
   (verified), spawned only inside the reserved run, held-out
   retirement rule matches R4/E3.
9. **Reservation + immutable bytes cover both incidents?** YES - the
   three-path atomic set reservation before RNG (R3 lesson) plus the
   new explicit rule that frozen result/sentinel artifacts are
   excluded from formatting hooks with immutable raw bytes (the R4-D
   line-ending lesson, adopting my preventive recommendation).
10. **A pass cannot open G2?** YES - claim boundary explicit (finite
    M [comp] on flat controls; no rank four, Lorentzian form, tetrad,
    spin, curvature, stress-energy, Einstein dynamics); the two
    failure-branch successors (fractional dual certificate via
    AtlasFractionalPackingDual, or annular/disjointness candidate
    redesign) are correctly fenced outside R5.

## R1 (blocking, one wording fix)

Selector step 2 reads "maximize marginal primary-rung bulk coverage".
For the PRIMARY rung this is correct and R4-verbatim; but the
DIAGNOSTIC rung recomputes "the same candidate, selector, cap, and
metric definitions" on its own rung, and a literal reading of step 2
would have the 1.00 selector score marginal coverage against the 1.25
bulk - breaking R4-parity of the negative control (R4's 1.00 selector
scored against the 1.00 bulk). Replace with "maximize marginal bulk
coverage of the rung under evaluation" (or equivalent). One word-level
edit; no design change; prevents an implementation dispute later.

On adoption, send the revised plan hash for a one-grep confirmation and
treat this review as PLAN-CLEARED; implementation may then proceed to
the source/hash audit per the plan's own sequence.
