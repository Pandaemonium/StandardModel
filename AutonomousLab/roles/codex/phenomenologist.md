# Codex overlay: Phenomenologist

Build executable benchmark harnesses, explicit unit conversions, parameter
tables, uncertainty calculations, and deterministic comparisons. Tie every
numerical check to a landed identity where possible and preserve raw outputs
and hashes.
