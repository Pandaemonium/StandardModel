# Visionary synthesis: compose the analytic spine

- Activation: `role-20260713-032713-0f407b26`
- Model/role: Codex / Visionary
- Horizon: next harvest cycle through five years
- Live proof fleet: eight active Aristotle jobs, reconciled 2026-07-13

## Changed strategic picture

The continuum program has crossed an architectural threshold. The exact Dirac
momentum multiplier is now a representative-safe `L2` isometry, its fixed-state
time orbits are strongly continuous, and the generic pointwise lift has a
quotient-safe composition theorem. Four jobs are attacking the remaining
analytic interfaces: the exact time-group law, the pointwise generator,
Schwartz preservation, and Fourier differentiation with the explicit `2*pi`
normalization. These are not four independent publications. If they land,
their highest value is one composed theorem that identifies a strongly
continuous unitary momentum flow and then transports its generator to a stated
position-space domain.

The other active lanes now serve as discriminating comparisons. The modular
lane asks whether Pluecker phase has operational content beyond basis
covariance. The A/E classification asks whether two attractive finite pictures
are canonically related or only coordinatized alike. The Lorentz-in-distribution
lane asks whether finite null support can avoid selecting a preferred frame
without decorating away the claim. These are three versions of the same
scientific discipline: preserve only structure that survives a precise change
of description.

## Ranked decisive gates

### 1. Exact `C0` unitary-flow capstone

Objective: compose the live `L2` isometry, strong orbit continuity, and pending
time-group law into an exact strongly continuous unitary representation of
`Real`. Then, on an explicit Schwartz or weighted-`L2` domain, identify its
generator and transport it through Fourier transform with the repository's
actual sign and `2*pi` convention.

Dependencies:

```text
time-group law + generic L2 composition + L2 zero-time identity
  -> exact L2 group law
  + strong orbit continuity
  -> C0 unitary flow
  + pointwise generator + temperate growth + Fourier derivative
  -> stated-domain position-space Dirac generator
```

First cheap test: immediately after `0704b7da` returns, prove the quotient-safe
`L2` group law using the already landed generic composition API. Do not wait for
the harder Schwartz job.

Payoff: this is the shortest route from finite momentum identities to a genuine
continuum dynamics theorem. Failure would also be valuable because it would
locate the exact representative, domain, or normalization obstruction.

Kill/pivot: if the Fourier-side derivative cannot be closed on a displayed
dense invariant domain, publish the `C0` momentum-flow theorem at that scope and
open a separate analysis-foundations project. Never rename modewise or `L2`
transport as the position-space PDE.

Conventional alternative: the standard free Dirac unitary group. The null-edge
construction earns significance only by deriving the same flow from its finite
update and controlling the approximation, not by restating the known group.

### 2. Operational phase discriminator

Objective: turn the pending arbitrary-phase operator maximum-entropy theorem
into one observable that distinguishes the Pluecker-origin theory from an
arbitrary inserted complex mass.

Dependencies:

```text
phase-covariant Gibbs/modular selection
  + full-Fock pair exponential
  + spatial phase-link or loop data
  -> gauge-invariant two-particle response
  -> comparison with an inserted scalar mass control
```

First cheap test: compute a two-step or one-loop pair-transfer amplitude for two
phase profiles with equal `|z|`, one globally removable and one with nontrivial
link holonomy. State exactly which probability or interference term differs.

Payoff: a positive result would move the phase claim from derivational language
to operational physics. A negative result would prevent years of treating
basis data as ontology.

Kill/pivot: if every finite preparation-and-measurement probability depends
only on `|z|` after allowed local basis changes, classify the phase as
bookkeeping in the current model and redirect topology work toward genuine
patched defects or interactions.

Conventional alternative: an ordinary Dirac mass with a removable constant
chiral phase and gauge connections supplied independently.

### 3. Description-invariance classification

Objective: use the A/E bridge result and Lorentz-in-distribution strategy to
separate canonical structure from chosen decoration across the program.

Dependencies:

```text
A/E invariant classification or no-canonicity theorem
  + finite-support Lorentz no-go
  + distributional Poisson construction with decoration control
  -> minimal-data reconstruction table
```

First cheap test: for every proposed bridge, list the structure needed beyond
the bare finite graph: grading, star, rank, tetrad/spin frame, measure, or
orientation. Attempt one automorphism counterexample before funding a positive
classification.

Payoff: a minimal-data theorem would clarify what the ontology actually
postulates and which symmetries are emergent. A no-canonicity theorem would be
equally useful by preventing coordinate coincidences from masquerading as
unification.

Kill/pivot: if arbitrary equivariant decorations can encode any desired frame
or bridge, the result has no explanatory force. Retain the finite mathematics
but remove claims of derivation from the bare causal structure.

Conventional alternative: start from a Lorentzian spin manifold or a standard
lattice action with the geometric and representation data declared as inputs.

## Cross-project opportunity

The reusable interface is not a universal word such as information, closure,
or decoding. It is a three-part contract:

1. an exact finite operator or stochastic update;
2. an invariant under the relevant change of description;
3. a controlled map from that invariant to an observable or continuum limit.

Continuum flow, modular selection, A/E classification, and Lorentz recovery can
share audit tooling and theorem architecture at this level without asserting
that their physical objects are already unified.

## Assumptions at risk

- The exact flow is supplied by the target Dirac Hamiltonian; the finite walk
  must still derive it as a limit rather than merely adopt it.
- Strong continuity does not identify an unbounded generator or its domain.
- A constant Pluecker phase is basis covariance, not an observable.
- Distributional Lorentz invariance may reside in the sampling law while each
  realization and its decorations retain preferred structure.
- Similar dimensions or block shapes do not make the A/E bridge canonical.

## Concrete kill test

Preregister the **equal-magnitude phase-profile test**. Construct two finite
pair-dynamics experiments with identical `|z(x)|`: one phase profile globally
removable and one carrying nontrivial patched link holonomy. Use the same
preparation and gauge-invariant measurement class. If all resulting
probabilities agree for every allowed experiment, then the present phase layer
has no operational content beyond a reparametrized mass and connection. That
outcome kills the strongest current phase-memory interpretation while leaving
the area/gap theorem intact.

## Recommended queue change

Reserve the next Aristotle slot released by `0704b7da` for the exact `L2`
time-group composition and `C0` capstone, using the landed generic composition
theorem and strong-continuity module. Reserve the next slot released by either
`06176494` or `3b1fe9d3` for the smallest stated-domain generator/Fourier
composition. Do not spend those two slots on additional entropy inequalities or
global Yang-Mills combinatorics unless the continuum compositions prove false
or fail their typechecking preflight.

This queue change concentrates the next harvest cycle on one scientific arrow:
finite unitary update to exact continuum dynamics. It still preserves the
phase, Lorentz, and bridge jobs as hostile controls on what the program is
allowed to call derived.
