# Codex Lab Manager cycle - 2026-07-13

## Executive verdict

The lab converted a vague request to think laterally about 3+1 into a
falsifiable two-route program with five new kernel-checked finite controls and
a fully refilled Aristotle fleet. The main scientific gain is not yet a 3+1
construction. It is a sharper frontier: anomalous Floquet topology can only be
relevant if projector-conditioned internal-sector shifts are accepted as
primitive null steps, and its boundary certificate must be relative or
half-space rather than a naive finite global flow.

## Throughput and quality

- Integrated five Aristotle returns after direct replay in the live repository.
- Added build-enforced axiom guards for the accepted headline statements.
- Targeted module builds passed; the large overnight axiom guard passed in
  411 seconds.
- Refilled the Aristotle registry to its cap of eight active jobs.
- Prevented three likely overclaims: an endpoint `T^3 -> SU(2)` degree is not a
  `pi_4` micromotion invariant; finite global transport trace cannot certify a
  boundary anomaly; the Weyl degree/Chern endpoint remains conditional.

## Operational bottlenecks

1. `OvernightTheoryAxiomGuard.lean` imports too much of the draft tree. A small
   guard edit triggers more than six minutes of replay and unrelated
   compiled-evaluator warnings. Split a dedicated `Floquet3Plus1AxiomGuard`
   lane from the broad overnight guard.
2. Focused Aristotle packages warn when `.lake` is absent even though they
   include the pinned manifest. The preparation helper should optionally run a
   dependency fetch/build or package a verified dependency cache.
3. Claude-family independent review is currently unavailable: the scripted
   CLI call returned `Credit balance is too low`. Per current lab policy, no API
   fallback should be used. Queue the review for the next interactive Claude
   Code session and do not mark the gate complete.
4. Strategy reports and proved theorem modules need distinct registry labels.
   An `integrated` strategy report must not look equivalent to integrated Lean.
   Add an artifact-kind field (`lean_theorem`, `counterexample`, `strategy`,
   `audit`) to `ARISTOTLE_JOBS.json` in a later infrastructure change.

## Process changes recommended

- Require every 3+1 job to name its topology category: local sphere charge,
  endpoint degree, gap-tagged census, or half-space boundary index.
- Require every finite boundary invariant to state where the compensating far
  boundary went and prove the corresponding limit or relative-index theorem.
- Treat "primitive null" as a versioned ontology choice. Record separately:
  spin-blind translation and internal-sector-conditioned translation.
- Keep the current harvest-first cadence. It produced more usable mathematics
  than launching broad full-repo jobs.

## Next manager checkpoint

Harvest the five new jobs, route each exact Lean result through live replay and
an axiom guard, then request interactive Claude Code review of the combined
route. Promote no claim beyond finite control, conditional topological bridge,
or open half-space theorem until those gates are complete.
