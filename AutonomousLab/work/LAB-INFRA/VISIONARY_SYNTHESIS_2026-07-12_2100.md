# Visionary synthesis: after the multiplier and YM decisions

- Activation: `role-20260712-210138-730ac5d1`
- Model/role: Codex / Visionary
- Horizon: next 24 hours through five years
- Decision sought: reallocate proof and synthesis effort around the changed
  dependency graph.

## The changed strategic picture

The strongest new fact is not another finite identity. The continuum lane now
has all three momentum-space layers under independent scope audit:

1. representative-safe arbitrary-`L2` changing-cell projection;
2. the actual scaled live walk converging to exact modewise Dirac evolution at
   cell centers; and
3. the continuously varying exact multiplier converging to the frozen-cell
   multiplier in total scheduled-cell energy.

This makes inverse-Fourier transport and position-space flow identification a
real critical-path gate rather than a distant aspiration. Conversely, the
Yang-Mills polymer item has produced a scientifically useful no-go: local
fibrewise bookkeeping is false at size seven. Its successor is now a global
labelled-tree recurrence problem with a mapped primary source, but it is not on
the shortest path to deciding whether the finite walk reconstructs physics.

## Allocation change for the next 24 hours

### Track A: reconstruction first (50%)

Objective: turn the integrated momentum-space convergence stack into an honest
position-space theorem.

Dependency chain:

```text
Mathlib Fourier-Plancherel API map
  -> spinor-valued L2 representation bridge
  -> unitary transport of the existing error theorem
  -> exact multiplier = position-space Dirac flow on a displayed domain
  -> nonzero wave-packet control and cross-family semantic audit
```

First cheap test: determine whether the existing embedded fields and Mathlib's
Fourier transform inhabit definitionally compatible Hilbert spaces. If not,
state the smallest explicit isometry before attempting a PDE theorem.

Kill/pivot: if a representative-safe vector-valued Fourier bridge requires
substantial new analysis, publish the momentum-space convergence theorem at its
current scope and open a dedicated analysis-foundations project. Never rename
the current theorem as PDE convergence.

Conventional alternative: standard Fourier spectral convergence and quantum
walk limit arguments. The null-edge program must match their theorem strength,
not merely share their dispersion relation.

### Track B: derived dynamics, not inserted dynamics (30%)

Objective: finish the two exact bridges still separating the full-Bloch
maximum-entropy theorem from the repository's actual pair generator.

Dependency chain:

```text
radial entropy = spectral von Neumann entropy
  + pair Bloch state = normalized Gibbs state of canonical B_z
  -> full operator-level modular selection theorem
  -> phase-sensitive two-particle observable
```

First cheap test: typecheck the spectral multiset identity without choosing an
ordered eigenbasis. This attacks the current API blocker directly.

Kill/pivot: if the final theorem only restates a generator supplied in the
energy constraint, classify it as a representation theorem. The next physical
question then becomes why that constraint or ensemble is selected.

Conventional alternative: ordinary finite-temperature quantum mechanics. The
program earns novelty only through a derived constraint, phase-sensitive
observable, or universality statement.

### Track C: bounded global-combinatorics investment (20%)

Objective: state one finite global planar-tree recurrence for
`GAUGE-YM-EGF-001`; do not chase the full proof yet.

First cheap test: exact single-polymer coefficients through degree seven,
including the known `(3,3)` local overshoot and global compensation.

Kill/pivot: if the unchanged target fails globally or the Fernandez-Procacci
map needs hypotheses absent from the live finite system, retain the conditional
polymer criterion and redirect the gauge lane toward locality/index/anomaly
results already closer to physics.

Conventional alternative: cite the standard cluster-expansion theorem at the
manuscript boundary while formalizing only the finite inputs the program adds.

## One-quarter synthesis

The next-quarter architecture should be organized around one reconstruction
spine, not around channel count:

```text
finite local unitary dynamics
  -> continuum one-particle flow
  -> local fermionic second quantization
  -> one phase-sensitive interaction/scattering observable
  -> gauge-covariant local family and anomaly/index control
  -> stress/response dictionary for geometry
```

Every side theorem should state which arrow it enables. Results that enable no
arrow can still be published as mathematics, but should not consume the core
reconstruction budget.

The decisive next-quarter payoff is a complete, reproducible free-fermion
reconstruction theorem plus one genuinely many-body observable. That would
change the program from a collection of finite mechanisms into a candidate
microscopic regulator with operational content.

## One-to-two-year architecture

Build a regulator-independent comparison program with at least three families:

- the preferred finite null-edge walk;
- a conventional Wilson/overlap lattice control;
- a random or distributionally Lorentz-invariant causal structure.

Measure which structures survive continuum and coarse-graining limits: phase,
index, locality radius, dispersion error, entropy production, and interaction
response. The goal is to identify a universality class or kill the claim that
the null ontology adds explanatory content.

## Moonshot and cheap falsifier

Moonshot: a reconstruction theorem in which particles and geometry are stable
decoder sectors of one finite causal information process.

Cheap falsifier: complete the smallest phase-sensitive two-particle process and
ask whether its observable content can be removed by a local basis change or
reproduced by an arbitrary inserted complex mass. If it can, the proposed
assembly phase is bookkeeping, and the five-year ontology must pivot.

## Recommendation

Promote `CONT-FOURIER-001` to the highest dependency-ready research item. Keep
`DYN-MODULAR-001` active until its two explicit bridges land. Keep
`GAUGE-YM-EGF-001` literature-mapped and bounded to a typechecking recurrence
plus degree-seven controls before any new Aristotle submission.

This allocation maximizes information gain: one success establishes a genuine
continuum reconstruction rung; one failure identifies the exact analytic gap;
and neither outcome is diluted by spending the fleet on a combinatorial theorem
whose current role is supportive rather than decisive.
