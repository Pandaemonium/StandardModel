# Archivist report - 2026-07-13T23:2x (activation role-20260713-232138)

- Model/role: claude / Archivist (solo mode, active=claude)
- Focus: source provenance + knowledge-graph state for the anomalous-Floquet
  flagship (`QCA-3PLUS1-001`), the stack I reviewed all session.
- Scope discipline: findings recorded as source-debt with exact remediation; I did
  NOT unilaterally run multi-service (Zotero+Neo4j+embed) writes on the
  codex-owned, paused `ARCHIVE-BASELINE-001` baseline.

## 1. Literature / provenance delta

- **Anomalous-Floquet anchor set is well-recorded in `Sources/Null_Edge_References
  .md`** (section "Anomalous-Floquet 3+1 route", lines 207-222): all five anchors
  present with verification tags -
  `1806.06868` (HNU / Higashikawa-Nakagawa-Ueda, existence proof),
  `2006.04204` (Bessho-Sato, NN-with-bulk-topology),
  `1212.3324` (Rudner AFAI, 0/pi edge modes + bulk-edge),
  `hep-lat/0105032` (Kikukawa, exact-gauge domain wall),
  `2502.03045` (Aoki-Fukaya-Kan, spherical domain-wall + doubler-relocation).
- **In-file provenance of tonight's modules:** PlueckerHNUIntertwiner (1),
  HNUGlobalZeroPiChargeLedger (1), HalfSpaceDefectIndex (2 job IDs),
  GateYM/RootedTouchSum (1) - all carry provenance. **HNUManyStepContinuum: 0** -
  but it is still a candidate (my review recommended explicit-bridge integration;
  not yet in the live tree), so no in-file provenance to check yet. SOURCE-DEBT
  item S4 below covers it on integration.

## 2. Source-debt list (ranked)

- **S1 (HIGH) - three anchor papers missing from the Neo4j graph.** Cross-check of
  the six AF anchors against `Paper` nodes (by `arxiv_id`, confirmed by title
  search): PRESENT = `2006.04204` (key RCSSD8MZ), `hep-lat/0105032` (N66TWUXG),
  `1610.01142` (TN53N8J2). MISSING = **`1806.06868`** (HNU - the primary
  single-Weyl-via-Floquet existence proof, the paper that makes the whole route
  non-vacuous), **`1212.3324`** (Rudner AFAI - 0/pi edge modes + bulk-edge
  correspondence), **`2502.03045`** (Aoki-Fukaya-Kan - the doubler-relocation
  warning). These underpin the flagship yet cannot be reached by graph traversal
  or chunk/semantic search. Remediation: ingest via
  `Scripts/lit/lit_ingest.py` (Zotero + Neo4j + embed), adding the `IN_COLLECTION`
  edge to `9W59V3K9` so scoped null-edge search reaches them; then confirm with
  `neo4j_paper_search.py --chunks`. Left for the owner / next collaborative
  session (multi-service write on a paused-owner item; Zotero search is a known
  degraded path).
- **S2 (MEDIUM) - duplicate reference entry for `1610.01142`.** Two rows in
  `Null_Edge_References.md`: `TN53N8J2` (line 150, KEYED-LOCAL) and
  `TBD-FosterJacobson2016-4D` (line 174, `[CRITICAL]`, which itself notes "already
  keyed TN53N8J2"). One paper, two keys. Canonical = bare Zotero key `TN53N8J2`
  (per AGENTS.md dedup rule). Remediation: fold the richer line-174 metadata
  (the `[CRITICAL]` must-cite note + the section 2a elevation) into the `TN53N8J2`
  row and remove the `TBD-` duplicate. The GRAPH is already clean (single
  `TN53N8J2` node) - the duplication is only in the refs file.
- **S3 (LOW) - reverse gap: a relevant graph paper may be uncited in AF refs.**
  `2006.04634` ("Chiral Floquet systems and quantum walks at half period", key
  `JNAZIEJ9`) is in the graph and is directly relevant to the 0/pi / half-period
  structure of the HNU walk, but does not appear in the AF anchor table. Consider
  adding it as an anchor (it is prior art for the half-period quantum-walk
  framing that HNUExactCore's endpoint realizes).
- **S4 (LOW) - HNUManyStepContinuum provenance on integration.** When the O(1/n)
  continuum result lands via the explicit bridge, its docstring must record
  provenance: Trotter-Kato strong convergence (the conventional control, per my
  review's boundary 6) + the live HNU endpoint as the one-step map. No external
  paper is copied (clean-room), but the Trotter-Kato comparison must be named.

## 3. Identifier deduplication record

- Scanned all `arxiv_id`-shaped tokens in `Null_Edge_References.md` for repeats.
  8 IDs appeared more than once; triage: `2502.03045` = within-line double-mention
  (code-span + "arXiv:" prose), not a dup; `2401.03705` = legitimate cross-
  reference (own entry at line 99 + cited as supporting content in the
  Chamseddine-Connes provenance at line 130); **`1610.01142` = TRUE duplicate
  entry** (S2 above). Remaining repeats not individually opened but follow the
  same double-mention pattern; no other divergent-metadata duplicate surfaced in
  the spot-check.
- Canonical-key hygiene: the live graph uses bare Zotero keys (RCSSD8MZ, N66TWUXG,
  TN53N8J2, JNAZIEJ9) - consistent with the `paper_key` = bare-key rule. The
  `TBD-...` placeholders in the refs file are pre-ingest markers, not graph keys.

## 4. Neo4j / Zotero / index maintenance report

- Neo4j: READ access healthy (via `neo4j_graph` read-cypher). Ran two exact
  queries (arxiv_id membership + title CONTAINS) to establish the AF anchor
  coverage above. No write performed.
- Zotero: not exercised this pass (known-degraded search path; ingest deferred to
  S1 remediation).
- Index: `Null_Edge_References.md` is current for the AF route (five anchors,
  verified 2026-07-13); the one structural defect is the S2 duplicate.
- Graph coverage metric for the flagship: **3/6 AF anchors in-graph** (50%); the
  three missing (S1) are the highest-value ones. Closing S1 is the single most
  useful archival action for `QCA-3PLUS1-001`.

## 5. Recommended next archival actions (for the owner)

1. Ingest S1's three papers (`1806.06868`, `1212.3324`, `2502.03045`) with the
   `9W59V3K9` collection edge; re-run chunk search to confirm reachability.
2. Merge the S2 duplicate into `TN53N8J2`.
3. Evaluate S3 (`2006.04634`) for addition to the AF anchor table.
4. Attach S4 provenance when HNUManyStepContinuum integrates.
