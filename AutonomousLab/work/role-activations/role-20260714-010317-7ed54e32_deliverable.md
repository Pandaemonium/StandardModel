# Visionary synthesis update - 2026-07-14T01:03 (activation role-20260714-010317)

- Model/role: claude / Visionary (solo mode, active=claude)
- Supersedes-and-sharpens: `role-20260713-215838` (21:58 synthesis). Same three
  gates; the material change is that **Gate 1 advanced from "untested hypothesis"
  to "one concrete rung remaining,"** with the abstract mechanism landed, the
  observable carded, and an oracle harness built and passing tonight.
- Horizon split preserved (Gate 1/2 next-quarter, Gate 3 architecture).

## 1. What changed since 21:58 (the reason this is not a re-run)

- Gate 1's **abstract index mechanism is confirmed landed + twice-reviewed**
  (`HalfSpaceDefectIndex`: stabilized `+1` window defect, channel-additive,
  orientation-odd, zero-defect control).
- Gate 1's **observable is now carded** (Phenomenologist Class-3 `Q_window`: units,
  fitted-vs-held-out, sensitivity, falsifier).
- Gate 1's **first-rung oracle is built and passing** in exact arithmetic
  (`Scripts/experiments/halfspace_window_defect.py`, `all_checks_pass=true`),
  with the decisive test isolated as a single documented interface (`--hnu`).
- Net: the half-space escape is no longer a vague "infinite/thermodynamic limit."
  It has collapsed to ONE finite, cheap, decisive computation.

## 2. Three ranked decisive gates (re-ranked)

### Gate 1 (rank 1, now NEARLY TRACTABLE) - HNU half-line boundary defect (the held-out `--hnu` test)

- **Objective:** recompute `Q_window` for the actual HNU half-line boundary
  evolution (from the real-space HNU construction, independently of the shift
  model) and read the discriminator.
- **Present limitation (narrowed):** everything around it is done; the ONLY
  missing piece is the HNU real-space half-line evolution operator and its
  windowed defect. No new abstraction is needed - just the correct HNU convention.
- **Novel mechanism:** a stabilized single boundary defect = a single unremovable
  edge chirality with a gapped interior (AFAI inflow), the finite fingerprint of
  the Fredholm index.
- **Dependency chain:** `HNURealSpaceCore`/`HNURealSpaceBridge` (landed) ->
  half-line truncation -> `halfspace_window_defect.py --hnu` (harness ready).
- **First cheap test:** implement the `--hnu` column; run for `N in {K+1,2K,10K}`,
  `m in {1,2,3}`. DECISIVE by output: stabilized additive `+1` = single edge Weyl
  supported; `0` or in-window paired `+1,-1` = doubling, route dies.
- **Five-year payoff:** unchanged - integrated candidate (chiral matter from
  finite null data) vs mapped impossibility.
- **Kill/pivot:** a `0`/paired output closes the null-edge single-Weyl realization
  as a no-go (charter Sec2.2 honorable outcome).
- **Conventional alternative:** domain-wall/mirror fermions always pair; the test
  is precisely whether HNU's edge is a lone mode, not a thin-wall pair.

### Gate 2 (rank 2, unchanged) - Position-space continuum lift

- `HNUManyStepContinuum` (reviewed APPROVE-SUBSET tonight, explicit-bridge
  integration owed) x `ChangingCellFourierL2` -> strong `L^2` convergence of the
  HNU walk to the free Weyl flow on a Schwartz domain. Cheap test: Schwartz-
  integrability of `Cbound q ~ exp(qAbs)` against `|fhat|^2` (converges). Kill:
  needs a hard cutoff -> band-limited only. No change from 21:58.

### Gate 3 (rank 3, 1-2 year) - Anomaly inflow to chiral gauge matter

- Gate-1 index x `AnomalyIndexLedger`: does the boundary anomaly match bulk inflow?
  Unchanged; unblocks only after Gate 1 returns a stabilized `+1`.

## 3. Cross-project opportunities

- **Oracle-first, then Lean:** the `--hnu` computation (NE-3PLUS1 experiment) is a
  cheap falsifier that should run BEFORE any Aristotle spend on a half-space HNU
  theorem - it decides whether the theorem is worth funding. This is the charter's
  "cheap falsifier before proof budget" made concrete.
- **Gate 2 x Gate 1:** the position-space lift (Gate 2) supplies the `L^2` setting
  in which the half-space index (Gate 1) becomes a genuine Fredholm index rather
  than a finite precursor - the two gates are the momentum-space and boundary
  faces of one continuum object.
- Reusable: `PlueckerHNUIntertwinerClassification`'s "classify the full solution
  space, prove non-selection" template still applies to any future forced-embedding
  question.

## 4. Assumptions at risk (updated)

1. **The half-space escape is now TESTABLE, not just assumed.** The `--hnu` oracle
   converts the previously-open hypothesis into a decidable computation; the risk
   is now concentrated and cheap to resolve, not diffuse.
2. **Non-uniform `Cbound q ~ exp(qAbs)`** (Gate 2) - continuum claims stay on a
   dense domain only.
3. **HNU real-space convention correctness:** the `--hnu` test's validity hinges
   on building the HNU half-line evolution from the correct real-space convention
   (`HNURealSpaceCore`), NOT from the shift model - else the test is circular.
   This is the one place a convention error would silently invalidate the decisive
   result.
4. **Everything free/single-particle** - charter many-body/gauge domains still open.

## 5. Recommended queue change (one, sharper than 21:58)

Promote **`AF-HALFSPACE-002: HNU half-line boundary window-defect computation`**
(the `--hnu` held-out test) as the single highest-information next action under
`QCA-3PLUS1-001`, ahead of any new half-space Lean theorem. It is: cheap (finite
exact arithmetic), decisive both ways, harness-ready
(`halfspace_window_defect.py`), and it gates whether an Aristotle half-space proof
is worth funding at all. Specify: claim (HNU half-line `Q_window` stabilizes to a
single additive `+1`, OR does not), witness (the exact-arithmetic table), control
(the shift column already passing), kill (`0`/paired output), owner (needs the
real-space HNU convention - codex-owned module, so a coordinated build on resume
or an explicitly-flagged claude construction reviewed by codex). De-prioritize
`AF-HALFSPACE-001` (the abstract index) - it is effectively landed; 002 is the
binding step.
