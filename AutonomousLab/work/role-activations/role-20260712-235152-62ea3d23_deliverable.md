# Impact audit: the 2026-07-12 information-theory / DYN-MODULAR results

- Activation: `role-20260712-235152-62ea3d23` (claude / Impact Strategist)
- Item context: `DYN-MODULAR-001`, `LAB-INFRA`
- Rule: maximize legitimate impact WITHOUT changing evidence grades.

## Strongest honest lede (one sentence, grade-faithful)

"A continuous-functional-calculus-free Lean 4 formalization of the general
(non-commuting) quantum Klein inequality and the spectral matrix logarithm,
proved without the operator-convexity infrastructure that Mathlib v4.28 lacks,
by reducing the two-basis cross term to a doubly-stochastic scalar Jensen bound."

What this lede is NOT (kept out of the headline on purpose): it is not a new
mathematical result (Klein's inequality is ~1930s), not a physics discovery, and
not evidence for the null-edge ontology. The contribution is FORMALIZATION +
technique, and the lede says so.

## Impact-ladder rung actually reached

**Rung 2-3** (reusable theorem/tool, and it resolves a concrete formalization
obstacle). Not rung 4-5: it does not reframe a community's problem or yield a
distinctive physical prediction.

- Rung 2: `GeneralQuantumKlein.qKlein_nonneg`, `logHermitian`,
  `entropy_trace_eq_sum`, `scalar_klein`, `cross_trace_eq_sum` are reusable,
  kernel-clean, and immediately gave `GeneralMaxEntropy.vonNeumann_le_cross_entropy`.
- Rung 3 (the genuinely interesting part): the doubly-stochastic overlap
  reduction discharges the non-commuting inequality using ONLY scalar
  `Real.log` concavity, sidestepping the absent operator-convexity/Lieb/CFC
  machinery. That is a recognized obstacle for anyone formalizing matrix
  entropy in current Mathlib.

## Nearest-work comparison

- Mathematics: Klein's inequality / quantum relative-entropy nonnegativity is
  standard (Nielsen-Chuang; Carlen's survey). No novelty claimed.
- Formal libraries: Mathlib v4.28 has `Matrix.IsHermitian` spectral theory but
  NO `Matrix.log`, no matrix entropy, no operator convexity. The nearest formal
  artifacts are the scalar `strictConcaveOn_log_Ioi` and `Real.log_le_sub_one`.
  The delta this work adds: a CFC-free spectral `logHermitian` and the first
  (to our knowledge, in Mathlib's ecosystem) general quantum Klein inequality.
- Within the program: it strictly generalizes the earlier commuting
  `QuantumKleinShared` and removes the `Fin 2` scope of the DYN-MODULAR qubit
  operator-S2 capstone.

## Target community (in priority order)

1. The Lean/Mathlib formalization community (matrix analysis / information
   theory) -- this is where the artifact is genuinely wanted and missing.
2. Quantum-information researchers who want machine-checked entropy inequalities
   or a reusable CFC-free matrix-function pattern.
3. The null-edge program internally (as infrastructure), NOT as an external
   headline.

## Decisive result needed for real external excitement

For a formalization audience the bar is essentially met; the decisive ACTION is
upstream contribution. For a wider audience the honest bar is a **distinctive,
non-fitted prediction or a resolved recognized open problem**, which none of
today's results reach -- and the audit must not pretend otherwise. The
program's most externally-legible open target remains a held-out prediction or
the mapped-impossibility frontier (Branch B), not another entropy inequality.

## Grade-faithful publication action

- DO: prepare a focused Mathlib PR (or PR-ready gist) for `logHermitian` +
  `entropy_trace_eq_sum` + `qKlein_nonneg`, cleaned to Mathlib style (remove the
  `set_option maxHeartbeats` heavy step if a lighter `Finset` reindex exists;
  drop the unused-simp-arg lints). This is the highest-leverage, grade-faithful
  move: it converts a draft-lane repo theorem into community infrastructure.
  ROUTE THROUGH THE DIRECTOR (external contribution = DQ decision; author/AI-use
  disclosure required).
- DO: keep it in the draft/infra layer of the manuscripts as a cited tool, NOT
  as a headline result.
- DO NOT: submit any of today's results to a physics venue as a discovery, name
  Klein's inequality as new, or let a manuscript headline imply the null-edge
  ontology gained empirical support. No claim inflation to reach a venue.
- Separate centerpiece from infrastructure: the DYN-MODULAR qubit capstone
  (integrated) and the general Klein are INFRASTRUCTURE for the modular-selection
  story; they are not themselves the program's centerpiece claim.

## Director-queue note

Recommend a DQ entry: "Upstream the CFC-free quantum Klein / spectral matrix-log
lemmas to Mathlib?" (default: keep internal). This is a human-only decision
(external contribution, authorship, AI-use disclosure).
