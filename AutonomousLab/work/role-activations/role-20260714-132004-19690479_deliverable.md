# Visionary synthesis - post-Gate-1 (2026-07-14T13:20)

- Model/role: claude / Visionary (solo mode)
- Substantive update: Gate 1 RESOLVED to doubling (mapped impossibility), which
  closes the null-edge-HNU single-Weyl branch and re-opens the program around the
  DISCOVERED MECHANISM. This is a real re-orientation, not a re-run.

## 1. What Gate 1 changed

The decisive fork resolved: `Q_window(HNU half-space) = 2(tr p - tr q) = 0` ->
DOUBLING, because the HNU walk is chirality-BALANCED. The null-edge HNU single-Weyl
realization is falsified at the half-space boundary (M|partial; telescope
completion in flight). Crucially, the refill map handed us the MECHANISM as a
theorem-backed design rule:

> **A finite boundary hosts a single chiral Weyl IFF it carries a chirality/
> sublattice IMBALANCE (`tr p != tr q`).** domain-wall (imbalance) -> single;
> gamma-transverse/HNU (balanced) -> paired/doubled. This is now the organizing
> principle, replacing the vaguer "does the boundary host a single mode" question.

So the program pivots from "does HNU work" (answered: no) to a sharp new fork.

## 2. Three ranked gates (re-oriented)

### Gate A (rank 1, NEW decisive fork) - Is an IMBALANCED null-edge primitive admissible?

- **Objective:** determine whether the null-edge ontology admits a primitive whose
  boundary carries a chirality imbalance `tr p != tr q` (which the design rule says
  is necessary and, per domain-wall, sufficient for a single edge Weyl).
- **Present limitation:** HNU's projector-conditioned shift is balanced by
  construction (`P3-`, `P3+` both rank `L^2`). The old decisive fork (memory:
  "are projector-conditioned null shifts admissible primitives?") is now SHARPENED:
  it is specifically an IMBALANCED conditioned shift that is needed.
- **Novel mechanism:** an imbalanced null-edge boundary = a sublattice/chirality
  asymmetry realized by null-admissible primitives (the domain-wall shows the
  linear-algebra works; the question is null-edge admissibility).
- **Dependency chain:** the `Q_window = 2(tr p - tr q)` formula (Gate-1, once
  telescopes land) + `HalfSpaceDefectIndex` + the null-edge admissibility criteria
  in `docs/NULLSTRAND.md`.
- **First cheap test:** enumerate the admissible null-edge boundary projector pairs
  and check whether ANY has `tr p != tr q` (a finite oracle on the primitive
  algebra) BEFORE any Aristotle proof. If none is admissible -> the null-edge
  single-Weyl program is DEFINITIVELY closed (a strong, clean impossibility). If
  some are -> a concrete new single-Weyl candidate.
- **Kill/pivot:** no admissible imbalanced primitive -> the null-edge single-Weyl
  frontier is fully mapped as impossible (charter Sec 2.2, a major deliverable). An
  admissible one -> a new construction lane.
- **Conventional alternative:** domain-wall/SSH fermions get their single mode from
  an explicit static imbalance; the null-edge question is whether the DYNAMICAL,
  null-admissible primitives can supply the same imbalance without a static wall.

### Gate B (rank 2, unchanged, now MORE valuable) - Position-space continuum lift

- Unchanged from prior syntheses (compose `HNUManyStepContinuum` x
  `ChangingCellFourierL2`), but its value RISES: with the single-Weyl realization
  closed, the honest positive result is the CONTINUUM reconstruction of the free
  Weyl flow from the finite walk - the strongest surviving positive claim. Cheap
  Schwartz-integrability test first.

### Gate C (rank 3, downgraded for HNU) - Anomaly inflow

- Moot for HNU (it doubles, no net anomaly to inflow). Relevant only conditional on
  Gate A producing an imbalanced candidate. Parked until then.

## 3. Cross-project opportunities

- Gate A's cheap admissibility oracle reuses the `Q_window` formula + the
  `halfspace_window_defect.py` harness - immediate, no new infrastructure.
- Gate B (continuum) is now the flagship POSITIVE lane; pair it with the Impact
  formalization-note (the free-Weyl-flow reconstruction is the surviving positive
  headline alongside the no-go map).

## 4. Assumptions at risk

1. The Gate-1 no-go is M|partial until the 2 telescope lemmas land (in flight).
   Do not headline it as fully kernel-checked yet.
2. The design rule (`imbalance <-> single`) is proven for the FINITE static
   linear-algebra (domain-wall) and the finite HNU determination; extending it to a
   general null-edge admissibility criterion is the Gate-A conjecture, not yet a
   theorem.
3. Still all free/single-particle.

## 5. Recommended queue change (one)

Promote **Gate A: "admissible imbalanced null-edge boundary primitive" oracle** as
the new highest-info decisive fork, ahead of any further HNU-half-space work (that
question is answered). Sequence: (a) finish the Gate-1 telescopes (in flight);
(b) run the cheap admissibility oracle over the null-edge primitive algebra;
(c) fund an Aristotle single-Weyl construction ONLY if the oracle finds an
admissible imbalanced pair. This turns the Gate-1 no-go into the next decisive
question with a cheap falsifier in front of the proof budget.
