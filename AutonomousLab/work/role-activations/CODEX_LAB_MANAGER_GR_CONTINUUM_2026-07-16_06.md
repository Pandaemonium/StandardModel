# Codex Lab Manager: bare-graph GR cycle restoration

Date: 2026-07-16 05:46-05:52 Pacific  
Activation: `role-20260716-054651-65d7ed88`  
Project: `NE-GRAVITY-SCALE`

## Mandate

Restore valid collaborative operations after the expired 2026-07-14 solo
interval and register the Research Director's bounded request to work on a
continuum-GR derivation from a bare graph through 18:00 Pacific.

## State actions

1. `labctl.py validate` passed before mutation.
2. The expired Claude solo interval was first replaced by the requested Codex
   interval, then immediately reset to collaborative mode when the Research
   Director reported that interactive Claude had become active.
3. Claude availability was recorded as `available`; no unread Codex mailbox or
   independent-review backlog was present at startup.
4. Began cycle `GR-CONTINUUM-2026-07-16`, bounded to approximately 18:00
   Pacific.
5. Parked `ARCHIVE-BASELINE-001` without changing its scientific status. This
   frees one Codex execution slot while preserving its accepted inventory and
   source-debt queue.
6. Registered and started `GRAV-ORDER-SUPPORT-001`, with Codex as Scientist and
   Claude as independent Skeptic.

## WIP audit

After the redirect, Codex owns three executing items:

- `QCA-3PLUS1-001`;
- `CONT-FOURIER-001`;
- `GRAV-ORDER-SUPPORT-001`.

This exactly meets, but does not exceed, the per-model limit. Claude owns the
two inherited executing items `GAUGE-YM-EGF-001` and `EDU-OVERVIEW-001`.
The new GR item is an empirical control rung, not an additional moonshot.

## Scientific gate

`GRAV-ORDER-SUPPORT-001` tests one uncertainty only: whether the empty
largest-scale shell in Stage A3b is caused by finite diamond boundaries.
It fixes `ell`, `L`, the adjacent ratio, and all count bands while increasing
duration and event count proportionally to four-volume.

The success and kill conditions are frozen in `WORK_ITEMS.json`:

- exact packed-versus-dense equality is mandatory;
- ten realizations at exact four-volume multipliers `1`, `2`, and `4` are the
  bounded scan, preserving `ell` without integer-density drift;
- boundary truncation survives only if the largest-volume largest-shell
  median is positive and randomized-mark rank-capable availability reaches
  `0.80`;
- otherwise same-scale source interiority is killed before any generalized
  eigensolver.

No target metric, coordinate-selected mark, eigensolver, new dependency, or
GR promotion is authorized by this item.

## Fleet and review audit

The Aristotle registry reports six active jobs against a cap of eight and a
large harvested backlog. No duplicate GR proof job was submitted during this
manager activation because the present gate is numerical and its theorem
target has not yet been isolated into a typechecking package. The two free
slots should next receive GR-local targets only after the boundary result
identifies the exact surviving or failed statement.

Claude is available and is the registered independent skeptic. A durable
review request must be sent after the implementation, raw JSON, and claim-
graded report exist. Codex self-review cannot advance that gate.

## Risks and controls

- The repository is heavily dirty with unrelated user/model work. Git commits
  must stage explicit GR/lab paths only; no broad add, reset, cleanup, or
  reformat is allowed.
- The current all-pairs dense oracle is cubic. The new implementation uses an
  exact sparse relation product and proves equality against the dense
  definition on chain and sprinkled partial-order controls.
- Ten realizations per volume are enough for the preregistered kill gate but not for a
  continuum claim. Any positive result still requires independent review and
  a larger held-out protocol.

## Next controls

1. Pass packed-versus-dense unit and relabeling controls.
2. Run and archive the frozen three-volume benchmark.
3. Send the exact artifacts and commands to Claude for hostile review.
4. Update the GR ledger, state, handoff, and scoped git commits only after the
   result and review disposition are explicit.
