# Impact strategy: ranking the corrected 3+1 anomalous-Floquet routes

- Author: claude Impact Strategist (activation role-20260713-132131),
  item QCA-3PLUS1-001
- Inputs: my accepted HNU adversarial audit
  (`CLAUDE_ADVERSARIAL_HNU_RECONSTRUCTION_2026-07-13.md`), my Visionary BB ladder
  (`CLAUDE_VISIONARY_MICROMOTION_BOUNDARY_SYNTHESIS_2026-07-13.md`), the corrected
  route memo (sha d21c2c32), the MicromotionWinding rejection.
- Date: 2026-07-13

## Executive ranking

| Route | Publishable impact (ceiling) | Decisive gate proximity | Overclaim risk | Verdict |
|---|---|---|---|---|
| **HNU endpoint-winding + primitive-null verdict** | Medium-high | **NEAR** (L9 kernel-clean now) | **Low** (if scoped as reconstruction+verdict) | **LAND FIRST** |
| **Open-boundary bulk-edge (BB correspondence)** | **Highest** | Medium (BB2 needs the chiral-coin build) | **High** (many gates) | **HEADLINE FLAGSHIP** |
| **Finite magnetic-cell (pi-flux census)** | Low (control) | Landed | Low | **KEEP as control/companion** |

One-line recommendation: **land the HNU `L9` primitive-null no-go + census/charge
now** (near-term, low-risk, flagship-grade, and it is simultaneously the bulk half
of the BB fallback), then **pursue BB2 as the headline flagship**; keep the
magnetic-cell as the periodic-obstruction control. The three are not competitors
- they are a layered program.

## Per-route analysis

### 1. HNU endpoint-winding + primitive-null verdict - LAND FIRST

- **What it publishes.** A kernel-checked formalization of the HNU single-Weyl
  Floquet model (corrected symbol, exact trace/census/charge) PLUS the genuinely
  novel piece: a machine-verified verdict that UNCONDITIONAL (spin-blind) null
  microsteps provably cannot carry the anomalous winding (`L9`:
  `U=e^{-ik.m}W0 => winding 3-form ≡ 0`), so the escape REQUIRES
  projector-conditioned (internal-channel-selective) null shifts.
- **Decisive gate - NEAR.** `L1-L8` are finite matrix/trig facts; `L9` is
  kernel-clean (antisymmetry `eps^{ijk} m_i m_j m_k = 0` + trace algebra, no
  `native_decide`, no degree API). `L9` is reachable in one focused Aristotle job.
- **Overclaim risk - LOW**, IF scoped honestly: this FORMALIZES HNU's model (not
  ours) and DELIVERS a no-go + a precisely located escape hatch. The trap to
  avoid: calling the reconstruction "a Null-Edge 3+1 result" - it is a
  formalization of a published model plus an ontology verdict, and the
  single-Weyl realizability is GATED on the projector-conditioned-shift decision.
- **Why first:** it is the only route with a flagship-grade, kernel-clean,
  near-term decisive theorem (`L9`) that is ALSO honest, and it anchors the whole
  program on a verified foundation.

### 2. Open-boundary bulk-edge (BB correspondence) - HEADLINE FLAGSHIP

- **What it publishes.** The high-ceiling result: a FINITE, kernel-checked
  bulk-boundary correspondence `netChiralTransport = W_gap` - the bulk micromotion
  winding of a quasienergy gap equals the net chiral transport across an open
  boundary. This is the community-exciting theorem: it makes "single bulk species"
  a measurable boundary integer and locates the Nielsen-Ninomiya partner as
  anomaly-inflow edge transport. Novel to formalize; resolves "where did the
  partner go" concretely.
- **Decisive gate - MEDIUM.** BB1 control (`netChiralTransport(bare stepEquiv)=0`
  via the single-`2(N+1)`-cycle) is near-term; BB2 (the equality) needs the
  minimal chiral-coin construction and the transport-winding bridge. Not a
  one-job land.
- **Overclaim risk - HIGH.** Many gates (BB0 timeframe-invariance, BB2 equality,
  BB3 null-support, both 0/pi sectors, interior decoupling). The single-species
  headline is only earned when ALL pass; the fallback no-go (null+local => transport
  0 in every gap) must be reported honestly as the live alternative. This is the
  highest-reward, highest-risk route - pursue it AS the flagship, but gated.
- **Why second (not abandoned):** highest impact, but it must stand on the L9
  foundation; and its bulk half IS L9 (see the shared-core insight below).

### 3. Finite magnetic-cell (pi-flux census) - CONTROL/COMPANION

- **What it publishes.** An exact finite magnetic-translation doubling census - a
  CONTROL showing the periodic magnetic-cell route still doubles. Landed
  (`PiFlux3Plus1Census`, my REVISE->ACCEPT). It confirms the obstruction the
  anomalous route is designed to evade.
- **Decisive gate - LANDED**; **overclaim risk - LOW** (a finite census, once the
  earlier prose overreaches were fixed).
- **Why keep but demote:** it is the "why the naive periodic route fails" context
  that motivates the anomalous route. It is NOT a standalone headline (a control
  confirming a known obstruction excites no one on its own), but it is valuable
  scaffolding and should be cited as the control, not promoted as a result.

## The key strategic insight: the top two routes SHARE their decisive no-go core

The HNU `L9` (spin-blind bulk schedule => `W = 0`) and the BB fallback no-go
(null+local boundary => `netChiralTransport = 0` in every gap) are the SAME
finding on the two sides of the correspondence. So landing `L9` NOW:
- delivers the HNU primitive-null verdict (route 1's flagship theorem), AND
- establishes the bulk half of the BB fallback (de-risking route 2), AND
- is the finite AF5/NS-1 gate the whole program hinges on.
This is why the sequencing is efficient rather than a three-way bet: one
kernel-clean theorem advances both top routes at once.

## Honest publishable headline: earned-now vs gated-flagship

- **Earned now (after `L9` + census/charge):** "A machine-checked proof that the
  anomalous single-Weyl escape of the lattice Nielsen-Ninomiya obstruction cannot
  be realized by unconditional (spin-blind) primitive null microsteps - it
  provably requires projector-conditioned internal-channel-selective shifts - with
  the HNU model formalized as the witness." This is sharp, honest, and exciting to
  the lattice-fermion and formal-methods communities: a no-go + a precisely priced
  escape hatch. It does NOT claim a solved 3+1 Standard Model.
- **Gated flagship (after BB2 + interior decoupling + the ontology decision):**
  "A finite kernel-checked bulk-boundary correspondence realizing a single 3+1
  Weyl in a Null-Edge QCA, with the Nielsen-Ninomiya partner as anomaly-inflow
  boundary transport." This is the community headline - but it is gated on the
  projector-conditioned-shift ontological decision AND BB2 AND both-sector
  interior decoupling. Do not advertise it until those land.

## Per-route kill/gate conditions (pre-registered)

- HNU route dies as a Null-Edge result if the project DECIDES projector-conditioned
  shifts are NOT admissible primitives (then `L9` stands as a pure no-go: the
  Null-Edge alphabet cannot host the anomalous Weyl - still publishable, as a
  no-go).
- BB route dies if the fallback no-go is provable (every null+local open-boundary
  schedule has `netChiralTransport = 0`); then reroute to the aperiodic-bulk
  fallback or accept the no-go.
- Magnetic-cell has no kill (it is a landed control).

## Recommendation to codex

1. Fire the focused HNU ladder job: `L1-L8` (finite, corrected symbols, "endpoint
   winding" language) + `L9` (the kernel-clean no-go). Land `L9` first as the
   flagship-grade near-term result.
2. In parallel, build BB1 (`netChiralTransport` + the transport-0 control on
   `stepEquiv`) - cheap, and it sets up the headline.
3. Make the projector-conditioned-shift ontological decision explicit (it gates
   both the HNU escape and BB3); it is a program-definition call, not a proof.
4. Cite the magnetic-cell census as the control; do not promote it.
5. Publish the earned-now no-go headline; reserve the bulk-edge headline for after
   BB2.
