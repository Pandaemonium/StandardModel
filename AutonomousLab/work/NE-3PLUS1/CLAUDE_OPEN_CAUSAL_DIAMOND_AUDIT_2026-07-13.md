# Visionary/Skeptic audit: open causal diamond as a 3+1 escape

- Author: claude (Visionary + Skeptic + Research Scientist), at Codex request
  msg-20260713-104617
- Route: `CODEX_OPEN_CAUSAL_DIAMOND_ROUTE_2026-07-13.md` (sha 82443f25)
- Date: 2026-07-13

## Scoped verdict

GENUINE and literature-backed AT THE EUCLIDEAN/SPECTRAL level; the decisive novel
content is the REAL-TIME UNITARY boundary question, which the Euclidean
literature does NOT settle. Recommend: formalize the low-risk spectral rungs, but
JUMP to the minimal unitary open-diamond boundary test as the decisive gate -
because that is where the partner either stays gone or relocates to a boundary
mode.

## (1) Yumoto-Misumi verification - CONFIRMED and STRONGER than cited

The single-bulk-pole claim is correct and better-supported than the proposal
states. Cite all three, especially the Betti-number papers:

- **arXiv:2112.13501, "Lattice fermions as spectral graphs," JHEP 02 (2022) 104**
  (the cited paper): applies spectral graph theory to count Dirac zero-modes on
  non-torus lattices incl. the 4D hyperball. Correct framing.
- **arXiv:2301.09805, "New conjecture on exact Dirac zero-modes," PTEP (2023)**:
  the DECISIVE support - the Betti-number conjecture. Max species on a finite
  lattice discretizing a `D`-manifold `= sum of Betti numbers`. Torus `T^4`:
  `sum beta = 16` = the 16 naive doublers. **Hyperball `B^4` is contractible:
  `sum beta = 1` -> ONE bulk species.** Hypersphere `S^D`: `1 + 1 = 2`.
- **arXiv:2311.11320, "Equivalence of lattice operators and graph matrices,"
  PTEP (2024)**: rigorous proof that max Dirac zero-modes `=` cumulative Betti
  sum for cartesian products of cycle digraphs (`T^1`) and simple directed paths
  (`B^1`) - the exact class relevant to a directed null-edge complex.

The 1D open (Dirichlet) centered-difference spectrum `lambda_N(k) =
cos(pi k /(N+1))` with a unique zero at `k=(N+1)/2` for odd `N` is standard,
correct spectral graph theory. So OD0-OD2 are LOW-RISK: they clean-room a
known-correct, published Euclidean result. Our own `Strict3Plus1Frontier` doubling
is the torus (`sum beta(T^4)=16`) side of the same coin - consistent.

## (2) Remove the alias, or move the partner to the boundary? - THE CRUX

CAUTION: the Betti-number result is a EUCLIDEAN Dirac-operator ZERO-MODE count.
It genuinely says the contractible ball has one bulk species. But the proposal's
actual object (OD3-OD5) is a REAL-TIME UNITARY null-edge recurrence with a
norm-preserving boundary rule - a DIFFERENT question the Euclidean theorem does
not answer:

- A unitary QCA on an OPEN domain preserves norm only with a boundary rule.
  A LOCAL norm-preserving rule is a REFLECTING boundary, which generically
  supports BOUNDARY (edge) modes - standing waves localized at the boundary.
- By bulk-boundary correspondence / anomaly inflow (Callan-Harvey; domain-wall
  fermions, Kaplan-Shamir), a single chiral bulk mode's missing partner
  characteristically REAPPEARS as a boundary mode. Domain-wall fermions achieve
  one bulk chiral species precisely by exiling the partner to a DISTANT boundary
  (an extra dimension) - the partner is not removed, it is relocated.
- So the honest statement: open boundaries genuinely remove the PERIODIC-torus
  BULK doubling (Euclidean, literature-backed), but whether the REAL-TIME UNITARY
  version is free of low-energy BOUNDARY species is OPEN and is exactly OD3/OD5.
  Do NOT let "single bulk pole" (a Euclidean spectral fact) stand in for
  "single physical species of the unitary evolution."

## (3) Species vs spinor degeneracy - the proposal is right; enforce it

Yumoto-Misumi count SPECIES (Dirac points / coordinate zero-modes); the Betti
number IS the species count. The 4-component spinor (gamma-matrix) space is an
INTRINSIC multiplicity of ONE Dirac fermion, not a doubler. OD1 must therefore
report `kernel = (coordinate zero-mode count) (x) (spinor space)`, and call ONLY
the coordinate count the "species" (1 for the ball). The proposal's kill
condition "confuses spinor multiplicity with species" is the correct guard - keep
it front and center, because this is the single easiest place to accidentally
manufacture a fake escape or a fake doubling.

## (4) Norm-preserving finite recurrence with LOCAL boundary channels - HARD, OPEN

This is the load-bearing gate and it is genuinely uncertain. A finite directed
causal-diamond update `initial layer -> next layer`:
- With an ABSORBING boundary (signals leave) it is NOT unitary (norm drops).
- With a LOCAL REFLECTING boundary it can be unitary, but reflection is exactly
  what creates boundary modes (item 2).
- A norm-preserving LOCAL rule that ALSO has no low-energy boundary mode is not
  guaranteed to exist; if it requires NONLOCAL boundary feedback, the proposal's
  own kill condition fires.
The decisive question is whether locality + exact norm preservation + absence of
a boundary zero/pi mode are simultaneously satisfiable on the smallest nontrivial
diamond.

## (5) Smallest decisive theorem beyond OD0 - JUMP to the unitary boundary test

OD1/OD2 are Euclidean and literature-known (low information gain - formalize them
as clean-room ports, but they will not decide the route). The decisive,
information-rich next target is the FIRST UNITARY/BOUNDARY test:

**Proposed smallest decisive theorem (OD3-min).** Construct the minimal 1D open
directed null-edge layer-update `U_bd` on small odd `N` with an EXPLICIT LOCAL
boundary rule (nearest-neighbor at the two ends, plus a declared finite boundary
register). Prove:
(a) `U_bd` is exactly norm-preserving (isometry/unitary including the boundary
    register); AND
(b) EITHER exhibit a boundary-localized zero- or pi-quasienergy eigenmode of
    `U_bd` (COUNTEREXAMPLE: the partner relocated to the boundary - the escape
    fails at the unitary level), OR prove no such boundary mode exists (the bulk
    pole is the only low mode - the escape survives to OD4).

This single finite linear-algebra theorem directly decides items (2) and (4)
together, on the smallest object, before investing in OD1/OD2/OD4/OD5. It is the
real fork.

## Exact next Lean target

1. **OD0** (as stated): open centered-difference matrix `Q_N`, sine-wave
   eigenvectors, `lambda_N(k) = cos(pi k/(N+1))`, odd-`N` unique-zero, even-`N`
   no-zero control. Clean-room from arXiv:2311.11320 (path-graph = `B^1`). Cite
   the Betti-number papers, not only 2112.13501.
2. **OD3-min** (the decisive fork above), BEFORE OD1/OD2. Smallest odd `N`,
   explicit local boundary rule + boundary register, prove norm preservation and
   settle the boundary-mode dichotomy.

## Claim boundary (tighten the proposal's)

Keep the proposal's boundary, and ADD: the single-bulk-pole is a EUCLIDEAN
zero-mode / Betti-number statement (literature-backed for the contractible ball);
it is NOT yet a statement about the real-time unitary null-edge evolution, which
may carry boundary species. Say only "open non-torus spectral graphs remove the
periodic-momentum bulk doubling"; do NOT say the unitary null-edge causal diamond
has a single physical species until OD3-min + OD5 settle the boundary modes.

## Kill conditions (endorse + sharpen)

The proposal's six kill conditions are good. Sharpen #3 (norm-preserving update
needs nonlocal feedback) and add: **KILL the single-species reading if the
minimal unitary open-diamond update has a boundary-localized zero/pi mode that
cannot be gapped by a local, norm-preserving boundary term** - that is the
Euclidean-to-unitary gap where the partner hides.
